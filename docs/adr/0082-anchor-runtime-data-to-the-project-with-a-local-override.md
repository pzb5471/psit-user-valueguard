# 以项目根目录定位运行数据，并保留本机覆盖

本机 MVP 默认把 `runtime_data/` 定位到项目根目录，而不是执行命令时的当前目录，避免同一仓库意外生成多套 SQLite。`config/local.toml` 可以把 `storage.runtime_dir` 显式改为另一处本机目录，相对值仍按项目根目录解析，UNC、临时或不可写位置被拒绝且不静默回退；这样当前交接简单，未来迁移到 Windows 用户数据目录也不需要修改业务代码。

