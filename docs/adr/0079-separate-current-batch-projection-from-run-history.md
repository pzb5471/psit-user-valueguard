# 分开批次当前投影和首次运行历史

人工重跑不能改写已经结束的首次批量运行历史，但页面如果继续显示旧错误数，会与当前案例状态冲突。

`batch_runs` 的总数、成功数、异常数和内部终态保存首次批量分析历史，人工重跑不修改。`batches` 的业务状态、成功数和异常数表示当前案例投影，在案例状态变化的同一短事务中更新。`BatchWorkspaceView` 只显示当前投影。

不覆盖历史批次运行，不在页面混合历史与当前计数，也不从前端自行统计形成另一套真相。
