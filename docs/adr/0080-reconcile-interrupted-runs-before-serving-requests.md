# 在接收请求前收敛中断运行

进程被关闭或强制结束后，SQLite 中可能保留活动运行。若直接启动服务，页面会永久显示分析中；若自动续跑，又可能在用户不知情时继续产生付费调用。

数据库迁移后、HTTP 开放前，M3 在一个恢复流程中把遗留活动案例运行和批次运行标为 `INTERRUPTED`，把没有完整结果的案例标为 `PROCESSING_ERROR` 和 `APP_INTERRUPTED`，保留全部阶段与模型记录，并重新计算批次当前投影。该流程可重复执行且不产生 GLM 请求。

不从中间阶段恢复、不自动创建新运行、不删除半程技术记录。用户只能通过现有单案例重跑从头处理。
