# 从当前案例推导四种批次业务状态

内部批次运行具有启动、运行、失败和中断等技术状态，但页面只需要判断是否尚未开始、正在分析、已经完成或仍有当前异常。直接暴露内部状态会让用户理解执行器。

批次业务状态固定为 `PENDING_ANALYSIS`、`ANALYZING`、`COMPLETED`、`COMPLETED_WITH_ERRORS`，根据当前案例状态推导。任一案例人工重跑时批次显示分析中，结束后按当前错误重新计算。ZIP 校验失败不创建批次。

内部 `batch_run` 状态不进入业务 API。页面不显示任务排队位置、线程或估算等待时间。
