# 把共享合同作为单独审查的变更

Pydantic、OpenAPI、数据库迁移、状态枚举、ZIP 合同和视觉令牌会同时影响多个模块。若把这些变化夹在普通功能 PR 中，其他成员可能继续依据旧合同开发，直到联调才发现不兼容。

共享合同由一名主负责人维护，并由所有受影响模块审查。合同变更使用独立任务和 PR，同时提交变更原因、新旧差异、影响范围、生成文件和合同测试。普通功能任务不得顺手修改共享合同。

不建立无人负责的通用业务目录，不允许通过复制合同绕开审查，也不允许只改提供方而要求调用方在联调阶段自行适配。
