# 预期情况返回类型化结果，意外异常统一映射

导入拒绝、证据不足、重试耗尽、幂等命中和运行过期都属于产品可预期情况，不应以任意 Python 异常字符串穿过模块。程序缺陷、磁盘失败和合同之外的 SDK 异常则需要保留技术因果链供排障。

可预期情况返回严格、可区分的合同结果。意外异常由最近模块边界捕获并记录，M3 统一映射成 `BusinessError`，只向页面提供稳定编号、中文摘要、失败对象、业务阶段、下一步和 `trace_id`。

页面和跨模块合同不包含异常类名、堆栈、供应商原始响应或本机路径。证据不足继续作为正常业务结果，不进入处理异常。
