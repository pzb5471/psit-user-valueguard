# 由 M3 决定状态流转，M1 负责原子写入

M3 掌握完整用例和状态机，M1 掌握 SQLite、约束和事务。若 M3 直接操作 Session，会穿透 M1；若 M1 自行决定业务状态，又会把流程规则埋入存储层。

M3 决定批次和案例是否可以开始、阶段事件是否合法、完整结果是否可以发布、失败是否耗尽以及人工确认是否通过业务校验。M1 通过短事务原子创建运行、保存阶段和模型记录、发布结果、记录失败及保存人工确认。

M3 不创建或提交 Session，M2 不接触事务，GLM 等待期间没有数据库事务保持打开。写入失败不留下半条状态或半个决策包。
