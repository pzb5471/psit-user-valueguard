# 使用固定回环默认端口和显式本机覆盖

演示环境默认只监听 `127.0.0.1:8000`，开发环境默认另用 Vite `127.0.0.1:5173`。端口冲突时程序明确停止，不随机换号；成员可以在不入 Git 的 `config/local.toml` 中显式选择另一组已校验端口，启动脚本、FastAPI 和 Vite 代理读取同一配置。该做法同时保留可复现默认值和成员电脑发生真实端口冲突时的受控出口，不允许用 `0.0.0.0` 扩大部署范围。

