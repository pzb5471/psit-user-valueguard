# 使用可验证的本机启动器和当前实例专属停止请求

演示启动器只复用输入指纹一致且文件完整的前端构建，否则先在临时位置重新构建并在失败时停止；应用和数据库就绪后才按实际端口打开一次浏览器，异常中断的导入目录先进入有上限的隔离区，而不是直接删除排障材料。安全停止除 `Ctrl+C` 外，由 `scripts/stop.ps1` 写入携带当前 `instance_id` 的本机停止请求并复用应用正常关闭流程，不增加无认证 HTTP 关机接口，也不强制杀死正在写库或等待模型的进程。该边界兼顾启动速度、演示可用性、故障排查和误操作风险，同时保持业务页面与十个 HTTP 接口不变。
