# 只在真实迁移前自动生成有界 SQLite 备份

启动脚本只在现有数据库确有待执行 Alembic 迁移时自动备份，重要演示前可通过独立脚本手工备份，避免每次无变化启动都制造重复文件。备份使用 SQLite backup API 并通过 `PRAGMA integrity_check` 后原子发布；失败则不迁移，迁移失败保留恢复点，只有应用成功就绪后才清理为最近 10 份有效备份。该本机恢复点不冒充异机备份或企业级灾备。

