# 将产品交付划分为四个主责模块

四人小组需要四块能够独立推进的工作，同时产品运行链路又必须保持清楚责任。把每位成员分配到零散横向任务会持续发生同文件修改，把每个人做成一套完整前后端又会重复基础设施。

M1 负责数据与存储，M2 负责 AI 分析引擎，M3 负责业务服务与 API，M4 负责前端工作台。每个模块有单一主负责人、固定目录、明确公开接口和禁止进入的职责。M3 负责编排 M1、M2 并向 M4 提供业务 API，但不复制其他模块实现。

不增加第五个业务模块，不让 M1 调模型、M2 读数据库、M4 直接访问数据库或 GLM，也不把全部集成责任留给没有明确主责的公共任务。
