# 使用一个应用级执行器并在阶段之间停止

每个请求或 Agent 自建线程池会突破并发上限，正常关闭时继续启动新阶段又会增加不可预测调用。强行中断正在进行的网络线程在 Python 中也不可靠。

M3 在 FastAPI 生命周期中创建一个 `ThreadPoolExecutor`，并发从配置读取。关闭时先拒绝新任务、取消未开始 Future，正在进行的 GLM 请求等待返回或超时；M2 在下一阶段开始前检查应用级关闭标记，完整结果正常发布，未完成案例标为中断。强制退出由下次启动恢复。

浏览器关闭不取消任务，一个案例失败不取消其他案例。本期不引入分布式队列、断点续跑或用户停止按钮。
