# 使用简单分页和固定服务端排序

演示批次案例少，但历史导入会持续积累。完全不分页会让测试环境逐渐变慢；游标分页和任意查询语言又超过本机 MVP 需要。

历史批次使用 `limit=20`、`offset=0`，案例队列使用 `limit=50`、`offset=0`，两者最大 100。队列只允许单个运行状态和单个介入等级筛选，排序始终由后端按冻结规则执行，前端把筛选和分页位置放入 URL。

不提供全文搜索、任意 `sort_by`、复杂组合筛选和游标分页。列表统一返回 `items` 与 `total`。
