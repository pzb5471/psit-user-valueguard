# 用 Windows 系统锁保护每个运行数据目录

一个 `runtime_data/` 同时只允许一个 PSIT 进程。应用在接触数据库和 GLM 前持有 `psit.lock` 的操作系统独占锁，Windows 交付使用 Python 标准库 `msvcrt`，不增加第三方锁依赖；是否成功取得系统锁才是判断依据，遗留锁文件本身不能阻止异常退出后的重新启动。不同运行目录和不同端口仍可供开发者显式并行使用，但它们不共享数据。

