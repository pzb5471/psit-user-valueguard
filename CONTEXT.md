# 高价值客户异常售后决策支持

本文件只定义项目中的业务术语。需求、流程、技术方案和实现决策不写入本文件。

## Language

**售后风险案例**:
一名客户围绕一张具有明确售后对话或投诉描述的主要订单形成的处理单位。案例可以包含该订单内的对话、履约事实、付款事实和图片，并附带该客户可复算的交易汇总；本期不自动把多张订单合成一个案例。
_Avoid_: 客户全部历史、没有售后触发信息的订单、自动跨订单组案

**高价值客户**:
根据现有清洗后交易数据批量计算 RFM 后，满足以下相对规则的客户：R 不处于最久未购买的 25% 客户中，并且 M 位于前 20%，或者 F 至少为 2 且 M 不低于中位数。百分位分界随数据版本计算并保存，边界并列客户一并纳入。这里的“高价值”只表示客户在当前数据集和当前规则下呈现较高的相对交易价值，不表示客户个人净资产；RFM 只判断客户价值，不直接决定售后风险或介入等级。
_Avoid_: 高净值客户、VIP、系统预先指定的高价值客户

**普通客户对照案例**:
未达到高价值判定规则、仅用于开发和验收时比较 RFM 是否真实影响客户价值判断的案例。普通客户对照案例不进入面向售后运营人员的正常业务队列，也不能替代高价值客户的核心验收案例。
_Avoid_: 正常业务队列案例、高价值客户案例、扩大为一般售后分析

**客户流失风险**:
现有售后事件、客户情绪和交易与履约行为表明客户未来关系可能受损的状态。系统说明风险原因和证据，但不输出未经训练与真实业务验证的流失概率，也不声称客户一定会流失。
_Avoid_: 已经流失、精确流失概率、无证据风险标签

**人工决策闭环**:
运营人员检查系统提供的证据、判断和建议，并完成通过、修改或驳回，留下最终人工决定。闭环不包含真实联系客户、执行退款或补偿，也不包含后续留存效果验证。
_Avoid_: 业务执行闭环、留存效果闭环

**证据不足**:
现有输入不足以支持可靠结论的案例状态。系统必须指出缺少的证据和不能成立的结论，再交给人工处理；该状态属于合格输出，不等同于系统运行失败，也不自动等同于暂不介入。存在可信但可能严重的问题时，仍应建议或要求人工介入核验。
_Avoid_: 强制生成结论、系统故障、因缺少证据而默认暂不介入

**案例触发事件**:
投诉、差评、退款、物流异常或商品问题等明确发生的售后事件。只有出现案例触发事件，系统才建立售后风险案例；本期不持续扫描全部客户并主动发现异常。
_Avoid_: 任意行为波动、全量异常扫描

**人工处置决策包**:
一个售后风险案例必须交付的完整结果，由系统建议介入等级、风险原因判断和处理方案组成。三个部分同等重要；证据不足时仍需保留对应位置，并写明无法判断的内容和缺失证据。
_Avoid_: 三段互不关联的分析文本、单一风险分数

**系统建议介入等级**:
系统根据客户价值、售后事件严重程度和紧迫性、当前情绪及证据状态，对案例给出的三档建议。语义判断先提出等级，固定业务底线再防止明显低估：“必须介入”表示存在未解决的明显损失、严重履约或商品问题、退货退款争议；严重问题存在冲突或证据不足时至少“建议介入并人工核验”；只有问题已确认解决或没有可执行风险事项时才允许“暂不介入”。高价值客户身份本身不能单独触发“必须介入”。每个等级必须说明理由，不包含未经真实业务规则确认的处理时限。
_Avoid_: 高中低风险分数、自动生成的 SLA

**介入等级最低要求**:
固定业务底线针对当前事件允许的最低介入等级。语义判断低于该等级时，系统采用最低允许等级并记录调整原因；该规则只防止明显低估，不代替风险原因和处理方案判断。
_Avoid_: 完整规则评分模型、静默修改、仅因高价值身份判为必须介入

**风险原因假设**:
对案例风险来源的排序判断。原因使用物流履约问题、商品问题、退货退款问题、服务沟通问题、价格或权益问题、其他六个业务类别，另设非原因兜底值 `INSUFFICIENT_EVIDENCE`。面向运营人员时，先给一句风险结论，再说明一个主要原因、支持证据、存在的反证和仍不能确认的内容；只有证据确实同时指向另一类原因时，才增加一个备选原因。原因不能表述为已经证明的因果事实。
_Avoid_: 确定原因、分类外自由命名、为填满字段强行生成备选原因、只给结论不留依据

**受约束处理方案**:
从预先批准的策略范围内选择的处理建议，包含系统建议介入等级、具体处理动作、与客户沟通时应说明的重点、适用条件与限制。证据不足时可以建议补充证据，但不得自由生成或自动执行真实业务动作。`NO_ACTION_MONITOR` 只与“暂不介入”搭配；“建议介入”或“必须介入”不能只给暂不行动，“暂不介入”也不能同时给出需要实际推进的处理动作。
_Avoid_: 自由生成策略、只有方向没有动作、自动执行动作

**交易与履约行为数据**:
现有清洗后数据能够提供的客户价值、交易和订单履约事实，包括 RFM 高价值结论、订单状态、付款总额，以及下单、批准、发货、预计送达和实际送达时间。现有数据不能稳定支持近期与基准期的行为变化，付款方式和分期信息也不作为 v1 风险判断输入。浏览、点击、加购、页面停留和未经现有数据支持的流失概率不属于本期行为数据。
_Avoid_: 网站行为埋点、行为变化窗口、付款分项、凭空生成的行为趋势、未经验证的流失分数

**订单级组案**:
本期以一张主要订单为边界，将该订单内可核验的对话、履约、付款和图片证据组成一个售后风险案例，并附客户级交易汇总。不同订单分别建立案例；只有未来存在可靠工单编号或问题关联标识时，才允许自动跨订单组案。
_Avoid_: CSV 每行一个案例、自动跨订单组案、同一客户永远只有一个案例

**策略范围**:
本期允许系统选择并组合的六类处理动作：补充或核实图片、物流、订单或退款证据；建议人工联系客户并给出沟通重点；建议升级物流或订单履约处理；建议补发、退货或退款核验；提交但不执行道歉、补偿或挽留申请；暂不采取动作并说明观察或复查理由。每个决策包最多选择三项；具体金额、权益和执行规则不由系统编造。动作目录具有独立版本，运行结果保留当时使用的目录版本。
_Avoid_: 无边界策略、自动发券、自动退款、自动联系客户

**产品侧人工最终确认**:
售后运营人员对系统建议介入等级、风险原因假设和受约束处理方案承担最终业务判断。全部认可时可以一键通过；修改时只能在已定义的等级、原因类别和批准动作中形成结构化结果，并填写修改原因；驳回时必须填写原因，并补充最终人工判断或标记证据不足。系统原结果与最终人工结果分别保存。
_Avoid_: 任意编辑内部 JSON、人工重新完成全部分析、开发侧人工验收、无痕改写

**驳回决策包**:
运营人员判断系统生成的人工处置决策包不可采用。驳回不代表客户无需介入；运营人员必须填写原因，并补充最终人工判断或将案例标记为证据不足。
_Avoid_: 暂不介入、无结果关闭案例

**原始观察（observed）**:
数据集或案例中直接存在的原始事实。每条原始观察必须保留来源；源数据存在发生时间时原样保留，不存在时不得补造。原始观察不包含系统应当完成的判断。
_Avoid_: 推测、总结、推荐动作

**派生事实（derived）**:
通过固定公式、代码或明确规则，由原始观察计算出的可重复结果，例如 `recency_days` 和 RFM 分数。派生事实必须能追溯到原始字段，不能包含情绪、事件总结、风险原因、证据总结或推荐动作。
_Avoid_: AI 判断、无法复算的结论

**模拟关系（simulated）**:
为了演示而人工建立的客户、文本、图片或事件之间的关系。模拟关系必须明确标记，说明被连接内容各自的真实来源，不能伪装成同一真实客户或真实事件。
_Avoid_: 真实关系、无标记的数据拼接

**MVP 模拟数据环境**:
本期工作台使用项目组已经整理好的数据，不接入企业实时业务数据。工作台必须统一提示当前为模拟数据环境；具体证据仍需分别标记为原始观察、派生事实或模拟关系，不能用“Mock 数据”抹平三者差异。
_Avoid_: 真实生产数据环境、全部凭空生成的数据、无来源区分的 Mock 数据

**核心输入模态**:
本期 MVP 必须处理的三类输入：文本、图片和行为数据。图片是必做能力，不是可有可无的附件；音频和视频不属于本期范围。
_Avoid_: 纯文本产品、音频模态、视频模态

**多模态验收案例**:
用于证明多模态能力完成的新案例，必须同时包含文本、图片和行为数据，不得预写目标答案。图片分析结果必须实际参与事件整理、风险原因假设或受约束处理方案，并能追溯到具体图片证据。
_Avoid_: 固定页面结果、只上传但不使用图片

**无图片案例**:
真实业务中没有图片证据的售后风险案例。系统可以降级处理，但必须标明图片证据缺失；该类案例不能用于证明多模态能力完成。
_Avoid_: 多模态验收案例

**图片观察**:
从图片中识别出的可观察事实，必须关联具体图片证据并说明不确定性。图片模糊或无法识别时必须明确失败，不能根据配套文本反向编造图片内容。
_Avoid_: 客户身份推断、客户情绪定论、无证据的风险原因

**图片特征**:
系统从图片中提取并结构化记录的最小业务信息，包括识别状态、最多三条可观察事实、图片与客户陈述的关系、关联文本证据和不确定性。图片只提供可观察事实，不要求输出当前数据没有参考口径的主体分类、异常位置或可见程度，也不参与客户情绪判断。
_Avoid_: 模型内部向量、无证据标签、固定图片分类学、客户身份推断、从图片猜客户情绪

**图片特征字段**:
承载图片特征的结构化输入输出合同。需求阶段只确定必须表达的业务信息；字段名、数据类型、枚举值、校验规则和模型映射在后续合同与技术文档中确定。
_Avoid_: 在需求阶段绑定 CLIP、RAG、训练方法或具体模型

**多模态证据联合判断**:
系统分别提取文本、图片和行为特征，再按案例与事件对齐；只有源数据存在可靠时间时才做时间对齐。系统判断不同模态之间是相互支持、相互冲突还是信息缺失，并据此形成事件状态、风险原因假设和受约束处理方案。每项重要判断必须标明使用的文本、图片和行为证据。
_Avoid_: 分别展示三类结果、固定要求特征级模型融合

**模态证据冲突**:
文本、图片或行为数据对同一事件给出不一致信号的状态。系统必须显示冲突并降低确定性或交给人工判断，不能静默忽略任一模态。
_Avoid_: 自动选择更有利的证据、隐藏冲突

**原始数据**:
项目获得但尚未完成清洗和关系核验的数据。原始数据可以缺少 ID、存在重复、格式不一致或字段缺失；处理过程中必须保留原始内容，不能被清洗结果覆盖。
_Avoid_: 标准案例包、可以直接运行的案例输入

**清洗后数据**:
项目组已经完成基础解析、格式处理、表间关联和部分特征工程的数据及其处理逻辑，是本期继续完成 RFM 核验、案例候选构造和标准案例准备的工程基线。清洗后数据不等于原始数据，也不等于已经可以直接进入产品的标准案例包。
_Avoid_: 原始数据、最终 CaseInput、可以直接运行的标准案例包

**数据处理流程**:
把原始数据先转换为清洗后数据，再将清洗后数据转换为标准证据记录和标准案例包的可重复流程，包括解析、清洗、去重、格式转换、来源标记、派生计算和关系核验。本期从现有清洗后数据继续，不重复建设完整原始数据清洗；人工处理歧义时必须留下决定记录。
_Avoid_: 私下修改数据、不可复现的手工拼接

**标准证据记录**:
现有清洗后数据经过来源核验和必要适配后形成的最小可追溯证据单位。`evidence` 固定分为 `text_items`、`image_items` 和 `behavior_items`；每项共同保存稳定 `evidence_id`、数据身份、关系身份、来源和内容哈希。文本证据定位到具体消息、顺序和说话方；图片证据定位到具体文件；行为证据保留事实值、来源字段和必要的计算规则。某类证据只有在源数据存在可靠时间时才保存时间，不为消息或图片统一填充空时间。证据编号由组包程序生成并在重跑时保持不变，Agent 只能引用已有编号，不能创建证据。
_Avoid_: 无来源数据、只引用整批文件、观察事实与系统推断混写、预写目标答案、模型自造 evidence_id

**陈述状态**:
事件还原中用于说明一句信息目前属于客户声称、客服陈述或承诺、客户确认结果、图片直接观察，还是程序派生事实的字段。客服说“已退款”只能算客服陈述；客户后续明确说“退款收到了”才能算客户确认结果。
_Avoid_: 把所有对话内容直接写成已证实事实、把承诺当完成

**标准案例包**:
从现有清洗后数据生成并固定版本的工作台运行输入。物理上是一个 ZIP 文件，包含批次清单、逐案例 `CaseInput v1`、案例实际引用的图片和文件校验值，不包含原始大 CSV、完整图片库或验收参考答案。每个案例对应一张主要订单，重复付款行已经合并；案例包包含稳定 `case_id`、脱敏客户标识、客户价值结论、案例触发信息、标准证据记录、来源与模拟标记。图片可以缺失，但必须存在有效售后对话或投诉描述。
_Avoid_: 未清洗原始文件、整张总表直接运行、随机变化的案例映射、人工预写分析结果、把验收答案放入运行 ZIP

**Mock 映射**:
为了验证产品流程，把来自不同真实数据源的交易事实、售后对话或图片按照固定清单组成模拟案例的关系，数据身份代码为 `mock_mapped`。每个组成部分仍保留自己的真实来源，但组合关系不代表真实客户或真实订单关系。当前映射一经写入 `mock_dataset_v1` 就不再随机变化。
_Avoid_: 真实客户关系、关系未确认、无标记随机拼接

**运行案例包**:
工作台唯一允许导入的标准案例 ZIP，只包含运行所需字段和证据。导入器必须递归拒绝 `mood`、`reason`、`solution`、`image_verification`、`key_answer`、`label`、`database_gt`、`trajectory`、`user_profile_st1`、`user_profile` 和 `question_type` 等答案或答案泄漏字段，并拒绝未规范化的 `user_address`、`database` 和重复的 `first_query`。
_Avoid_: 验收参考包、开发测试答案、完整 `service_tasks.json`

**验收参考包**:
与运行案例包物理分离、只供测试程序读取的参考结果，保存人工参考答案或现有答案性质字段。它不放入产品运行目录，不被产品导入器接收，也不发送给任何 Agent。
_Avoid_: 运行案例包、Agent 输入、业务页面数据源

**CaseInput v1**:
标准案例包中单个案例进入运行流程时必须满足的固定输入合同。顶层只保留 `schema_version`、`data_version`、`batch_id`、`case_id`、`data_identity`、`primary_order`、`customer`、`customer_value`、`evidence` 和 `provenance`。必需内容包括版本与标识、主要订单、脱敏客户、高价值结论及内部复算信息、订单状态、下单时间、付款总额、至少一条脱敏客户售后消息和逐项来源。履约时间和图片按源数据有无选择性出现；不设置币种、付款分项、案例触发、模态状态、行为变化和模拟等待时间等可删除、可推导或无来源的输入字段。
_Avoid_: 整张清洗总表、缺少售后消息的订单、用 `null` 填满无来源字段、同义顶层字段并存、为演示功能补造业务事实

**关系未确认**:
现有信息不足以证明两条数据属于同一客户或同一事件的状态。关系未确认的数据不得强行合并进同一标准案例包。
_Avoid_: 模拟关系、真实关系、强制拼接

**数据处理失败**:
原始数据因无法解析、来源丢失或关键关系无法核验，不能形成合格标准证据记录或标准案例包的状态。该状态不同于案例已经建立后的证据不足。
_Avoid_: 证据不足、系统分析失败

**变通处理**:
原始数据不规范时采用的受约束处理，包括格式清洗、字段映射、去重、生成内部追踪 ID、计算可复现的派生事实，以及为演示建立明确标记的模拟关系。变通处理不得补造原始事实、伪造真实关系或提前写入系统应当输出的答案。
_Avoid_: 复制扩增冒充真实案例、无标记嫁接、AI 补写原始事实

**上游数据工作**:
由团队数据负责人基于现有清洗后数据继续完成的关系核验、RFM 方法核验、必要修正、特征准备和数据源专项处理。同源原始表只用于追溯、核验和必要补漏，不重新启动完整清洗工程；具体输出字段由清洗后数据实际支持情况决定。
_Avoid_: 重新建设完整原始数据清洗、产品运行时分析、按理想 Schema 反向补造字段

**产品数据接入**:
产品接收上游处理结果后完成的校验、来源保留、少量格式适配和案例组装。产品数据接入不重复完整数据清洗与数据挖掘，具体输入合同等待真实处理样本后冻结。
_Avoid_: 上游数据工作、任意原始文件自动治理

**事件还原**:
根据文本、图片和行为证据，按时间说明客户经历了什么、涉及哪些订单和售后事件，以及关键事件之间的关系。事件还原必须区分已观察事实、派生事实、模拟关系和系统推断。
_Avoid_: 无证据故事、情绪标签汇总

**情感感知结果**:
基于事件还原形成的客户情绪解释，v1 只要求当前可能的情绪、一句解释、客户文本证据和不确定性。情绪结论只能引用客户文本证据；图片和行为数据只能支持触发事件，不能证明客户情绪。情绪变化、目标情绪和备选解释不是 v1 必填字段；每项判断必须能追溯到证据，证据不足时允许无法判断。
_Avoid_: 读心结论、只给情绪分数、为了完整补写故事

**情绪摘要**:
对情感感知结果的内部简短索引。当前情绪只允许平静、不耐烦、无法判断，与现有人工参考字段保持一致；业务页面不单独展示情绪卡片，只在确实影响风险判断时把业务含义融入“为什么存在流失风险”。图片和行为数据可以说明触发事件，但不能用来猜测客户情绪。
_Avoid_: 独立情绪看板、任意扩展情绪标签、情绪变化必填、单一情绪分数、从图片猜客户内心、准确性证明

**还原精准**:
事件与情绪还原同时满足事实忠实、证据可追溯、客户与事件关系正确、情绪解释符合人工参考答案，以及面对缺失或冲突时诚实表达不确定性。还原精准不代表系统读取了客户真实内心。
_Avoid_: 单一情绪分数、绝对准确、输出完整但无证据

**人工参考答案**:
由至少两人独立阅读案例证据后形成、与系统运行输入分开保存的验收依据，包含事件事实、合理情绪解释和允许的不确定性。现有数据中的 `solution`、`mood`、`reason`、`image_verification`、`key_answer`、`label`、`database_gt`、`trajectory`、`user_profile_st1`、`user_profile` 和 `question_type` 等答案或答案泄漏字段也按人工参考答案管理，任何 Agent 不得读取。判断存在分歧时，需要先形成共同验收依据再评估系统结果。
_Avoid_: 写入系统输入的标准答案、Agent 可读取的隐藏答案、唯一真实情绪

**开发侧人工验收**:
团队成员检查 Coding Agent 产出的代码、测试和功能是否符合要求。开发侧人工验收不属于售后运营人员使用产品的业务流程。
_Avoid_: 产品侧人工最终确认

**MVP 核心流程**:
标准案例包进入产品后，依次完成三类模态特征提取、事件与情绪还原、人工处置决策包生成和产品侧人工最终确认。五个环节共同构成端到端完成，不能用彼此独立的静态页面代替。
_Avoid_: 静态演示、自动执行真实业务动作

**案例队列**:
售后运营人员决定“先处理谁”的工作入口。队列一次只展示一个批次，默认选择最近导入批次，并允许切换历史批次；不同批次、不同数据版本的案例不得混合排序。每个案例展示脱敏客户标识与高价值客户标识、风险摘要、系统建议介入等级与排序理由、运行状态和系统导入时间；不展示 RFM 分数、分位、权重和阈值。证据冲突、证据不足和模态识别失败需要醒目标记。存在最终人工介入等级时按人工等级排序，否则按系统建议等级排序；同一等级先按系统导入时间、再按稳定 `case_id` 排序。MVP 只按介入等级和运行状态筛选，不扩展成完整 CRM。
_Avoid_: 单案例演示页、客户资料库、完整 CRM

**业务错误提示**:
面向运营人员显示的可处理错误信息，包含中文摘要、失败对象、失败环节、稳定错误编号和允许的下一步。模型供应商原始响应、SDK 错误、程序堆栈、请求参数、API Key 和内部文件绝对路径只保存在技术记录中。
_Avoid_: 只有“运行失败”、直接显示原始异常、泄露密钥或内部路径

**案例详情**:
售后运营人员决定“怎么处理”的工作页面，按“这是谁、为什么存在流失风险、应该怎么做”组织内容，包含案例概览、风险原因与关键证据、受约束处理方案和产品侧人工最终确认。页面不按感知 Agent、归因 Agent、策略 Agent 的技术阶段组织，也不展示模型、Prompt 和内部运行方法；每项重要业务判断仍能按需查看对应证据。
_Avoid_: Agent 运行看板、技术分析过程、案例队列、静态分析报告

**最终人工介入等级**:
售后运营人员确认或修改系统建议介入等级后形成的最终排序依据。人工修改必须填写原因，同时保留系统建议等级和排序理由；案例队列按最终人工介入等级重新排序。
_Avoid_: 无记录拖动排序、覆盖系统原建议

**案例运行状态**:
案例在业务页面中的处理阶段，只允许待分析、分析中、待人工确认、已完成和处理异常五种状态。处理异常表示数据读取、模态识别或系统运行没有正常完成。感知、归因、策略等内部阶段只用于技术记录，不作为业务状态展示。
_Avoid_: 人工审核结果、证据不足、Agent 阶段、模型调用日志

**案例运行版本**:
同一案例每次完整或重新分析形成的不可覆盖结果。处理异常和待人工确认案例可以重新分析并生成新版本；已经完成人工确认的案例在业务页面保持只读，不再重新分析。旧系统结果、对应人工决定、失败原因和操作时间必须保留并能够读回。
_Avoid_: 原地覆盖、已完成案例重新分析、重跑后丢失人工决定、只有最新结果

**批次运行**:
运营人员一次明确执行“开始批量分析”形成的运行记录，保存批次整体状态、案例总数、完成数、异常数和开始结束时间。它只负责组织本次批次执行，不直接保存单个案例的 Agent 输出。
_Avoid_: 与案例运行混用、把单案例结果写入批次记录

**案例运行**:
一个案例的一次完整分析或重新分析记录。首次批量分析形成的案例运行隶属于对应批次运行；人工单案例重跑不计入原批次运行，而是通过 `previous_case_run_id` 连接上一个案例运行。感知、归因、策略阶段结果和模型调用都关联具体案例运行；人工确认同时指向案例和被审核的案例运行。
_Avoid_: 用一条批次运行代替全部案例运行、覆盖旧运行

**当前结果指针**:
`cases` 表中指向当前有效 `case_run` 和当前人工确认的引用。案例重新分析完成后在一个数据库事务中切换指针，旧运行、旧结果和旧人工记录继续保留；页面默认读取指针所指版本。
_Avoid_: 复制整份当前结果、删除旧版本、让页面自行猜测最新版本

**人工审核结果**:
案例完成产品侧人工最终确认后的业务结果，包括通过、修改后通过、驳回并填写人工判断，以及证据不足。证据不足是有效业务结果，不属于处理异常。
_Avoid_: 案例运行状态、系统故障

**批量预分析**:
一批标准案例包校验并导入后先停在待分析状态。运营人员查看批次摘要并执行一次“开始批量分析”，系统才分别完成特征提取、事件与情绪还原、系统建议介入等级和排序理由。同一时间只允许一个活动批次；其他批次保持待分析，不自动排队或自动开始。全部案例立即出现在队列中；每个案例完成后即可进入详情和人工确认，不等待整批结束。单个案例失败不得阻断同批其他案例。v1 不提供停止或取消运行。
_Avoid_: 逐个打开后才分析、等待整批完成才展示、实时企业数据接入

**本机网页工作台**:
安装和运行在一台演示电脑上的网页产品，默认通过该电脑浏览器访问 `http://127.0.0.1:8000`。它具有真实后端调用、持久化和交互流程，一个运行数据目录同一时间只允许一个进程使用；本期不承诺局域网多人访问或公网部署。
_Avoid_: 纯静态网页、公网生产系统、企业多用户系统

**工作台页面路由**:
浏览器可刷新、可返回的页面地址。`/` 是工作台入口，没有批次时显示 ZIP 导入，有批次时进入最近批次；`/batches/:batchId` 展示所选批次摘要、进度和案例队列；`/batches/:batchId/cases/:caseId` 展示案例详情、证据和人工确认。ZIP 导入仍属于工作台，不建设独立数据管理系统。
_Avoid_: 仅靠页面临时状态切换、独立登录页、模型管理页、技术日志页

**REST API v1**:
React 页面与 FastAPI 后端之间的固定接口边界，统一以 `/api/v1` 开头。v1 只提供批次上传与查询、明确启动分析、批次案例查询、案例详情、允许的案例重跑、人工确认、证据内容和健康检查，不提供通用搜索、删除、模型配置或技术日志接口。
_Avoid_: GraphQL、无版本接口、一个包办全部动作的通用接口

**接口返回合同**:
单个对象直接返回真实对象，列表返回 `items` 和 `total`，创建或修改返回保存后的记录。业务错误统一返回稳定 `code`、中文 `message`、失败对象、业务可理解的 `stage`、可执行 `next_action` 和排障用 `trace_id`；不使用只有 `success: true` 的空响应。
_Avoid_: 每个接口自造格式、只返回成功布尔值、把供应商原始错误返回页面

**SQLite 混合存储**:
数据库用独立列保存需要查询、排序、去重和建立约束的标识、状态、等级、计数、时间、校验值、相对路径和当前指针；版本化业务输入、Agent 结果、人工修改、模型原始响应和错误详情整体保存为 JSON。核心使用 `data_versions`、`batches`、`cases`、`evidence`、`batch_runs`、`case_runs`、`stage_results`、`model_calls`、`reviews` 九张表。
_Avoid_: 所有内容塞进一个 JSON、把 Agent 输出拆成几十张小表、完整 CRM 表

**数据库历史边界**:
当前运行状态、进度计数、开始结束时间和当前结果指针可以更新；已经形成的阶段结果、模型调用、结束后的案例运行、人工确认和失败详情只追加、不覆盖。这里的追加历史不等于分析中的状态永远不能更新。
_Avoid_: 原地改写历史结果、把所有字段都设计成不可更新

**运行数据目录**:
不进入 Git 的本机数据根目录，默认是项目根目录下的 `runtime_data/`，包含 `psit.db`、单实例锁、临时导入目录、正式批次文件和技术日志；成员可以通过本机配置显式迁移位置，但不能由当前命令目录决定或失败后静默切换。固定运行夹具进入 `tests/fixtures/runtime/`，密封人工参考答案进入 `tests/fixtures/sealed/`，两者不能被产品运行输入混用。
_Avoid_: 图片存数据库 BLOB、把运行数据库提交 Git、按当前目录生成第二套数据、把验收答案放进运行包

**MVP 流程验收集**:
最少包含 5 个未预写目标答案的案例，分别覆盖必须介入、建议介入、暂不介入、证据冲突，以及模态缺失或识别失败。案例一起进入批量预分析并完成队列排序和产品侧人工最终确认；该验收集只验证流程和关键分支，不证明真实准确率或商业效果。
_Avoid_: 模型训练集、准确率证明、固定页面结果

**案例批次导入**:
MVP 用于测试和演示的最小数据入口。用户通过浏览器选择一个标准案例 ZIP，系统在临时区域完整校验案例数量、模态、来源标记和必需内容，再导入并展示批次摘要。固定演示包也必须经过同一个入口，不能使用预写结果或隐藏捷径。导入本身不调用模型，只有明确执行“开始批量分析”后才进入批量预分析。该入口不清洗任意原始文件，也不代表正式产品中售后运营人员需要手工上传数据。
_Avoid_: 填写服务器文件路径、原始数据上传、正式业务流程、绕过校验的固定预置案例

**隔离导入残留**:
程序异常中断后留下、尚未发布且未被数据库引用的临时导入目录。本期不建设隔离区、恢复元数据和保留策略；这类残留不进入批次列表、不被证据接口提供、不能自动恢复成正式批次，程序也不得因清理临时残留而删除陌生文件或正式批次。
_Avoid_: 半成品批次、自动恢复批次、正式导入数据、隔离区保留策略

**原子导入**:
系统先在临时目录完整解压并校验一个标准案例 ZIP，只有全部案例、图片、校验值和禁用字段检查都通过后，才一次性写入正式文件目录和数据库。任一项失败时整批拒绝，不留下半个批次。
_Avoid_: 合格案例部分导入、失败后残留文件、边校验边正式写入

**导入幂等**:
同一个标准案例 ZIP 被重复提交时，系统依据文件校验值返回已经存在的批次，不重复创建案例、证据或运行记录。`batch_id` 在系统内全局唯一；已有 `batch_id` 对应不同内容时必须拒绝，并要求生成新的批次标识。
_Avoid_: 重复案例、静默覆盖批次、只按文件名判断重复

**操作幂等**:
浏览器因重复点击或网络重试再次提交同一操作时，系统不得重复调用模型或重复保存业务结果。同一批次正在运行或同一案例正在重跑时，重复请求返回对应资源的当前业务视图，不向页面暴露内部运行编号；人工确认使用唯一 `submission_id`，相同编号重试返回原结果，已完成案例的新提交被拒绝。
_Avoid_: 重复付费调用、重复人工结果、依赖按钮临时禁用保证唯一性

**证据内容接口**:
页面通过 `batch_id`、`case_id` 和 `evidence_id` 获取属于当前案例的图片，不接触本机绝对路径。后端按文件真实内容识别 JPG、JPEG、PNG、GIF，确认文件位于正式批次目录且证据归属匹配；GIF 页面展示原文件，模型使用静态首帧。
_Avoid_: 直接公开文件目录、只按扩展名判断、允许 ZIP 路径越界

**判断依据**:
支撑事件还原、情感感知结果、风险原因假设和受约束处理方案的具体文本、图片或行为证据。运营人员可以在案例详情中展开查看原始内容、来源、派生规则、模拟标记和不确定性；源数据存在时间时同时显示时间，不需要离开页面查找原始文件。
_Avoid_: 只有内部编号、无法展开的 AI 结论

**特征展示规则**:
案例详情默认只展示高价值客户结论、客户流失风险原因、建议处理方案、关键证据和不确定性提示。运营人员核验风险判断时，可以通过“查看判断依据”按需展开业务可读的原始证据；RFM 计算过程、分数、分位、权重、阈值、Agent 分工、模型版本、Prompt 版本、阶段耗时、模型向量、Embedding 和其他技术参数只保留在内部记录中，不向业务用户展示。
_Avoid_: 默认铺开内部特征、完全隐藏业务证据、向业务用户解释技术方法

**业务结果视图**:
工作台面向运营人员的唯一内容投影。除完成操作所需的五种业务状态和可处理错误外，只回答三个问题：谁是高价值客户、为什么有流失风险、应该怎么做。批次、案例和证据标识只用于页面定位，不作为业务内容突出展示；数据库字段、RFM 方法、运行编号、数据版本、Schema、Agent 阶段、Prompt、Token、模型调用、技术日志和原始 JSON 不进入业务页面。
_Avoid_: 把数据库详情当产品内容、把分析方法当客户价值、展示技术运行看板

**业务响应 DTO**:
FastAPI 专门为工作台定义的返回结构白名单，包括批次工作台、案例队列项、案例详情、人工确认结果和业务错误。业务 DTO 从数据库与当前有效结果组装，不直接序列化 SQLAlchemy 对象，也不提供阶段结果和模型调用的业务查询接口。
_Avoid_: 数据库对象直接返回、通用任意 JSON、开发模式泄露技术字段

**当前有效业务结果**:
页面应该采用的业务结论。案例未完成人工确认时使用系统建议；完成确认后使用人工最终结果。人工修改过的案例默认展示人工结果，系统原建议和修改原因只在折叠的“查看系统原建议”中提供；没有修改时不重复展示两份相同内容。
_Avoid_: 队列继续展示过时系统建议、人工与系统结果无差别并排

**业务证据投影**:
只把当前业务结果实际引用的客户对话、售后图片、订单与客户价值事实转换成业务可读内容。页面可显示证据支持或冲突了哪项判断、实际缺失和不确定性，以及必要的 Mock 模拟关联标记；不显示证据内部编号、哈希、文件路径、内部身份字段、来源 JSON 和模型参数。
_Avoid_: 默认铺开全部证据记录、隐藏支撑结论的证据、展示内部证据结构

**结果发布门槛**:
感知、归因、策略三个阶段的最终结构全部通过 Schema 与程序规则后，案例才一次性发布完整业务结果并进入待人工确认。分析期间和任一后续阶段失败时，业务页面不展示已经形成的内部半成品；内部阶段结果仍保存用于排障和允许的重新分析。
_Avoid_: 边生成边展示、只完成原因就让用户处理、失败时展示残缺决策包

**阶段受限输入**:
从当前 `CaseInput v1` 和已通过的上游结果生成、只供某一个 Agent 调用的最小视图。感知阶段读取对话、图片和核验后的订单履约事实；归因阶段读取感知结果及其引用的文本和订单事实，不重复传原图；策略阶段读取感知、归因、高价值结论、证据编号和动作约束。RFM 过程、其他案例和验收答案不进入任何阶段。
_Avoid_: 三个 Agent 都读取完整案例、跨案例记忆、后阶段结果反向污染前阶段

**GlmClient**:
产品中唯一允许直接调用智谱官方 SDK 的薄接入层，负责鉴权、请求发送、JSON 输出模式、超时、网络重试、供应商请求编号、用量和原始响应记录。Agent 负责业务 Prompt，`RunService` 负责流程、校验、状态和持久化；`GlmClient` 不做客户价值、风险原因或处理动作判断。
_Avoid_: 每个 Agent 各写一套 SDK 调用、客户端直接改数据库、在业务层散落 API Key

**Prompt 与 Schema 版本**:
三个 Prompt 分别保存在 `prompts/perception/v1.md`、`prompts/attribution/v1.md`、`prompts/strategy/v1.md`。Pydantic 模型是字段结构唯一来源，程序从模型生成 JSON Schema 注入 Prompt；每次调用记录 Prompt 语义版本和文件 SHA-256。页面不提供 Prompt 编辑功能。
_Avoid_: Prompt 与字段清单各自维护、无版本修改、Prompt 包含固定答案

**阶段结果严格校验**:
模型响应依次通过 JSON 解析、Pydantic 严格字段、枚举、案例身份、证据归属、跨阶段引用、禁用内容和程序业务规则校验。未声明字段拒绝，类型不宽松转换；结构合格但业务仍有不确定性时进入人工确认，不为了得到更漂亮的答案自动重跑。
_Avoid_: 只要是 JSON 就接受、静默丢字段或改类型、按主观喜好无限重试

**单阶段请求上限**:
一次阶段执行最多产生四次供应商请求：首次一次、网络或临时服务故障最多重试两次、收到不合格响应后定向修复最多一次。修复请求发生网络错误后不继续重试；每次尝试都进入 `model_calls`。
_Avoid_: SDK 和业务层双重重试、修复请求无限递归、失败次数不可追溯

**模型 PoC**:
使用固定 10 个演示案例和密封 5 个验收案例，对 GLM 配置做的小规模运行验证。选定配置必须完整运行 15 个固定案例的 45 个阶段，每个阶段留下真实调用或明确失败记录；不得出现跨案例或虚假证据、目录外动作和验收答案泄漏。PoC 记录首次结构通过率、修复后通过率、失败类别、耗时、Token、实际调用次数、429 和 SQLite 写入情况；未实测前不把具体通过率、首例耗时或整批耗时写成硬门槛，正式阈值只能从真实记录产生。5 个密封案例均须由两名验收者判断为可作为人工决策初稿，两个图片贡献案例和一个行为贡献案例须达到规定差异。这些结果只证明固定案例链路可用，不代表生产准确率。
_Avoid_: 把初始参数宣称为最佳配置、只看一例、修改密封答案、删除失败案例、把固定案例表现包装成真实业务效果

**项目运行配置**:
团队默认非敏感参数进入 `config/default.toml`，成员电脑的显式覆盖进入不提交的可选 `config/local.toml`，两者由 `pydantic-settings` 合并校验，覆盖模型、重试、执行器、轮询、存储、端口和日志。智谱密钥只从 `ZAI_API_KEY` 读取；配置值不合法时启动失败，不能静默更换目录、端口或模型继续运行。
_Avoid_: API Key 写进源码、Prompt、TOML 或页面，把超时和并发散落在 Agent 代码中，自动随机端口

**分析服务就绪状态**:
工作台、数据库和模型分析能力分别报告状态。缺少 `ZAI_API_KEY` 时，工作台仍可打开并读取历史数据，但开始分析和重新分析不可用，页面只显示业务可理解的处理提示；`/health` 可报告应用、数据库和分析服务是否就绪，但不得返回密钥或技术原始错误。
_Avoid_: 缺少密钥导致整个工作台崩溃、把密钥值或供应商响应放进健康检查

**开发工具链**:
后端统一使用 Python 3.12、`pyproject.toml`、`uv` 和 `uv.lock`；前端统一使用 Node.js 24、pnpm 11、`package.json` 和 `pnpm-lock.yaml`。仓库提供 `scripts/bootstrap.ps1`、`scripts/dev.ps1`、`scripts/test.ps1`、`scripts/start.ps1` 四个入口，并从项目相对路径发现工具，不写任何成员电脑或 Codex 缓存的绝对路径。
_Avoid_: 只在某一台电脑可运行、依赖未锁定、脚本硬编码 WindowsApps 或个人缓存路径

**结构化技术日志**:
应用把技术运行事件按 JSONL 写入 `runtime_data/logs/app.jsonl`，每条至少含时间、级别、`trace_id`、批次、案例、运行、阶段、事件、状态、耗时和稳定错误编号。单文件 10 MB 轮换并保留 5 份；同一次请求到模型调用和数据库写入共用 `trace_id`。日志不记录 API Key、图片 base64、完整客户对话、完整 Prompt 或本机绝对路径，模型原始响应只保存在 `model_calls`，不重复写日志。
_Avoid_: 自由文本无法检索、技术日志出现在业务页面、在多处复制客户原文或模型原始响应

**PoC 产物与失败处理**:
每次正式 PoC 在 `artifacts/poc/<run_id>/` 保存 `summary.json` 和 `case-results.json`，并把结论写入 `docs/poc/glm-5.3-flash-poc.md`；这些验收产物与 `runtime_data/` 分离。失败时固定按输入与证据、Prompt 与 Schema、本地校验器、`high/max`、并发 1/2/3 的顺序定位并完整重跑。两种推理强度仍未达到硬门槛时，明确判定当前模型路线不可用，不以降低门槛或改答案宣布通过。
_Avoid_: 只留口头结论、覆盖失败记录、边调边删案例、PoC 未通过就冻结正式参数

**PSIT 前端设计语言**:
面向售后运营人员的 B2B 决策支持工作台。采用用户指定 `DESIGN-apple.md` 的单一动作蓝、白与浅灰表面、清楚排版、胶囊主操作和克制动效，并吸收 GitHub `taste-skill` 的一致性、完整状态与可访问性规则。固定设计参数为 `DESIGN_VARIANCE=3`、`MOTION_INTENSITY=2`、`VISUAL_DENSITY=6`；完整规则位于 `docs/PSIT前端视觉与交互规范.md`。
_Avoid_: Apple 官网仿站、通用后台默认皮肤、营销 Hero、AI 紫色渐变、满页玻璃拟态、装饰性数据大屏

**前端设计依据边界**:
`DESIGN-apple.md` 是用户指定的 `version: alpha` 设计分析，不冒充 Apple 官方规范。GitHub `taste-skill` 固定参考提交 `ccbc15639c97057cbfcf32ecebc38ef716e4bb37`，只读采用主 Skill 的通用质量规则；该 Skill 自述不适用于 dashboard、数据表格和多步骤产品界面，因此不采用其营销页结构，也不采用 `gpt-taste` 的随机版式、AIDA 和 GSAP 强制要求。外部 Skill 不安装、不执行。
_Avoid_: 把外部 Skill 所有指令自动升级为项目要求、把社区审美建议当官方标准、让审美覆盖业务可用性

**前端视觉验收**:
批次导入、案例队列、案例详情和人工确认必须在 `1366×768` 与 `1024×768` 完成截图和操作验证。页面主体不得水平滚动；文字、按钮、表单和焦点达到 WCAG AA；点击区域不小于 44×44px；颜色不单独承担状态含义；加载、空数据、错误、只读和缺少模型密钥状态均有稳定页面表现。
_Avoid_: 只验正常成功页、只看设计稿不跑浏览器、为了好看隐藏错误和不确定性

**前端工程栈**:
React、TypeScript 和 Vite 构成前端运行基础；Ant Design 只提供上传、表格、表单、弹窗等可靠交互，通过 `ConfigProvider`、集中 CSS 变量和 CSS Modules 落实项目视觉令牌，不交付默认皮肤。React Router 管理页面地址，TanStack Query 管理服务端数据，Ant Design Form 管理人工确认；图标只使用 `@ant-design/icons`。不引入 Tailwind、Redux、Zustand、GSAP 或 Motion。
_Avoid_: 混用多套组件或图标、页面散落样式常量、为了简单直接交付 Ant Design 默认外观

**前端类型化 API 客户端**:
FastAPI OpenAPI 是接口结构真源。使用 `openapi-typescript` 生成并提交 TypeScript 类型，使用 `openapi-fetch` 发出请求；后端合同变化但生成文件未同步时，测试必须失败。所有供应商和内部错误先由后端映射为稳定业务错误，再由一个前端错误入口显示。
_Avoid_: 手抄两份接口类型、页面分别拼 URL 和解析错误、后端字段变化后静默运行

**前端服务端状态**:
当前批次、案例和筛选条件进入 URL；批次、案例、分析状态和人工结果只以服务端 SQLite 为准。活动批次每 2 秒查询，终态停止；页面不可见时暂停，重新可见时立即刷新；写操作完成后只使相关 Query 失效。`localStorage` 不保存业务真相。
_Avoid_: React 本地状态复制完整服务端对象、结束后继续轮询、刷新页面后拿旧浏览器数据覆盖数据库

**ZIP 上传交互合同**:
一次只接收一个 ZIP。“导入批次”和“开始分析”是两个独立动作；导入完成后显示文件名、案例数、证据数、校验结果和 Mock 标记。浏览器只做扩展名、空文件和大小快速检查，FastAPI 负责全部最终校验。`upload.max_zip_bytes` 在固定案例包形成后按最大合格 ZIP 的实际大小留约两倍余量并向上取整后冻结。
_Avoid_: 上传后自动调用模型、前端校验替代后端校验、拍脑袋写 50MB 或 100MB

**前端测试分层**:
Vitest 与 React Testing Library 验证组件和页面状态，MSW 模拟前端测试所需 API，Playwright 验证浏览器完整流程。普通自动测试使用只存在于测试环境的确定性模型替身；真实 GLM-5.3-Flash 只用于模型冒烟和正式 PoC。报告必须分别标记产品流程与真实模型结果，测试替身不得进入正式启动模式或绕过统一导入校验。
_Avoid_: 每个前端测试都付费调用 GLM、用模型替身声称真实模型可用、生产环境存在可切换的假模型入口

**四模块并行架构**:
四名成员分别主责 M1 数据与存储、M2 AI 分析引擎、M3 业务服务与 API、M4 前端工作台。M4 只调用 M3，M3 通过公开接口编排 M1 和 M2，M1 与 M2 互不调用。纯共享合同目录不承载业务实现，也不算第五个模块。完整边界位于 `docs/PSIT四模块并行开发架构.md`。
_Avoid_: 按横向技术层让四人反复修改同一文件、增加无人负责的公共业务模块、跨模块传 ORM 或 SDK 对象

**接口先行并行开发**:
M1—M3、M2—M3、M3—M4 的输入、输出、错误和状态必须在功能开发前冻结。M1 使用固定 ZIP 和临时 SQLite，M2 使用固定分析输入和测试模型客户端，M3 使用 `FakeDataGateway` 与 `FakeAnalysisEngine`，M4 使用 MSW；替身与真实适配器通过相同合同测试，最终阶段只替换连接，不重新设计接口。
_Avoid_: 四个人分别猜接口、等待上游完成才开工、最后联调时才补字段

**共享合同变更**:
Pydantic、OpenAPI、数据库迁移、状态枚举、ZIP 合同、固定案例和视觉令牌属于共享合同。合同有一名主负责人，变更必须使用独立任务和 PR，说明新旧差异、影响模块、生成文件和测试变化，并由受影响模块共同审查。
_Avoid_: 在普通功能 PR 中顺手改合同、复制一份接口规避审查、提供方单方面破坏调用方

**四模块集成顺序**:
先冻结合同，再建立四个模块骨架与替身，然后并行开发；真实接线依次为 M1—M3、M2—M3、M4—M3，之后运行确定性 E2E、真实 GLM 冒烟和正式 PoC。禁止四个模块在一次大合并中首次连接。
_Avoid_: 一次性大联调、问题同时横跨数据库模型页面、用真实 GLM 调用代替确定性集成测试

**多人任务与审查规则**:
首轮以 37 张任务卡为基准，M1 九张、M2 十张、M3 九张、M4 九张，最终总数允许在 32—40 之间调整。一张任务卡对应一名负责人、一个主模块、一个分支和一个 PR；分支使用 `task/T编号-简短名称`，相邻模块交叉审查，共享状态和验收夹具由四人审查。
_Avoid_: “完成整个前端”式大任务、普通任务跨多个模块、一个模块只有一人理解

**代码合并质量门槛**:
后端合并前执行 Ruff、Pyright、Pytest、Alembic 和依赖边界检查；前端执行 ESLint、TypeScript、Vitest、OpenAPI 生成结果、Playwright 和视觉静态检查。本机和 GitHub PR 通过同一个 `scripts/test.ps1` 入口；真实 GLM 冒烟和 PoC 单独运行。
_Avoid_: 只凭人工判断合并、各成员使用不同测试命令、在普通 PR 中消耗真实模型密钥

**纯共享合同**:
`backend/app/contracts/` 只保存 `data.py`、`analysis.py` 和 `states.py`，使用严格 Pydantic 定义数据、使用 Python `Protocol` 定义行为；只依赖标准库、`typing` 和 Pydantic。现有 `CaseInput v1`、`PerceptionResult`、`AttributionResult`、`DecisionPackage` 继续作为字段真源，不因模块化重新增加无来源字段。
_Avoid_: 合同目录导入 FastAPI、SQLAlchemy、智谱 SDK 或业务模块、宽松转换字段、复制合同

**M1 五个数据端口**:
M1 对 M3 提供 `BatchImportGateway`、`CaseQueryGateway`、`EvidenceGateway`、`RunStore`、`ReviewStore` 五个小接口，共享同一 SQLite 和迁移体系。接口只返回 Pydantic 对象或受控文件流，不返回 ORM、Session、SDK 对象或绝对路径；Fake 与真实实现运行同一合同测试。
_Avoid_: 一个无限扩张的 DataGateway、M3 直接写 SQL、把五个端口误建成五个服务

**M2 分析接口**:
M2 公开同步阻塞的 `AnalysisEngine.analyze_case(AnalysisRequest, AnalysisEventSink) -> AnalysisOutcome`。M2 负责三个阶段和严格校验，通过类型化事件报告阶段与模型尝试；M3 在后台线程池调用并通过 M1 保存。证据不足属于完整成功结果，M2 不持有 DataGateway 或数据库 Session。
_Avoid_: M2 直接写数据库、M3 拆读 Agent 内部对象拼结果、只在最后返回而丢失阶段记录

**模块错误边界**:
导入拒绝、证据不足、重试耗尽、幂等命中和运行过期使用类型化结果；程序缺陷、存储故障和合同之外的 SDK 错误作为意外异常，由模块边界记录后交给 M3 映射为稳定 `BusinessError`。页面不显示异常类型、堆栈、供应商响应或本机路径。
_Avoid_: 用异常文本当业务接口、把证据不足记为处理异常、吞掉异常因果链

**业务状态与事务归属**:
M3 决定是否开始、如何流转、何时发布、是否失败和人工请求是否合法；M1 用独立短事务原子创建运行、保存阶段、发布完整结果、记录失败和保存审核。M3 不提交 Session，M2 不接触事务，等待 GLM 时不保持数据库事务。
_Avoid_: M3 穿透存储层操作 Session、M1 自行发明业务状态、模型等待占用长事务

**开发与演示装配**:
M3 使用应用工厂显式注入 M1 与 M2。`scripts/dev.ps1` 以同一配置启动 FastAPI 与 Vite；`scripts/start.ps1` 按固定顺序校验配置、取得运行目录单实例锁、构建当前前端、执行数据库迁移和中断恢复检查，再由 FastAPI 提供 API 和前端。停止只通过 `Ctrl+C` 进入正常关闭流程，不提供停止脚本、关机接口或自动打开浏览器；Fake 只能由测试显式注入，缺少密钥时禁用分析而不自动切换假模型。
_Avoid_: 全局可变单例、重复实例共享数据库、旧前端页面、无认证关机接口、隐藏页面开关启用 Fake、演示依赖 Vite 或 Docker、缺密钥自动生成假结果

**HTTP API v1 合同**:
M3 在 `/api/v1` 只提供批次上传与列表、批次详情与启动、案例队列与详情、案例重跑、人工确认、证据内容和健康检查十个接口。单项返回业务资源，列表返回 `items` 和 `total`，异步开始返回 `202`；完整字段、状态码和审核请求位于 `docs/PSIT-HTTP-API合同.md`。
_Avoid_: 通用 CRUD、运行详情和日志接口、只有 success 布尔值的空响应、页面等待 GLM 才返回

**运行编号与审核并发**:
业务 API 不返回 `batch_run_id` 或 `case_run_id`，前端只按批次和案例地址轮询。允许人工确认时，案例详情提供不展示的 `review_token`；当前结果变化后 Token 必须变化，旧 Token 返回 `STALE_CASE_RESULT`，存储内部仍把审核关联到真实案例运行。
_Avoid_: 页面显示运行编号、无法识别审核旧结果、把 Token 当登录权限或让用户编辑

**API 状态码与错误**:
v1 只使用 200、201、202、400、404、409、413、415、422、503 和 500。所有 JSON 错误，包括 FastAPI 默认校验与内部异常，统一转换为稳定 `BusinessError`；供应商状态码、堆栈、密钥和路径不透传。
_Avoid_: 每条接口自造错误结构、HTML 错误页、把 SDK 原始错误直接显示给用户

**API 列表查询**:
历史批次使用 `limit=20`、`offset=0`，案例队列使用 `limit=50`、`offset=0`，最大均为 100；队列只允许一个运行状态和一个介入等级筛选，排序始终由后端按冻结规则执行，前端把筛选和分页写入 URL。
_Avoid_: 任意 sort_by、客户全文搜索、复杂查询语言、前端重新排序破坏统一顺序

**API 业务视图**:
批次、队列、详情和人工结果 DTO 只返回页面完成动作所需的业务字段；`HealthView` 只表达应用、数据库和分析能力。可选字段在不适用时省略，不返回 RFM 过程、数据或 Schema 版本、运行编号、数据库主键、Prompt、Token、路径、哈希和日志。
_Avoid_: 先返回全部数据库或 Agent 对象再由 React 隐藏、响应充满无意义 null

**人工确认请求合同**:
人工确认按 `outcome` 严格区分直接通过、修改后通过、驳回并给出判断、证据不足四种请求。修改与驳回提交完整人工最终结果和必填原因，证据不足提交必填缺口说明，直接通过不得附带另一份结果；共同携带 `submission_id` 和 `review_token`。
_Avoid_: 一个塞满可选字段的通用请求、JSON Patch、修改无原因、驳回无人工判断

**原因枚举边界**:
风险原因只有物流履约、商品、退货退款、服务沟通、价格或权益、其他六个真实业务类别；`INSUFFICIENT_EVIDENCE` 是无法可靠判断时的非原因兜底值，不得宣传为第七种原因。人工选择证据不足时只填写证据缺口，不强迫选择原因。
_Avoid_: 把七个枚举值误写成六类、把证据不足并入其他、证据不足时编造原因

**案例业务状态机**:
案例只使用待分析、分析中、待人工确认、已完成、处理异常五种业务状态。完整且诚实的证据不足结果进入待人工确认；技术故障和程序中断进入处理异常；待人工确认和处理异常允许人工重跑，已完成是不可修改和不可重跑的终态。
_Avoid_: 暴露 Agent 阶段、把证据不足当技术异常、已完成后无痕修改、增加暂停恢复状态

**批次当前状态**:
批次页面只使用待开始分析、分析中、已完成、已完成但存在异常四种状态，对应内部合同值 `PENDING_ANALYSIS`、`ANALYZING`、`COMPLETED`、`COMPLETED_WITH_ERRORS`，并根据当前案例推导。任一案例人工重跑时批次显示分析中；ZIP 导入失败不创建批次，内部 batch_run 状态不进入业务页面。
_Avoid_: 显示线程和内部运行状态、创建导入失败半成品批次、批次状态与当前案例冲突

**批次当前与历史统计**:
`batches` 的状态、分析成功数和异常数表示案例当前投影，人工重跑时原子更新；`batch_runs` 保存首次批量分析历史，人工重跑不得改写。`BatchWorkspaceView` 只显示当前统计，不把历史失败数和当前状态混在一起。
_Avoid_: 重跑成功后页面仍显示旧异常数、覆盖首次运行历史、前端自行统计形成第三套结果

**应用中断恢复**:
迁移完成后、HTTP 开放前，M3 将遗留活动案例运行和批次运行标为 `INTERRUPTED`，未形成完整决策包的案例进入 `PROCESSING_ERROR` 并记录 `APP_INTERRUPTED`，保留所有阶段和模型记录，再重算批次当前投影。恢复检查不自动调用 GLM，不从中间阶段续跑。
_Avoid_: 永久停在分析中、启动后自动继续付费、删除半程记录、从归因中间恢复

**应用级后台执行器**:
FastAPI 生命周期内只创建一个 ThreadPoolExecutor，并发从配置读取。正常关闭时拒绝新任务、取消未开始 Future，当前 GLM 请求完成或超时后不再进入下一阶段；完整结果正常发布，未完成结果标为中断。浏览器关闭不停止分析，强制退出由下次启动恢复。
_Avoid_: 每请求新建线程池、关闭后继续新阶段、浏览器轮询控制后台任务、引入分布式队列

**核心链路验证 MVP**:
用于验证标准案例能够进入系统，文本、图片和行为数据能够完成有证据的事件与情绪还原，并形成可供人工确认的介入优先级和处理建议的可运行产品。本期只证明核心链路可运行，不证明真实业务需求、生产级准确率、客户留存提升、ROI 或企业部署能力。
_Avoid_: 正式生产产品、业务价值已验证、静态概念演示

**项目核心需求目标**:
在 10 天内，面向处理高价值客户异常售后的电商运营人员，建设核心链路验证 MVP。产品接收上游处理后的标准案例批次，从文本、图片和行为数据中提取业务特征，完成多模态证据对齐、有证据的事件与情绪还原，并生成系统建议介入等级、风险原因假设和受约束处理方案；运营人员先通过案例队列决定处理谁，再通过案例详情决定怎么处理并完成最终确认。所有重要判断能够追溯到原始证据，系统结果与人工最终结果持久保存。
_Avoid_: 把上游数据工作作为产品核心、只有情绪分数、自动业务执行

**图片模态贡献对比**:
在 MVP 流程验收集中，至少选择一个图文相互支持案例和一个图文存在冲突案例，分别比较“文本加行为数据”与“文本、图片和行为数据”的结果。图片必须补充或修正事件事实、支持或冲突文本、改变严重程度、介入等级、风险原因、处理方案或不确定性中的至少一项，才能证明图片真实参与联合判断。
_Avoid_: 图片只上传不使用、普遍性能提升证明

**行为模态贡献对比**:
在 MVP 流程验收集中，至少选择一个案例比较“文本加图片”与“文本、图片和行为数据”的结果。行为数据必须补充客户价值，并改变系统建议介入等级、处理顺序、处理方案或不确定性中的至少一项，才能证明行为数据真实参与联合判断；模拟关系只能验证流程。
_Avoid_: 只展示 RFM 数字、真实客户因果证明

**人工确认记录**:
产品侧人工最终确认完成后持久保存的记录，包含当时的标准案例包与证据来源、系统事件与情绪还原、系统原始决策包、人工审核结果、最终人工结果、修改或驳回原因和操作时间。刷新或重新打开产品后仍能查看。
_Avoid_: 页面临时状态、完整企业审计系统

**模态技术失败**:
文件无法读取、格式不支持、模型服务超时或程序报错，导致某种模态没有正常完成处理的状态。案例进入处理异常，显示失败环节和原因，并允许重新分析。
_Avoid_: 模态证据不足、正常模态缺失

**模态证据不足**:
系统已经正常读取并分析某种模态，但证据本身无法支持可靠结论的状态，例如图片模糊或拍摄角度无法看到问题。系统必须明确无法确认，案例可以继续运行并交给其他模态和人工判断。
_Avoid_: 模态技术失败、强行生成结论

**正常模态缺失**:
普通真实案例本身没有某种可选证据的状态，不属于技术失败。系统进入降级流程并标记缺失；多模态验收案例缺少文本、图片或行为数据时属于输入不合格，不能用于多模态验收。
_Avoid_: 模态技术失败、多模态验收完成
