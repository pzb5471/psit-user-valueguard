# 高价值客户异常售后决策支持 MVP 需求文档

> 文档日期：2026-09-04  
> 文档角色：PSIT MVP 唯一需求真源  
> 需求状态：产品需求已确认；技术实现合同和 37 张 Task 已冻结在 `docs/PSIT-MVP技术实施规格.md`  
> 使用规则：开发只使用本需求文档和上述技术执行文档。其他需求稿、技术专题、ADR、handoff 只用于追溯，不作为开发输入  
> 产品定位：核心链路验证 MVP  
> 唯一目标用户：处理高价值客户异常售后的电商售后运营人员

## Problem Statement

### 1. 现实问题

售后运营人员面对高价值客户的投诉、差评、退款、物流异常、商品问题时，需要同时回答三个问题：

1. 哪些案例必须优先人工介入。
2. 当前风险可能由什么原因造成。
3. 下一步采用什么受约束的处理方案。

现有材料分散在文本、图片、订单、物流、评价、退款和客户价值数据中。单看一条投诉、一个情绪分数或一张图片，无法稳定支持以上三个决定。运营人员还需要知道系统使用了哪些证据、哪些内容只是推断、哪些证据互相冲突、哪些信息仍然缺失。

### 2. 本期需要验证的产品假设

本期验证以下假设，不把假设写成已证明事实：

- 上游处理后的文本、图片、行为数据可以组成可追溯的售后风险案例。
- 三类模态经过特征提取和证据对齐后，可以形成有证据的事件与情绪还原。
- 事件与情绪还原可以支持系统生成介入等级、风险原因假设、受约束处理方案。
- 案例队列可以帮助运营人员决定先处理谁。
- 案例详情和人工最终确认可以帮助运营人员决定怎么处理，并留下可回看的最终人工结果。

### 3. 当前已知事实

- 项目必须在 10 天内交付可运行、可演示、可人工检查的 MVP。
- 团队共 4 人，不具备模型微调、复杂模型训练、自研多模态模型的交付能力。
- 当前没有已建成的项目仓库、代码、Agent、模型调用、测试、工作台。
- 数据采用“一主两辅”：Olist 是业务主数据；E Commerce Dataset 是独立流失预测辅助数据；JDDC 是独立客服文本分析辅助数据。
- 三套数据没有可用于真实客户级连接的共同 Customer ID。
- 当前公开数据没有真实干预动作、干预成本、干预后结果、随机对照组。
- 音频和视频已经从本期删除；文本、图片、行为数据保留。

### 4. 不能被当前材料证明的结果

本期不得声称：

- 已经验证真实企业存在稳定付费需求。
- 已经达到生产级事件还原或情绪识别准确率。
- 已经提高真实客户留存率。
- 已经产生真实 ROI。
- 已经证明某个处理策略与客户留存之间存在因果关系。
- 已经训练出新的多模态融合模型。
- 已经具备企业生产部署能力。

## Solution

### 1. 产品结果

建设一个高价值客户异常售后决策支持 MVP。产品接收上游处理后的标准案例批次，完成批量预分析，生成案例队列。运营人员先在案例队列中决定先处理谁，再进入案例详情核验多模态证据、事件与情绪还原、人工处置决策包，最后完成通过、修改后通过、驳回并填写人工判断、证据不足四类人工审核结果。

### 2. 端到端流程

    上游数据工作
        |
        v
    标准案例批次
        |
        v
    产品接入校验
        |
        v
    批量预分析
        |
        +-- 文本特征提取
        +-- 图片特征提取
        +-- 行为特征提取
        +-- 客户、案例、事件、时间对齐
        +-- 事件与情绪还原
        +-- 人工处置决策包
        |
        v
    案例队列：先处理谁
        |
        v
    案例详情：怎么处理
        |
        v
    产品侧人工最终确认
        |
        v
    持久保存系统原结果与人工最终结果

### 3. 产品职责边界

| 环节 | 本期负责 | 本期不负责 |
|---|---|---|
| 上游数据工作 | 接收其处理结果和来源说明 | 原始数据完整清洗、完整数据挖掘、数据源专项治理 |
| 产品数据接入 | 校验、来源保留、少量格式适配、案例组装 | 接受任意原始文件并自动治理 |
| 多模态分析 | 提取业务可读特征、对齐证据、识别支持与冲突 | 研究级特征融合、复杂模型训练 |
| 决策支持 | 给出系统建议介入等级、风险原因假设、受约束处理方案 | 自动联系客户、自动退款、自动发券、自动执行补偿 |
| 人工确认 | 保存系统原结果、人工最终结果、原因、时间 | 验证真实留存效果、执行真实业务动作 |

## Core Terms

### 1. 案例与用户

| 术语 | 定义 |
|---|---|
| 售后风险案例 | 一名客户围绕一张具有明确售后对话或投诉描述的主要订单形成的处理单位，并附带该客户可复算的交易汇总。 |
| 高价值客户 | 根据现有清洗后交易数据批量计算 RFM 后，达到版本化高价值判定规则的客户。本期规则只适用于 Mock 数据环境，不代表已经获得真实企业验证。 |
| 案例触发事件 | 投诉、差评、退款、物流异常、商品问题中的明确售后事件。没有触发事件，不建立本期售后风险案例。 |
| 订单级组案 | 同一主要订单内的对话、履约、付款和图片证据组成一个案例；本期不自动把多张订单合成一个案例。 |
| 产品侧人工最终确认 | 售后运营人员对系统三项建议承担最终业务判断，并留下通过、修改、驳回、证据不足结果。 |

### 2. 数据身份

| 术语 | 定义 |
|---|---|
| 原始观察（observed） | 数据源直接存在的事实，保留来源；数据源存在时间时原样保留，不含系统判断。 |
| 派生事实（derived） | 由固定公式、代码或明确规则从原始观察计算出的可重复结果，能够追溯到原始字段。 |
| 模拟关系（simulated） | 为演示人工建立的客户、文本、图片或事件关系，必须明确标记并说明各自真实来源。 |
| 标准证据记录 | 处理后的最小可追溯证据单位。原数据有 ID 时保留；无 ID 时生成内部 evidence_id，并保留来源位置和处理记录。 |
| 标准案例包 | 关系核验后生成 case_id 的运行输入，包含客户价值依据、标准证据记录和数据身份标记。售后事件由系统根据对话和图片还原，不在输入中预写。 |
| 关系未确认 | 现有信息不足以证明两条数据属于同一客户或同一事件。该数据不得强行合并进同一标准案例包。 |

### 3. 系统结果

| 术语 | 定义 |
|---|---|
| 事件还原 | 按时间说明客户经历了什么、涉及哪些订单和售后事件、关键事件之间有什么关系。 |
| 情感感知结果 | 说明客户当前可能的情绪、简短解释、客户文本证据和不确定性。情绪变化只在后续数据和验收依据能够可靠支持时增加。 |
| 人工处置决策包 | 由系统建议介入等级、风险原因假设、受约束处理方案组成的完整结果。三部分必须同时存在。 |
| 证据不足 | 输入不足以支持可靠结论。系统指出缺失证据和不能成立的结论后交给人工处理；这是合格业务输出，不是系统故障。 |
| 模态证据冲突 | 文本、图片、行为数据对同一事件给出不一致信号。系统必须显示冲突并降低确定性或交给人工判断。 |
| 判断依据 | 支撑事件还原、情感感知、风险原因、处理方案的具体文本、图片、行为证据。 |

### 4. 状态边界

| 类型 | 允许值 | 含义 |
|---|---|---|
| 案例运行状态 | 待分析、分析中、待人工确认、已完成、处理异常 | 表示系统处理阶段。 |
| 人工审核结果 | 通过、修改后通过、驳回并填写人工判断、证据不足 | 表示业务审核结论。 |
| 模态技术失败 | 文件无法读取、格式不支持、模型服务超时、程序报错 | 案例进入处理异常，显示失败环节和原因，允许重新分析。 |
| 模态证据不足 | 模态已正常读取和分析，但内容不能支持可靠结论 | 案例继续运行，明确“无法确认”。 |
| 正常模态缺失 | 普通案例本身没有某种可选证据 | 案例降级运行并标记缺失；不能用于证明该模态能力完成。 |

## Requirements

### R1. 目标用户与使用场景

- 产品只服务处理高价值客户异常售后的电商售后运营人员。
- 产品帮助该用户完成“先处理谁”和“怎么处理”两类决定。
- 产品不同时承担消费者入口、客服坐席系统、CRM 管理后台、管理层经营看板。
- 产品不持续扫描全部客户主动发现异常。
- 售后风险案例只能由明确案例触发事件建立。

### R2. 高价值客户识别

- 高价值客户从 RFM 入手识别。
- 现有清洗逻辑中的绝对阈值只作为历史结果对照，不直接成为产品规则。
- MVP 使用 `high_value_rule_v1`：`R <= P75(R) AND (M >= P80(M) OR (F >= 2 AND M >= P50(M)))`。
- 百分位按当前客户级数据快照计算，边界并列客户全部纳入，实际分界值随数据版本保存。
- 上游必须分别保存 R、F、M、计算日期、高价值判定、判定理由和规则版本，Agent 不重新计算。
- R 越小表示最近购买越近；R、F、M 与高价值判定不得混用同一字段。
- 业务页面只显示高价值客户结论，不显示 RFM 分数、权重和阈值。
- 该规则用于 Mock 数据下稳定复现，不得表述为已获得真实企业验证的业务规则。

### R3. 案例建立与组案

- 一个运行案例对应一张具有有效售后对话或投诉描述的主要订单。
- 同一订单的重复付款行必须先聚合，再进入标准案例包。
- 客户其他订单只用于形成可复算的客户级交易汇总，不自动合并进当前案例。
- 同一客户的不同主要订单分别建立案例；未来只有存在可靠工单编号或问题关联标识时才支持跨订单组案。
- 当前数据不能稳定形成近期与基准期的行为比较，v1 不生成行为变化；客户其他订单只用于确定性计算客户价值。

### R4. 上游数据工作与产品数据接入分离

- 团队数据负责人完成原始数据清洗、数据挖掘、去重、特征准备、数据源专项处理。
- 产品接收上游处理结果后，只完成输入校验、来源保留、少量格式适配、案例组装。
- 产品不得重复建设完整原始数据治理流程。
- 产品输入合同在获得至少一份真实处理样本和来源说明后冻结。
- 原始数据必须保留，不能被清洗结果覆盖。

### R5. 数据身份与可追溯性

- 每条标准证据记录必须标明 observed、derived、simulated 中的一种身份。
- 每条原始观察必须保留来源；源数据存在发生时间时原样保留，不存在时不得补造。
- 每条派生事实必须能够追溯到原始字段和固定计算规则。
- 每条模拟关系必须说明被连接内容各自的真实来源。
- 原始数据已有 ID 时必须保留该 ID。
- 原始数据没有 ID 时可以生成内部 evidence_id。
- 内部 evidence_id 必须保留来源位置和处理记录。
- 关系未确认的数据不得强行合并。
- 无法证明的客户关系或事件关系不得编造。
- 复制扩增、无标记嫁接、AI 补造原始事实、提前写入目标答案均不允许。
- 当前清洗结果中的订单、售后对话和图片由随机扩增逻辑形成 Mock 组合关系，统一标记为 `mock_mapped`，不得表述为真实客户或真实订单关系。
- 当前组合关系以 `mock_dataset_v1` 固定快照保存；不得通过重新运行无固定随机种子的旧 Notebook 改变案例映射。
- 新数据或新映射必须生成新的数据版本，并保存源文件和被引用证据文件的校验值，不得覆盖旧版本。

### R6. 案例批次导入

- MVP 提供案例批次导入能力，接收上游已处理的标准案例批次。
- 用户通过浏览器选择并上传一个标准案例 ZIP；页面不得要求用户填写服务器文件路径。
- 上传内容先保存到批次专用临时目录，完成全部校验后再原子移入正式目录并写入批次记录。
- 仓库内固定演示包和验收包必须经过同一上传与校验入口，不得使用绕过导入器的隐藏路径。
- 导入时检查案例数量、核心模态、来源标记、必需内容。
- 导入失败时显示具体案例、具体缺失内容、具体校验结果。
- 完全相同的 ZIP 再次上传时返回已有批次，不重复创建案例、证据或运行记录。
- `batch_id` 在系统内全局唯一；已有 `batch_id` 对应不同 ZIP 内容时，必须拒绝导入并返回 `BATCH_ID_CONFLICT`。
- 案例批次导入只用于测试和演示，不代表正式产品中运营人员需要手工上传文件。
- 案例批次导入不得清洗任意原始文件。
- 导入合格后全部案例进入待分析状态，页面只展示完成操作所需的批次名称、案例数量、图片数量和 Mock 标记摘要；数据版本、Schema 和文件校验值只保留在内部记录中。只有运营人员明确点击一次“开始批量分析”后才调用模型；导入不得自动产生 API 调用。
- 单个案例必须包含合同与数据版本、批次和案例标识、主要订单、脱敏客户、客户价值结果及内部复算信息、订单状态、下单时间、聚合付款金额、至少一条客户售后消息，以及逐项来源。
- `CaseInput v1` 顶层字段只保留 `schema_version`、`data_version`、`batch_id`、`case_id`、`data_identity`、`primary_order`、`customer`、`customer_value`、`evidence` 和 `provenance`，不得为同一含义增加多个入口。
- 履约时间和图片按源数据有无选择性出现；不得用一批必填 `null` 补齐币种、付款分项、消息时间、行为变化等当前输入不支持或 v1 不需要的字段。
- 完整案例记录与 Agent 受限输入分开，Agent 只读取当前职责必需的字段。
- 工作台只接收标准案例 ZIP。ZIP 至少包含 `manifest.json`、逐案例 JSON、案例实际引用的图片和 `checksums.json`；原始大 CSV、完整图片库和验收参考答案不得进入运行 ZIP。
- `image_paths` 必须在组包时从 Python 列表字符串转换为标准 JSON 数组，CSV 中无字段名的旧索引列必须丢弃。
- 图片按真实文件内容识别格式并生成规范文件名；原始文件哈希必须保留。普通 JPG、JPEG、PNG 原样使用，GIF 生成静态首帧作为模型输入并保留原文件。当前图片不统一压缩。
- 运行案例包与验收参考包必须物理分离。产品导入器递归发现 `mood`、`reason`、`solution`、`image_verification`、`key_answer`、`label`、`database_gt`、`trajectory`、`user_profile_st1`、`user_profile`、`question_type` 等答案或答案泄漏字段时必须拒绝导入；未规范化的 `user_address`、`database` 和重复的 `first_query` 同样不得进入运行包。
- 固定提供互不重叠的 `demo_batch_v1` 10 个案例和 `acceptance_batch_v1` 5 个案例；前者用于开发与演示，后者用于最终验收，参考答案单独密封。技术故障案例作为测试夹具保存，不混入正常演示批次。
- 产品必须先完整校验整个 ZIP，再一次性写入正式文件目录和数据库；任一案例或文件失败时整批拒绝，不留下半成品批次。
- 相同 ZIP 校验值再次导入时返回已有批次，不重复创建；已有 `batch_id` 对应不同内容、同一 ZIP 内 `case_id` 重复时整批拒绝。
- 客户对外标识由 `customer_unique_id` 确定性生成，源客户标识只保留在内部来源记录中；城市、州、邮编不进入案例输入。
- 同一订单的付款值先聚合为付款总额。v1 不保存付款分项和币种字段；页面统一标注“Mock 数据金额，源数据未提供币种”，不得显示为人民币或其他推测币种。
- 对话按 `User` 和 `Assistant` 拆成有顺序的客户与客服消息；原数据没有消息时间时直接省略时间字段，不得用下单时间冒充售后时间。运行输入只保留脱敏消息，固定 15 个案例必须逐条人工检查地址、手机号、订单号、物流单号和 URL 是否已经脱敏。
- 当前数据没有售后案例创建时间，标准案例包不补造等待时间。系统导入成功时写入 `imported_at`；同一介入等级先按 `imported_at`、再按稳定 `case_id` 排序。
- 完整 RFM 复算信息只保留在内部记录。模型运行链中只有策略 Agent 接收高价值客户结论，感知 Agent 和归因 Agent 不接收；业务页面只展示高价值结论，不接收 RFM 过程。v1 不设置行为变化及比较窗口字段，不得生成消费下降、频次下降或即将流失等结论。
- 由于当前订单、售后对话和图片属于 `mock_mapped` 组合，订单和付款事实完整保留用于来源追溯与页面说明；只有固定案例经过人工检查、确认语义不冲突的事实才发送给 Agent。行为模态对决策的最低有效贡献是高价值客户结论影响介入优先级。

### R7. 核心输入模态

- 本期核心输入只包含文本、图片、行为数据。
- 图片是 MVP 必做能力。
- 音频不进入本期输入、分析、界面、验收。
- 视频不进入本期输入、分析、界面、验收。
- 普通真实案例允许没有图片，系统必须标记图片缺失并降级运行。
- 多模态验收案例必须同时具有文本、图片、行为数据。
- 缺少任一核心模态的案例不能用于证明多模态能力完成。

### R8. 文本、图片、行为特征

- 系统必须分别提取文本、图片、行为的业务可读特征。
- 文本特征必须保留对应文本证据、来源、时间。
- 图片特征只要求识别状态、最多三条可观察事实、图片与客户陈述的关系、关联文本证据和不确定性。
- 图片观察必须关联具体图片证据。
- 图片模糊、拍摄角度不足、内容无法识别时，系统必须明确失败或无法确认。
- 系统不得根据配套文本反向编造图片内容。
- 图片和行为数据可以支持情绪触发事件的事实判断，但不得用于猜测客户情绪。
- 行为特征只表达现有清洗后数据能够确定计算的高价值客户结论，以及经过固定案例人工核验后允许进入 Agent 的订单与履约事实。
- 当前数据没有稳定行为比较窗口，v1 不设计行为变化字段；后续获得可比较历史后再新增合同版本。
- 模型向量、Embedding、技术参数不得作为运营人员默认可见内容。
- `evidence` 固定分为 `text_items`、`image_items` 和 `behavior_items`，所有证据共同具有稳定 `evidence_id`、数据身份、关系身份、来源和内容哈希；时间仅在对应源数据确实提供时出现。
- `evidence_id` 由组包程序生成并在同一案例重跑时保持不变；Agent 只能引用已有编号，不得创建新证据。
- 每张输入图片必须形成一条图片观察，状态明确区分正常分析和证据不足；“没有拍到”不得自动解释为“现实中不存在”。
- 行为事实由确定性程序生成并作为只读证据提供，GLM 不得重新计算或修改客户价值、订单状态、履约时间和付款总额，也不重复输出一份 `behavior_observations`。

### R9. 多模态证据联合判断

- 系统分别提取三类模态特征后，按案例和事件进行对齐；只有源数据存在可靠时间时才做时间对齐。
- 系统必须判断三类模态之间是相互支持、相互冲突、信息缺失中的哪一种状态。
- 三类模态结果必须进入同一个事件还原、情感感知结果、人工处置决策包。
- 系统不得只在页面上并排展示三份互不关联的分析结果。
- 每项重要判断必须标明使用的文本证据、图片证据、行为证据。
- 某类模态与其他模态冲突时，系统必须显示冲突内容。
- 冲突不得被静默忽略。
- 冲突必须降低判断确定性或转交人工判断。
- 需求不绑定 Feature Fusion、CLIP、RAG、训练型融合网络、特定多模态算法。

### R10. 有证据的事件还原

- 系统按时间说明客户经历了什么。
- 系统说明涉及哪些订单和售后事件。
- 系统说明关键事件之间的关系。
- 系统区分原始观察、派生事实、模拟关系、系统推断。
- 系统列出支持当前事件解释的证据。
- 系统列出与当前解释冲突的证据。
- 系统列出缺失信息。
- 系统在存在真实歧义时写入一句不确定性说明，不为满足固定数组强行生成备选事件解释。
- 系统不得为了输出完整故事而补写无证据事实。
- 系统不得编造退款、客服承诺、客户必然流失、真实干预结果。
- 每个事件只保存事件编号、类别、摘要、陈述状态、解决状态、支持证据，以及按需出现的冲突证据和不确定性。
- 陈述状态必须区分客户声称、客服陈述或承诺、客户确认结果、图片直接观察和程序派生事实。客服承诺不得直接标记为实际完成。
- 解决状态只允许已解决、未解决、无法确认；没有原始时间时按消息顺序还原，不得生成具体发生时间。

### R11. 有证据的情感感知结果

- 当前情绪只允许平静、不耐烦、无法判断。
- 系统用一句简短解释说明当前情绪指向的对象或触发事件，不拆成多个必填字段。
- 系统从客户文本中列出情绪判断依据；图片和行为数据只能支持触发事件，不能证明客户情绪。
- 系统仅在确有歧义时列出判断不确定性。
- 单一情感倾向标签不能代替完整结果。
- 单一情绪强度分数不能代替完整结果。
- 证据不足时允许输出未知，不能强制生成情绪结论。
- 系统不得把情感感知表述为读取了客户真实内心。
- `PerceptionResult` 只保存版本、案例、事件、逐图片观察、当前情绪和缺失证据。文本事实由事件引用文本证据表达，行为事实沿用输入证据，不再重复输出；图片与客户陈述的关系只允许相互支持、相互冲突、补充信息和无法判断。
- 情绪依据只能引用客户文本证据。情绪变化、目标情绪、独立备选解释数组和通用警告数组不属于 v1 必填合同。

### R12. 人工处置决策包

每个完成分析的案例必须同时交付以下三部分。任一部分不能消失。

#### R12.1 系统建议介入等级

- 只允许“必须介入”“建议介入”“暂不介入”三档。
- 介入等级综合客户价值、售后事件严重程度、紧迫性、当前情绪和证据状态。情绪变化与行为变化只有在后续数据可靠支持时才进入新合同版本。
- 必须介入：存在未解决的明显损失、严重履约异常、商品损坏或错漏发、退货退款争议，或者严重问题存在证据冲突。
- 建议介入：客户已经表现不满、沟通受阻或问题可能继续恶化，但尚无明确严重损失。
- 暂不介入：有证据说明问题已经解决，或者当前没有形成可执行的风险事项。
- 证据不足不自动等于暂不介入；存在可信但可能严重的问题时，仍应建议或要求人工核验。
- 固定业务底线计算当前案例允许的最低等级：未解决的明显损失、严重履约异常、商品损坏或错漏发、未到货和退货退款争议不能判为“暂不介入”；严重问题存在冲突或证据不足时至少为“建议介入并人工核验”。
- 高价值客户身份本身不能单独触发“必须介入”。
- Strategy Agent 的等级低于固定最低要求时，程序直接采用最低允许等级并记录调整原因，不增加模型调用；程序不得借此改写风险原因和处理方案。
- 每个等级必须说明排序理由。
- 系统不得生成未经真实业务规则确认的处理时限或 SLA。
- 证据不足时必须保留该字段，并说明无法判断的原因和缺失证据。

#### R12.2 风险原因假设

- 原因使用物流履约问题、商品问题、退货退款问题、服务沟通问题、价格或权益问题、其他六个真实业务类别；另设非原因兜底值 `INSUFFICIENT_EVIDENCE`，表示当前证据无法支持可靠原因判断。
- 系统给出一个主要原因。
- 系统只要求一个主要原因及其解释和支持证据。
- 存在反证时必须列出反证；没有反证时省略，不用空数组或套话占位。
- 只有证据确实同时指向另一类原因时才增加一个备选原因，并说明不确定性。
- 原因只能表述为排序假设，不能表述为已证明因果事实。
- 证据不足时必须保留该字段，并说明不能成立的原因判断。

#### R12.3 受约束处理方案

系统只能从以下六类动作中选择并组合：

1. 补充或核实图片、物流、订单或退款证据。
2. 建议人工联系客户，并给出沟通重点。
3. 建议升级物流或订单履约处理。
4. 建议补发、退货或退款核验。
5. 提交但不执行道歉、补偿或挽留申请。
6. 暂不采取动作，并说明观察或复查理由。

每个动作只保存动作类型、具体描述、原因、支持证据，以及确有必要时才出现的前置条件。全部方案统一经过产品侧人工最终确认，`requires_human_approval` 不由模型重复输出。禁止事项由程序和提示词统一校验，不要求模型逐条返回 `prohibited_claims`。

- 六类动作保存在只读、版本化的 `action_catalog.v1.json`，后端启动时完成结构和重复代码校验。
- 每个决策包最多选择三项动作；运行记录保存动作目录版本。
- MVP 工作台不提供动作目录增删改页面。

系统不得：

- 自由生成不在策略范围内的业务动作。
- 编造具体补偿金额。
- 编造具体权益。
- 自动联系客户。
- 自动退款。
- 自动发券。
- 自动执行补偿。

### R13. 批量预分析

- 一批标准案例包导入后先展示摘要并停在待分析状态。
- 运营人员执行一次“开始批量分析”后，系统统一完成该批次预分析。
- 一次开始操作创建一个 `batch_run`，并为批次内每个案例创建独立 `case_run`。首次案例运行关联该 `batch_run`；人工单案例重跑的 `batch_run_id` 为空，通过 `previous_case_run_id` 指向上一个案例运行，不伪造新的整批运行，也不回写原批次统计。
- 启动请求必须立即返回更新后的批次业务视图，不得让浏览器连接等待整个批次完成；内部批次运行编号不得进入业务响应。
- `RunService` 在 FastAPI 进程内使用应用级后台执行器运行案例，案例并发初始为 2。
- 页面在批次运行期间每 2 秒查询一次批次和案例状态，批次结束后停止查询；v1 不使用 WebSocket 或 SSE。
- 同一时间只允许一个活动批次；启动第二个批次时返回当前活动批次编号，第二个批次保持待分析，不自动排队，也不会在前一批结束后自动开始。
- 同一批次处于分析中时重复点击开始，返回正在运行的同一个运行记录，不创建第二个运行，也不产生重复模型调用。
- 批次开始后，全部案例立即进入队列并显示各自状态；每个案例完成后立即开放详情和人工确认，不等待整批结束。
- v1 不提供停止或取消分析按钮；应用退出导致的未完成案例在下次启动时标记为“处理异常：程序中断”。
- 每个案例独立完成三类模态特征提取。
- 每个案例独立完成事件与情绪还原。
- 每个案例独立生成人工处置决策包。
- 每个案例独立生成系统建议介入等级和排序理由。
- 单个案例失败不得阻断同批其他案例。
- 运行中必须能够区分待分析、分析中、待人工确认、已完成、处理异常。
- 感知、归因、策略三个阶段全部通过结构校验和程序规则前，案例不得向业务页面发布事件、情绪、原因或策略半成品。

### R14. 案例队列

案例队列是运营人员决定“先处理谁”的入口。每个案例必须显示：

- 脱敏客户标识。
- 高价值客户标识。
- 流失风险原因摘要。
- 当前有效介入等级；人工确认前使用系统建议，人工确认后使用人工最终等级。
- 排序理由。
- 案例运行状态。
- 证据冲突标记。
- 证据不足标记。
- 模态识别失败标记。

批次仍在运行时，队列必须同时容纳待分析、分析中、待人工确认和处理异常案例。已经完成分析的案例可以立即打开，其他案例的状态继续刷新。

- 队列一次只展示一个批次，默认选择最近导入批次，并允许通过批次选择器切换历史批次。
- 不同批次、不同数据版本的案例不得混合排序。
- 业务页面只显示待分析、分析中、待人工确认、已完成、处理异常五种案例状态，以及总案例数、分析完成数和处理异常数。
- 业务页面不显示感知、归因、策略等内部阶段，也不显示模型代码、Prompt、Token、请求参数或调用日志。
- 系统导入时间可以作为同等级排序依据，但不作为业务结果字段突出展示。
- 队列使用当前有效业务结果：尚未完成人工确认时使用系统建议；完成确认后使用人工最终结果。
- `CaseQueueItemView` 只返回页面定位所需 `case_id`、脱敏客户标识、高价值标识、流失风险原因摘要、当前有效介入等级、排序理由、业务状态和必要异常标记。

排序规则：

- 存在最终人工介入等级时，按最终人工介入等级排序。
- 不存在最终人工介入等级时，按系统建议介入等级排序。
- 同一介入等级先按系统导入时间升序、再按稳定 `case_id` 升序排序。
- 运营人员修改介入等级后，队列按最终人工介入等级重新排序。

筛选规则：

- MVP 只按介入等级筛选。
- MVP 只按运行状态筛选。
- MVP 不提供完整 CRM 的组合搜索、客户画像管理、营销活动管理。

### R15. 案例详情

案例详情是运营人员决定“怎么处理”的页面，只围绕业务结果组织：

1. 谁是高价值客户。
2. 为什么存在流失风险。
3. 应该怎么处理。
4. 按需展开的关键证据和不确定性。
5. 产品侧人工最终确认。

展示规则：

- 页面先展示结论，再允许按需展开判断依据。
- 每项重要判断都能查看对应文本、图片、行为证据。
- 展开的证据显示原始内容、来源、派生规则、模拟标记和不确定性；源数据存在时间时同时显示时间。
- 默认不铺开全部特征。
- 运营人员可以通过“查看分析依据”展开业务可读特征。
- 模型向量、Embedding、技术参数始终不向运营人员展示。
- RFM 方法、分数、分位、权重和阈值不得向运营人员展示。
- 数据库字段、内部整数 ID、运行编号、数据版本、Schema、Agent 阶段、Prompt、Token、模型调用记录、技术日志和原始 JSON 不得进入业务页面。
- 事件、情绪和多模态分析只作为“为什么存在流失风险”的业务依据呈现，不使用 Agent 或算法阶段名称组织页面。
- 人工确认前显示系统建议；人工确认后优先显示人工最终结果。只有人工实际修改时，才允许折叠查看系统原建议和人工修改原因。
- 默认只加载并展示当前结果实际引用的关键证据；内部证据编号、文件哈希、文件路径、身份字段、来源 JSON 和模型参数不得呈现。
- 证据类型在页面只使用“客户对话”“售后图片”“订单与客户价值事实”等业务名称。
- Mock 模拟关联关系必须以业务可理解的方式标记，但不解释内部字段结构。
- 运营人员不需要离开页面查找原始文件。
- 图片证据只能通过当前 `batch_id`、`case_id` 和 `evidence_id` 获取；页面和业务接口不得返回本机绝对路径。
- 后端必须验证证据属于当前案例、文件位于正式批次目录，并按文件真实内容识别类型。
- 当前数据实际包含的 JPG、JPEG、PNG、GIF 可以展示；GIF 页面展示原文件，模型分析使用已提取的静态首帧。

### R16. 产品侧人工最终确认

- 运营人员对系统建议介入等级、风险原因假设、受约束处理方案承担最终业务判断。
- 三部分全部认可时允许一键通过。
- 修改介入等级时只能从三档中重选，并且必须填写原因。
- 修改风险原因时只能从六类原因中重选，并且必须填写原因。
- 修改处理方案时只能增删六类批准动作，并可填写一段执行说明；修改后必须填写原因。
- 页面不得向运营人员开放任意编辑内部 JSON 的入口。
- 每次人工确认提交必须携带前端生成的唯一 `submission_id`；相同编号的网络重试返回原提交结果，不重复保存。
- 驳回决策包时必须填写原因。
- 驳回不代表客户无需介入。
- 驳回后必须补充最终人工判断，或把案例标记为证据不足。
- 系统原结果不得被人工结果覆盖。
- 人工最终结果不得无痕改写系统原结果。
- 人工确认后默认显示人工最终结果；发生修改时允许折叠查看系统原业务建议和人工修改原因，没有修改时不得重复展示两份相同结果。

### R17. 持久保存

人工确认记录必须保存：

- 当时的标准案例包。
- 证据来源。
- 系统事件还原。
- 系统情感感知结果。
- 系统原始人工处置决策包。
- 人工审核结果。
- 最终人工介入等级。
- 最终人工风险原因判断。
- 最终人工处理方案。
- 修改或驳回原因。
- 操作时间。

刷新页面或重新打开产品后，以上记录必须仍可查看。

- 系统运行结果和已提交人工结果均采用追加新版本的方式保存，不得原地覆盖。
- SQLite 使用 `data_versions`、`batches`、`cases`、`evidence`、`batch_runs`、`case_runs`、`stage_results`、`model_calls`、`reviews` 九张核心表；批次运行和案例运行不得混用一张表。
- `reviews` 必须同时关联案例和被审核的 `case_run`。
- 需要查询、排序、去重或建立约束的标识、状态、等级、计数、时间、校验值、相对路径和当前版本引用使用独立列。
- `CaseInput v1`、三个 Agent 结果、人工修改内容、模型原始响应和错误详情按照版本化合同整体保存为 JSON。
- 当前状态、进度计数、开始结束时间、`current_case_run_id` 和 `current_review_id` 可以更新；已经形成的阶段结果、模型调用、已结束案例运行、人工确认和失败详情不得覆盖。
- 案例重跑完成后，必须在同一个数据库事务中保存新结果并切换当前结果指针，旧运行和旧人工记录继续保留。
- 人工确认提交后案例进入已完成并保持只读；v1 不提供已提交人工结果的更正入口。
- 业务页面不提供永久删除批次、系统运行或人工确认记录的入口。
- 应用启动时发现上次意外中断的分析任务，必须标记为“处理异常：程序中断”并保留已有阶段结果，不得在用户未操作时自动继续产生付费模型调用。

### R18. 错误、缺失、冲突、降级

- 面向运营人员的错误必须包含中文摘要、失败对象、失败环节、稳定错误编号和允许的下一步。
- 模型供应商原始响应、SDK 错误、程序堆栈、请求参数、API Key 和内部文件绝对路径只进入技术记录，不得直接返回业务页面。

#### R18.1 数据处理失败

- 原始数据无法解析、来源丢失、关键关系无法核验时，不能形成合格标准证据记录或标准案例包。
- 产品显示失败对象和失败原因。
- 数据处理失败不能伪装成案例证据不足。
- 导入阶段必须检查必填字段、重复案例标识、证据文件路径和隐藏答案，失败案例不得调用模型。

#### R18.2 模态技术失败

- 文件无法读取、格式不支持、模型服务超时、程序报错时，案例进入处理异常。
- 产品显示失败模态、失败环节、失败原因。
- 处理异常和待人工确认案例允许重新分析；已完成案例在业务页面保持只读，不允许重新分析。
- 同一案例已有重跑正在执行时，重复请求返回当前重跑记录，不创建第二次重跑。
- Agent 结构不合格时，系统携带具体校验错误进行一次定向修复。
- 网络或模型临时故障最多重试两次；具体超时时间在模型 PoC 后写入配置。
- 重试失败只影响当前案例，同批其他案例继续运行。
- 重新分析创建新运行版本，不覆盖旧系统结果。已完成案例的重新分析只允许开发测试工具执行，不属于业务页面能力。

#### R18.3 模态证据不足

- 模态已经正常处理，但内容不能支持可靠结论时，系统明确显示“无法确认”。
- 案例可以依靠其他模态继续运行。
- 人工处置决策包保留对应字段和缺失说明。

#### R18.4 正常模态缺失

- 普通案例缺少图片时，系统降级运行并标记图片缺失。
- 普通案例缺少可选证据不属于技术失败。
- 多模态验收案例缺少文本、图片、行为中的任一项时，输入不合格。

#### R18.5 模态证据冲突

- 冲突内容必须醒目标记。
- 系统必须说明冲突影响了哪项事件、情绪、原因、方案判断。
- 系统降低确定性或交给人工判断。
- 系统不得自动选择更有利的证据。

### R19. MVP 流程验收集

- 验收集最少包含 5 个案例。
- 案例运行输入不得预写事件总结。
- 案例运行输入不得预写情绪结论。
- 案例运行输入不得预写主要问题。
- 案例运行输入不得预写风险原因。
- 案例运行输入不得预写证据总结。
- 案例运行输入不得预写推荐动作。
- 隐藏人工参考答案与系统运行输入分开保存。
- 5 个案例共同覆盖必须介入、建议介入、暂不介入、证据冲突、模态缺失或模态识别失败。
- 5 个案例一起进入批量预分析。
- 5 个案例完成队列排序。
- 5 个案例进入案例详情。
- 5 个案例完成产品侧人工最终确认。

### R20. 图片模态贡献对比

- 至少选择一个图文相互支持案例。
- 至少选择一个图文存在冲突案例。
- 两个案例分别运行“文本加行为数据”和“文本、图片加行为数据”。
- 图片必须补充或修正事件事实、支持或冲突文本、改变严重程度、改变介入等级、改变风险原因、改变处理方案、改变不确定性中的至少一项。
- 图片只上传但不进入判断，不能通过图片模态验收。
- 两个案例只能证明图片参与了当前固定案例的联合判断，不能证明普遍准确率提升。

### R21. 行为模态贡献对比

- 至少选择一个案例比较“文本加图片”和“文本、图片加行为数据”。
- 行为数据必须补充客户价值，并改变系统建议介入等级、处理顺序、处理方案或不确定性中的至少一项。
- 只展示 RFM 数字但不影响判断，不能通过行为模态验收。
- 使用模拟关系的案例只能验证流程，不能证明真实客户因果关系。

### R22. 能力与声明边界

- 当前交付只证明新案例可以进入系统。
- 当前交付只证明文本、图片、行为数据可以参与联合判断。
- 当前交付只证明案例队列、案例详情、人工确认链路可以运行。
- 当前交付不能证明真实业务需求成立。
- 当前交付不能证明生产级准确率。
- 当前交付不能证明真实留存提升。
- 当前交付不能证明真实 ROI。
- 当前交付不能证明企业部署能力。

### R23. 工作台页面与 REST API

- `/` 是工作台入口；没有批次时显示 ZIP 导入，有批次时进入最近批次。
- `/batches/:batchId` 显示所选批次摘要、进度和案例队列。
- `/batches/:batchId/cases/:caseId` 显示案例详情、证据和人工确认。
- 页面刷新后必须通过后端恢复批次、案例和人工结果，不能依赖 React 内存保存业务真相。
- 后端接口统一使用 `/api/v1`，只包含批次上传与查询、批次启动、案例列表与详情、案例重跑、人工确认、证据内容和健康检查十个接口。
- v1 不提供登录、模型配置、技术日志、通用搜索或永久删除接口。
- 业务接口使用 `BatchWorkspaceView`、`CaseQueueItemView`、`CaseDetailView`、`ReviewResultView` 和 `BusinessError` 五类明确业务 DTO；列表使用只含 `items` 与 `total` 的分页结构，健康检查使用不含分析结果的 `HealthView`，所有接口均不得直接序列化 SQLAlchemy 数据库对象。
- 阶段结果和模型调用不提供业务查询接口，开发测试通过数据库和内部技术日志核验。

### R24. API 返回与安全边界

- 单个对象直接返回真实对象；列表返回 `items` 和 `total`；创建或修改返回保存后的记录。
- 业务错误统一返回稳定 `code`、中文 `message`、`object_type`、`object_id`、业务可理解的 `stage`、`next_action` 和排障用 `trace_id`。
- API 不使用只有 `success: true`、却没有业务结果的空响应。
- 图片内容接口只接受批次、案例和证据标识，不接受本机路径。
- ZIP 中出现绝对路径、`../` 路径穿越或解压目标越过批次临时目录时，整批拒绝导入。

### R25. 数据库连接、目录与启动

- 后端使用同步版 SQLAlchemy 2；每个 API 请求和每个后台案例任务使用独立 Session，不在线程或任务之间共享。
- 等待 GLM API 响应期间不得保持数据库事务，所有数据库写入使用短事务。
- SQLite 开启外键约束、WAL 和 5 秒 `busy_timeout`。
- 数据库约束保证全系统最多一个活动 `batch_run`，并保证同一案例最多一个活动 `case_run`。
- 数据库默认位于项目根目录的 `runtime_data/psit.db`；临时导入、正式批次和技术日志分别放入同一已解析运行根目录的 `imports/tmp/`、`batches/` 和 `logs/`。
- `runtime_data/` 不进入 Git；固定运行夹具和密封人工参考答案分别放入 `tests/fixtures/runtime/`、`tests/fixtures/sealed/`。
- 统一启动脚本检查配置和运行目录，取得单实例锁，构建当前前端，执行 Alembic 创建或升级，收敛中断运行，再启动本地服务。
- Alembic 升级失败时立即停止，不删除数据库，也不自动重建。
- 重要演示前需要保护数据时，先停止应用，再人工复制完整 `runtime_data/`；本期不建设自动备份、完整性校验和轮换体系。

### R26. 九表逻辑结构和标识约束

- 所有表使用内部整数主键，API 只暴露稳定文本标识，不暴露数据库整数 ID。
- `data_version`、`batch_id`、`run_id`、`review_id` 和 `submission_id` 分别全局唯一；`case_id` 在所属批次内唯一；`evidence_id` 在所属案例内唯一。
- `data_versions` 保存 `data_version`、来源快照引用、来源清单校验值、来源说明 JSON 和创建时间。
- `batches` 保存 `batch_id`、数据版本引用、Schema 版本、ZIP 校验值、相对路径、当前业务状态、案例总数、当前分析成功数、当前异常数、当前批次运行引用和导入更新时间；三个当前投影字段随案例当前状态原子更新。
- `cases` 保存批次引用、`case_id`、Schema 版本、`CaseInput v1` 快照、脱敏客户标识、高价值结论、业务状态、系统与最终人工介入等级、当前案例运行和人工确认引用、导入更新时间。
- `evidence` 保存案例引用、`evidence_id`、模态、可选顺序、数据身份、关系身份、来源、内容校验值、源中实际存在的可选时间、可选相对路径和媒体类型，以及证据合同 JSON。
- `batch_runs` 保存 `run_id`、批次引用、内部状态、总案例数、分析成功数、异常数和开始结束更新时间。
- `case_runs` 保存 `run_id`、案例引用、可选批次运行引用、可选上一运行引用、触发类型、内部状态、当前技术阶段、错误编号、错误环节、错误详情和开始结束更新时间。
- `stage_results` 保存案例运行引用、阶段名称、合同版本、通过校验的结果 JSON、结果校验值和创建时间；同一案例运行每阶段最多一份最终结果。
- `model_calls` 保存 `call_id`、案例运行、阶段、尝试序号、模型名、Prompt 版本、请求清单、状态、供应商响应或错误、耗时和供应商实际返回的可选 Token 计数；不保存图片 Base64 和 API Key。
- `reviews` 保存 `review_id`、`submission_id`、案例与被审核运行、审核结果、最终介入等级、最终原因和动作、审核原因及创建时间。v1 每个案例最多一份正式人工确认。
- 业务 API 不返回 `case_run_id`。人工确认提交时必须携带案例详情中的不可读 `review_token`；M1 在保存事务内确认其对应的内部案例运行仍是当前运行，否则返回 `STALE_CASE_RESULT`。

### R27. 状态、计数、时间与业务结果投影

- 案例状态固定为 `PENDING_ANALYSIS`、`ANALYZING`、`PENDING_REVIEW`、`COMPLETED`、`PROCESSING_ERROR`，页面分别显示待分析、分析中、待人工确认、已完成、处理异常。
- 案例运行内部状态固定为 `STARTING`、`PERCEPTION_RUNNING`、`ATTRIBUTION_RUNNING`、`STRATEGY_RUNNING`、`SUCCEEDED`、`FAILED`、`INTERRUPTED`；这些内部值不得出现在业务页面。
- 批次运行内部状态固定为 `STARTING`、`RUNNING`、`COMPLETED`、`COMPLETED_WITH_ERRORS`、`FAILED`、`INTERRUPTED`；这些内部值不得出现在业务页面。
- 人工确认结果固定为 `APPROVED`、`MODIFIED_AND_APPROVED`、`REJECTED_WITH_JUDGMENT`、`INSUFFICIENT_EVIDENCE`。
- 介入等级固定为 `MUST_INTERVENE`、`SHOULD_INTERVENE`、`NO_IMMEDIATE_INTERVENTION`，页面分别显示必须介入、建议介入、暂不介入。
- `batches.analysis_succeeded_count` 表示当前处于待人工确认或已完成且具有完整决策包的案例数，`batches.error_count` 表示当前处于处理异常的案例数，人工单案例重跑时随当前案例状态原子更新；`batch_runs` 中的同名计数只表示首次批量分析历史，人工重跑不得改写。
- 程序生成时间统一保存为带毫秒的 UTC ISO 8601，页面转换为北京时间；系统运行时间与源数据事件时间分开，源中没有的时间不得补造。
- 数据库 `CHECK` 约束和 Python 枚举共同限制状态值。
- 业务 API 只向页面提供完成操作所需的标识、五种业务状态、可处理错误，以及“谁是高价值客户、为什么有流失风险、应该怎么做”的结果投影；内部表字段和技术记录不进入业务响应 DTO。
- `CaseDetailView` 只组织高价值客户结论、流失风险原因、处理建议、按需关键证据与不确定性、人工确认；不按事件、情绪、感知、归因、策略等内部阶段分区。
- 完整业务结果只在三个阶段全部成功后一次性发布。后续阶段失败时案例进入处理异常，业务 DTO 不包含已完成内部阶段的半成品。

### R28. 三个 Agent 的阶段受限输入

- 感知 Agent 只读取当前案例对话、当前案例图片、经过固定案例人工核验的订单状态和履约事实、`case_id` 及可引用证据编号；不读取高价值结论、RFM、风险答案、动作目录、其他案例或验收答案。
- 归因 Agent 只读取 `PerceptionResult`、其中实际引用的客户文本和订单事实、图片观察及可引用证据编号；不重复发送原始图片，不读取高价值结论、RFM、动作目录、策略结果、其他案例或验收答案。
- 策略 Agent 只读取 `PerceptionResult`、`AttributionResult`、高价值客户结论、获准证据编号、版本化动作目录和介入等级最低规则；不读取原始图片、完整原始对话、RFM 过程、其他案例或验收答案。
- 高价值客户结论只允许影响策略阶段的介入优先级和处理方式，不得反向影响事件、情绪和风险原因。
- 源数据没有币种且当前关系为 Mock 映射，付款金额不进入三个 Agent 的模型上下文；数据库仍保留来源事实供追溯。

### R29. 统一 GlmClient 接入边界

- 产品中只有 `GlmClient` 可以直接调用固定版本的智谱官方 `zai-sdk`；三个 Agent 不得分别编写 SDK、鉴权、超时或网络重试代码。
- `GlmClient` 只负责从 `ZAI_API_KEY` 读取鉴权、组装 Chat Completions 请求、发送文本与允许阶段的图片、启用 JSON 输出、应用统一模型参数、记录供应商请求编号、Token、耗时、原始响应和技术错误。
- Agent 负责阶段 Prompt 和输入输出合同；`RunService` 负责阶段顺序、状态、严格校验、修复决策、程序规则和持久化。
- `GlmClient` 不读取 SQLAlchemy 业务对象，不计算 RFM，不判断原因或动作，也不直接更新案例状态。

### R30. Prompt 与 Schema 版本

- 三个 Prompt 分别保存为 `prompts/perception/v1.md`、`prompts/attribution/v1.md` 和 `prompts/strategy/v1.md`。
- 每份 Prompt 必须写明唯一职责、允许输入、禁止推断、证据引用、不确定性、JSON 输出和禁用内容规则。
- Pydantic 模型是输出字段结构的唯一来源；程序运行时从对应模型生成 JSON Schema 注入 Prompt，不手工维护第二份字段清单。
- 每次模型调用记录 Prompt 语义版本、Prompt 文件 SHA-256 和结构合同版本。
- Prompt 内容变化必须形成明确版本变更并通过合同测试；业务页面不提供 Prompt 编辑器。
- Prompt 不得包含 API Key、隐藏验收答案或固定案例答案。

### R31. 阶段结果严格校验

- 每次模型响应必须依次通过 JSON 解析、Pydantic 严格字段、枚举、`case_id`、`evidence_id` 归属、跨阶段引用、禁用内容和当前阶段程序规则校验。
- Pydantic 禁止未声明字段，不进行字符串、列表和数字之间的宽松自动转换。
- Agent 只能引用当前案例已有且当前阶段获准使用的证据编号，不得生成新证据编号。
- 归因只能引用感知阶段获准传递的证据；策略动作只能从当前版本动作目录选择，最多三项；沟通重点最多三项。
- 策略等级低于程序最低要求时，程序直接提升并记录调整原因，不新增模型请求，也不改写风险原因或处理动作。
- 结构和硬规则合格但业务判断仍有不确定性时，结果进入人工确认，不为获得更符合主观偏好的答案自动重跑。

### R32. 重试、修复与单阶段请求上限

- SDK 自动重试关闭，所有尝试由 `RunService` 统一控制并写入 `model_calls`。
- 连接中断、读取超时、HTTP 429 和 HTTP 5xx 最多重试两次；401、403、请求参数、图片格式、图片大小和输入合同错误不得重试。
- 模型已返回但结构、枚举、案例、证据引用或程序规则不合格时，只允许一次定向修复。修复输入仅包含原阶段上下文、原始错误输出和明确校验错误，不增加证据，不读取验收答案。
- 每阶段供应商请求总数最多四次：首次一次、网络重试最多两次、定向修复最多一次。修复请求发生网络错误后不继续重试。
- 所有尝试都保存尝试序号、失败类型和最终状态；单阶段耗尽上限后，当前案例进入处理异常，同批其他案例继续。

### R33. 模型 PoC 与参数冻结

- PoC 初始配置为 `stream=false`、`response_format=json_object`、`thinking=enabled`、`reasoning_effort=high`、`do_sample=false`、`max_tokens=4096`、连接超时 10 秒、完整响应上限 300 秒、案例并发 2。
- 初始配置只用于建立基线，不得表述为最佳配置或正式验收阈值。
- PoC 使用固定 10 个演示案例和密封 5 个验收案例，记录 JSON 首次通过率、修复后通过率、三个阶段失败率、阶段和案例耗时、Token、每案例实际调用次数及图片是否改变规定判断项。
- PoC 比较 `reasoning_effort=high` 与 `max` 的结构通过率、人工判断质量、耗时和用量，并比较案例并发 1、2、3 时的 429、超时和 SQLite 写入表现。
- 密封验收答案只由测试程序评分，不进入 Prompt、运行包或 Agent 上下文。
- PoC 完成后才冻结正式响应超时、推理强度、默认并发数和内部成本预警线；相关数值写入配置，不散落在 Agent 代码中。

### R34. 固定案例 PoC 通过门槛

- 选定配置必须完整运行固定 10 个演示案例和密封 5 个验收案例，45 个阶段均产生真实调用或明确失败记录；不得跳过、替换或删除失败案例。
- 所有已经发布的阶段结果必须通过严格合同；耗尽允许请求的失败必须原样保留并进入可解释的处理异常，不能用 Fake、预写答案或删除案例补成成功。
- 全部已发布结果中不得出现不存在或跨案例的证据引用、动作目录外动作、隐藏答案字段或密封答案泄漏。
- 5 个密封验收案例均须满足：没有编造关键事实、证据能够支持主要原因、不确定性表达诚实、介入等级满足程序规则、处理动作来自批准目录，并由两名人工验收者一致判断可作为人工决策初稿。
- 两个图片贡献案例和一个行为贡献案例必须分别达到 R20、R21 的规定差异。
- PoC 必须记录首次结构通过率、修复后通过率、耗时、Token、实际调用次数、429、超时和 SQLite 写入情况；未实测前不把 41/45、2 分钟或 10 分钟写成硬门槛。
- 以上结果只用于判断固定案例链路是否可用，不得表述为生产准确率、真实留存效果或企业部署能力。

### R35. 配置、密钥与就绪状态

- 非敏感运行参数集中在 `config/default.toml`，由 `pydantic-settings` 在启动时读取和严格校验；至少覆盖模型、重试、执行器、前端轮询、存储目录、服务端口和日志参数。成员电脑的显式非敏感覆盖只写入不进 Git 的 `config/local.toml`。
- 智谱 API 密钥只能从 `ZAI_API_KEY` 环境变量读取，不得写入源码、Prompt、TOML、日志、数据库业务字段或页面；`.env` 不进入 Git，`.env.example` 只包含变量名和配置说明。
- 配置项缺失、类型错误或超出允许范围时，应用启动失败并明确指出错误配置项，不使用隐式替代值继续运行。
- 缺少 `ZAI_API_KEY` 时，工作台和数据库仍可启动，历史批次与结果仍可读取；开始分析和重新分析操作不可用，并返回不含技术细节的业务提示。
- `/health` 分别报告应用、数据库和分析服务是否就绪，不返回密钥内容、供应商原始响应或本机绝对路径。

### R36. 可复现开发工具链和统一脚本

- 后端统一使用 Python 3.12、`pyproject.toml`、`uv` 和 `uv.lock`；前端统一使用 Node.js 24、pnpm 11、`package.json` 和 `pnpm-lock.yaml`。
- 依赖安装必须服从锁文件；实际依赖精确版本在仓库初始化和兼容性验证后写入锁文件，不在本文凭经验猜测补齐。
- 仓库必须提供 `scripts/bootstrap.ps1`、`scripts/dev.ps1`、`scripts/test.ps1`、`scripts/start.ps1`，分别用于初始化、开发启动、完整测试和演示启动。
- 脚本只能使用项目相对路径、PATH 或明确的项目级工具发现逻辑，不得写入成员电脑路径、WindowsApps 别名路径或 Codex 缓存绝对路径。
- `bootstrap.ps1` 必须检查 Python、Node、uv、pnpm 的主版本和锁文件一致性；检查失败时停止并给出可执行的修复信息。

### R37. 可关联且脱敏的结构化日志

- 应用技术日志采用 JSONL，写入 `runtime_data/logs/app.jsonl`；是否轮换以及轮换大小属于 M3 内部默认值，不作为 MVP 跨模块合同和验收门槛。
- 每条日志至少包含 `timestamp`、`level`、`trace_id`、`batch_id`、`case_id`、`run_id`、`stage`、`event`、`status`、`duration_ms`、`error_code`；不适用字段可为空，但字段名保持稳定。
- 同一次业务请求经过 API、`RunService`、Agent、`GlmClient` 和数据库写入时必须沿用同一个 `trace_id`，便于开发人员离线定位问题。
- 日志不得记录 API Key、图片 base64、完整客户对话、完整 Prompt、本机绝对路径或密封验收答案；Prompt 只记录版本与哈希。
- 供应商原始响应只保存在 `model_calls`，不重复写入日志；业务页面不提供技术日志页，也不向业务 DTO 返回日志字段。

### R38. PoC 产物、失败路径与闭环

- 每次正式 PoC 必须生成 `docs/poc/glm-5.3-flash-poc.md`、`artifacts/poc/<run_id>/summary.json` 和 `artifacts/poc/<run_id>/case-results.json`；报告引用对应 `run_id`，机器可读结果不得只存在终端输出中。
- `summary.json` 至少汇总配置、案例数、阶段数、首次与修复后结构通过率、质量门槛、模态贡献、耗时、429、超时和 SQLite 错误；`case-results.json` 保存逐案例逐阶段结果和评分状态，但不得包含密封参考答案正文。
- PoC 失败时固定按输入与证据、Prompt 与 Schema、本地校验器、`reasoning_effort=high/max`、并发 1/2/3 的顺序定位，每次改变后完整重跑固定案例集。
- 禁止修改密封答案迎合模型、删除失败案例、把答案放入 Prompt、放宽证据和动作规则、使用预写静态结果或只重跑成功子集。
- 只有满足 R34 后才允许把对应参数写为正式配置；`high` 和 `max` 均不能通过时，必须明确判定 GLM-5.3-Flash 当前路线未达到可用 MVP 门槛，再根据失败证据重新决定技术路线。

### R39. 前端视觉语言与审美适配

- 前端以 `docs/PSIT前端视觉与交互规范.md` 为唯一项目级视觉合同；用户指定的 `DESIGN-apple.md` 提供 Apple 风格视觉令牌，GitHub `taste-skill` 提供经过适配的通用审美和质量规则。
- 产品设计判断固定为面向售后运营人员的 B2B 决策支持工作台，强调冷静、可信、克制和证据感；设计参数固定为 `DESIGN_VARIANCE=3`、`MOTION_INTENSITY=2`、`VISUAL_DENSITY=6`。
- 主动作使用 `#0066cc`，页面使用白色与 `#f5f5f7` 浅灰表面，主文字使用 `#1d1d1f`；语义状态色只用于成功、提醒和错误，并必须同时显示文字与图标。
- 字体随产品本地打包，运行时不得请求外部字体；主要采用 34px 页面标题、21px 区块标题、17px 结论正文和 14px 表格表单层级。
- 页面层级依靠表面颜色、1px 分隔线、字重和留白表达；卡片、按钮、表格和文字不使用投影，不使用装饰性渐变、AI 紫色光晕或满页玻璃拟态。
- 批次导入不做营销 Hero；案例队列不使用三个等宽统计卡或 Bento；案例详情首屏先显示高价值结论、主要风险原因和处理建议，证据按需展开。
- 交互只保留按下、展开和状态过渡，不使用 GSAP、视差、滚动锁定、无限轮播、磁性按钮或随机版式。
- 所有主要点击区域不小于 44×44px，按钮、表单和正文达到 WCAG AA；表单标签位于输入框上方，字段错误位于输入框下方，不得用 placeholder 代替标签。
- 三个产品页面必须在 `1366×768` 和 `1024×768` 完成截图与操作验收；本期不做手机端。
- GitHub 外部 Skill 只作为固定提交的只读参考，不安装、不执行；不得把社区 Skill 的操作指令、安全假设或营销页强制规则带入产品实现。

### R40. 前端技术栈与样式边界

- 前端固定使用 React、TypeScript 和 Vite；Ant Design 是唯一功能组件库，React Router 管理页面路由，TanStack Query 管理服务端状态，Ant Design Form 管理表单。
- 图标只使用 `@ant-design/icons`；本期不引入第二套组件库、第二套图标族、Tailwind CSS、Redux、Zustand、GSAP 或 Motion。
- Ant Design 只提供交互和组件能力，必须通过 `ConfigProvider`、集中 CSS 变量和 CSS Modules 映射项目视觉令牌，不得直接交付默认皮肤。
- 页面不得散落颜色、间距、圆角、字体和动效常量；增加前端依赖前必须由当前需求证明必要。

### R41. OpenAPI 驱动的类型化前端接口

- FastAPI OpenAPI 是前后端接口字段的唯一来源，前端使用 `openapi-typescript` 生成并提交 TypeScript 类型，使用 `openapi-fetch` 发起请求。
- 接口合同变化后必须重新生成类型；自动测试检查生成结果是否过期，过期时阻止合并。
- 前端不得手写第二份重复业务接口类型，不得直接接收 ORM 对象，也不得由页面组件解析供应商原始响应、程序堆栈或内部字段。
- 后端先把异常映射为稳定业务错误，前端再通过集中错误呈现层显示业务可读内容。

### R42. URL 状态、服务端真源与有边界轮询

- 当前批次、当前案例和队列筛选条件保存在 URL 中；刷新、浏览器返回和直接打开地址均从后端恢复相同业务位置。
- FastAPI 与 SQLite 是批次、案例、分析结果和人工结果的事实真源；TanStack Query 只保存可失效缓存，不使用 `localStorage` 保存或覆盖业务事实。
- 活动批次每 2 秒轮询；批次进入终态后停止，页面进入后台时暂停，回到前台时立即刷新一次。
- 写操作成功后只使受影响的批次、队列或案例查询失效，不允许多个页面组件各自建立无边界计时器。

### R43. 单 ZIP、两步导入与数据推导大小上限

- 浏览器每次只上传一个 ZIP；“导入并校验”与“开始批量分析”是两个独立动作，导入成功不自动产生模型调用。
- 导入结果至少显示文件名、案例数、证据数、校验结论和 Mock 标记。
- 浏览器只快速检查扩展名、空文件和配置大小上限；FastAPI 对包内容、目录、身份、关系和安全边界做最终校验。
- `upload.max_zip_bytes` 在固定演示包形成后确定：取最大合格 ZIP 实际大小约两倍并向上取整；前端与测试从同一配置含义读取，不预设未经数据支持的 50 MB 或 100 MB。

### R44. 确定性产品流程测试与真实模型验证分离

- 前端组件与交互使用 Vitest 和 React Testing Library，接口场景使用 MSW，完整浏览器旅程使用 Playwright。
- 普通自动化使用确定性的测试专用模型替身，覆盖导入、分析中、成功、失败、人工确认和刷新读回；该结果只证明产品流程通过。
- 真实 GLM-5.3-Flash 只用于独立冒烟和正式 PoC；结果单独记录为真实模型验证，不与产品流程测试合并成一个通过结论。
- 测试模型替身不得进入生产构建或业务页面，不得绕过 ZIP、Pydantic、证据引用、状态机和数据库约束。

### R45. 前端工程验收面

- 批次导入、案例队列和案例详情必须在 `1366×768` 与 `1024×768` 覆盖全部主流程，以及加载、空数据、分析中、待人工确认、已完成、处理异常、只读和密钥缺失状态；本期不做手机端。
- 页面主体不得水平滚动，1024px 下只允许表格局部横向滚动；键盘能够完成核心操作，主要点击区域不小于 44×44px，文字与控件达到 WCAG AA。
- 字体、图标和必要页面资源必须本地打包，外部网络断开时工作台仍能加载和操作。
- 自动化与人工验收都必须确认业务文字只呈现用户需要的结果，不暴露技术实现信息。

### R46. 模块优先的单仓库结构

- 项目使用一个 Git 仓库，业务实现划分为 M1 数据与存储、M2 AI 分析引擎、M3 业务服务与 API、M4 前端工作台四个模块。
- 后端以 `backend/app/contracts/` 保存纯跨模块合同，以 `backend/app/modules/data/`、`analysis/`、`application/` 保存三个后端模块；前端全部位于 `frontend/`。
- 测试、配置、脚本、文档和本机运行数据分别位于 `tests/`、`config/`、`scripts/`、`docs/`、`runtime_data/`。
- `contracts/` 不承载业务实现，不算第五个业务模块；不得建立无人负责的 `common`、`utils` 或 `shared` 业务杂物目录。

### R47. 模块单向依赖和数据边界

- M4 只能通过 M3 的 REST API 使用产品能力；M3 只通过公开接口调用 M1 与 M2；M1 与 M2 互不调用。
- M1、M2、M3 可以依赖纯共享合同，共享合同不得反向依赖任何模块。
- 跨模块只允许传递 Pydantic 数据对象、协议接口或受控文件流，不允许传递 SQLAlchemy ORM、智谱 SDK 响应或前端状态对象。
- 自动依赖边界检查必须阻止跨模块内部导入、反向依赖和循环依赖。

### R48. 共享合同单独治理

- Pydantic 跨模块结构、FastAPI OpenAPI、数据库迁移、状态枚举、错误编号、ZIP 合同、固定验收案例和前端视觉令牌属于共享合同。
- 每份共享合同只有一名主负责人，但所有受影响模块必须参与审查；全局状态枚举和验收夹具由四名成员共同审查。
- 共享合同变更必须使用独立任务和 PR，并同时说明变更原因、新旧差异、影响模块、生成文件和测试变化。
- 普通功能任务不得顺手修改共享合同，不得通过复制一份合同绕过审查。

### R49. 一任务、一分支、一 PR

- 一张任务卡对应一名负责人、一个主要模块、一个 `task/T编号-简短名称` 分支和一个 PR，不直接向主分支提交。
- 普通任务只能修改其声明的模块目录和对应测试；跨模块任务只允许修改接口适配层和合同测试。
- PR 必须关联任务编号、允许修改的文件、验收条件和实际测试结果，不得顺手重构其他模块。
- 相邻模块执行交叉审查：M1 与 M3、M2 与 M3、M3 与 M4 相互审查。

### R50. 统一代码质量门槛

- 后端在合并前必须通过 Ruff 格式与检查、Pyright、Pytest、Alembic Schema 检查和模块依赖边界检查。
- 前端在合并前必须通过 ESLint、TypeScript 类型检查、Vitest、React Testing Library、OpenAPI 生成结果检查、Playwright 和视觉静态检查。
- 本机与 GitHub PR 使用同一个 `scripts/test.ps1` 确定性入口，禁止成员各自维护互不一致的验收命令。
- 真实 GLM 冒烟和 PoC 因密钥、费用和随机性单独执行，不作为普通 PR 的自动必过检查。

### R51. 四个模块的主责边界

- M1 负责 ZIP 解析、输入校验、案例组装、证据读取、SQLite、Alembic 和 Repository；不得调用模型、决定风险或开发页面。
- M2 负责三个 Agent、Prompt、GlmClient、结构与证据校验、重试和 PoC；不得直接读写数据库、提供 FastAPI 路由或开发页面。
- M3 负责 FastAPI、RunService、后台执行、幂等、并发限制、状态机、业务 DTO 和错误映射；不得解析 ZIP 内部文件、编写 Agent Prompt 或开发 React 组件。
- M4 负责 React 工作台、上传、队列、详情、人工确认、轮询、视觉实现和浏览器测试；不得实现后端业务规则、直接访问 SQLite 或调用智谱 SDK。

### R52. 三组接口在实现前冻结

- M1—M3 数据接口必须覆盖 ZIP 导入、标准案例与证据读取、运行记录读写、决策包与人工确认保存、批次和案例查询。
- M2—M3 分析接口固定接收一个 `AnalysisRequest`，返回一个 `AnalysisOutcome`；后者只能表达完整决策包、证据不足的完整结果或带稳定分类的分析失败。
- M3—M4 通过 `/api/v1` OpenAPI 连接，覆盖导入、开始分析、进度、队列、详情、证据、重跑、人工确认和就绪状态。
- 三组接口的输入、输出、错误和状态必须在各模块功能实现前形成合同示例与合同测试；最后联调只能替换连接，不得临时重新设计接口。

### R53. 模块替身保证独立开发

- M1 使用固定 ZIP、临时目录和临时 SQLite 独立验证；M2 使用固定 `AnalysisRequest` 和测试模型客户端独立验证。
- M3 使用 `FakeDataGateway` 和 `FakeAnalysisEngine` 验证 API、状态机、幂等和错误；M4 使用 MSW 验证三个页面和完整状态。
- 替身和真实适配器必须通过同一组合同测试，真实接线通过依赖注入完成，不修改调用方业务代码迎合实现。
- 替身只能替换模块边界，不得跳过统一上传、后端校验、状态机、持久化、响应 DTO 或页面读回。

### R54. 分阶段真实接线

- 合同冻结后，四个模块先建立骨架、合同测试和替身，再由四名成员并行完成真实实现。
- 真实连接顺序固定为 M1—M3、M2—M3、M4—M3，随后运行确定性 E2E、真实 GLM 冒烟和正式 PoC。
- 禁止四个模块在一次大合并中同时首次接线；每次接线失败必须能够归属到一组合同和两个相邻模块。
- 确定性 E2E 未通过前不得进入真实 GLM 验证，产品流程通过和模型能力通过继续分别记录。

### R55. 37 张任务卡拆分基准

- 首轮以 37 张任务卡规划：M1 九张、M2 十张、M3 九张、M4 九张，最终总数允许根据真实工作量在 32—40 张之间调整。
- 每张任务卡只能有一个可验收结果、一名负责人、一个主要模块、明确文件范围、一条直接可执行的验收命令和至少一个失败场景。
- 任务执行者不需要重新决定全局架构、模块边界和共享合同；若任务暴露合同缺口，先停止该依赖实现并单独创建合同变更。
- 模块完成必须同时满足接口一致、依赖边界、正常和失败测试、替身与真实适配器合同一致、独立可运行和真实接线不改调用方业务代码。

### R56. 三份纯共享合同

- `backend/app/contracts/` 只保存 `data.py`、`analysis.py` 和 `states.py`，分别承载数据接口、分析接口及状态错误含义。
- 合同目录只允许依赖 Python 标准库、`typing` 和 Pydantic，不得导入 FastAPI、SQLAlchemy、智谱 SDK 或任何业务模块。
- 合同数据使用严格 Pydantic，禁止额外字段和宽松类型转换；行为接口使用 Python `Protocol`，不提供业务实现。
- `CaseInput v1`、`PerceptionResult`、`AttributionResult` 和 `DecisionPackage` 继续使用基于现有数据冻结的字段，不因模块化重新增加无来源字段。

### R57. M1 的五个公开数据端口

- `BatchImportGateway` 负责 ZIP 临时保存、完整校验、原子导入、重复识别和导入摘要，不接受服务器文件路径，也不调用模型。
- `CaseQueryGateway` 负责批次、队列、案例、当前业务结果和 `CaseInput v1` 查询；排序字段来自数据库列，不从任意 JSON 临时推导。
- `EvidenceGateway` 负责证据归属、安全路径、媒体类型和受控文件流，不返回本机绝对路径。
- `RunStore` 负责批次与案例运行、阶段和模型记录、完整决策包发布及失败历史；`ReviewStore` 负责幂等人工确认和当前运行校验。
- 五个端口共享同一 SQLite 和迁移体系，只返回 Pydantic 对象或受控文件流，不返回 ORM 或 Session；Fake 与真实实现使用相同合同测试。

### R58. M2 的单一分析入口和事件出口

- M2 公开同步阻塞的 `AnalysisEngine.analyze_case(AnalysisRequest, AnalysisEventSink) -> AnalysisOutcome`，由 M3 放入本机后台线程池执行。
- `AnalysisRequest` 只组合当前 `trace_id`、案例运行标识、`CaseInput v1` 和获准的合同、Prompt、动作目录版本引用，不增加当前数据不支持的业务字段。
- 成功 `AnalysisOutcome` 包含同一案例通过严格校验的 `PerceptionResult`、`AttributionResult` 和 `DecisionPackage`；证据不足通过完整合同表达，仍属于成功结果。
- M2 在阶段开始、模型尝试完成或失败、阶段校验通过、阶段最终失败和全部完成时发出类型化事件；M3 接收后通过 M1 保存。
- M2 不持有 DataGateway、SQLAlchemy Session 或数据库连接，不建立第二套任务队列或独立服务进程。

### R59. 预期结果和意外异常分离

- 导入拒绝、关系未确认、证据不足、模型严格校验失败、重试耗尽、幂等命中、案例完成和运行过期属于可预期情况，必须返回可区分的类型化结果。
- 程序缺陷、SQLite 损坏、磁盘写入失败、合同之外的 SDK 异常和原子写入失败属于意外技术异常。
- 意外异常由最近模块边界捕获并保留技术因果链，M3 映射为稳定 `BusinessError`；跨模块和业务页面不传播异常类名、堆栈、供应商响应或本机路径。
- 证据不足不得进入处理异常，预期错误也不得用任意 Python 异常字符串充当业务合同。

### R60. M3 决定状态，M1 原子写入

- M3 决定批次和案例是否允许开始、阶段事件是否符合状态机、完整结果能否发布、失败是否耗尽以及人工确认是否通过业务校验。
- M1 使用独立短事务原子创建或返回运行、保存阶段与模型记录、发布完整结果、记录失败和保存人工确认。
- M3 不创建、提交或跨调用传递 SQLAlchemy Session，M1 不自行决定业务流程，M2 不接触任何数据库事务。
- 等待 GLM 时不得保持数据库事务；任一写入失败不得留下半条状态、半个决策包或已经切换但未保存的当前指针。

### R61. 显式装配的开发和演示模式

- M3 使用应用工厂集中创建 FastAPI 并显式注入 M1 端口与 M2 引擎，不使用跨模块可变全局单例。
- `scripts/dev.ps1` 同时启动 FastAPI 与 Vite，用于热更新、模块调试和真实前后端联调。
- `scripts/start.ps1` 校验配置和运行目录、取得单实例锁、构建当前前端、执行迁移与中断恢复，再由 FastAPI 同时提供 `/api/v1` 和前端页面。演示不依赖 Docker、Vite 开发服务器或第二个手工终端。
- Fake 只能由测试命令或显式测试配置注入，演示模式和业务页面不得启用；缺少 `ZAI_API_KEY` 时保留历史读取并禁用分析，不自动切换测试模型。

### R62. 十个资源型 HTTP 接口

- `/api/v1` 只提供批次上传与列表、批次详情与启动、案例队列与详情、案例重跑、人工确认、证据内容和健康检查十个接口，完整路径以 `docs/PSIT-MVP技术实施规格.md` 第 12 节为准。
- 上传新批次返回 `201 BatchWorkspaceView`，相同 ZIP 返回 `200 BatchWorkspaceView`；开始分析返回 `202 BatchWorkspaceView`，案例重跑返回 `202 CaseDetailView`。
- 新人工确认返回 `201 ReviewResultView`，相同 `submission_id` 重试返回 `200 ReviewResultView`；证据接口返回真实媒体类型的文件流。
- 单项查询直接返回资源，列表返回 `items` 和 `total`，写操作返回保存或接受后的业务资源，不使用只有 `success: true` 的空外壳。
- 开始与重跑没有 JSON 请求体，页面不得传入模型参数、Prompt、并发或运行编号。

### R63. 隐藏运行编号并使用审核 Token

- 批量开始和案例重跑的业务响应均不返回 `batch_run_id`、`case_run_id` 或其他运行编号，前端只按 `batch_id` 和 `case_id` 轮询。
- 只有允许人工确认的 `CaseDetailView` 才包含不展示给用户的 `review_token`；它是不可解析的并发校验值，不是运行编号、登录或权限凭证。
- 当前结果因重新分析而变化时 Token 必须变化；人工确认原样带回 Token，缺少时按字段合同错误处理，不一致时返回 `409 STALE_CASE_RESULT`。
- M1 必须在保存审核的同一事务内再次校验当前内部案例运行，并把 `reviews.case_run_id` 关联到真实运行，不新增用户可编辑的运行字段。

### R64. 有限状态码和统一 BusinessError

- v1 只使用 200、201、202、400、404、409、413、415、422、503 和 500，并严格采用 API 合同中的固定含义。
- 所有 JSON 错误统一返回 `BusinessError` 的稳定编号、中文摘要、失败对象、业务阶段、下一步和 `trace_id`。
- FastAPI 默认 422 校验详情和默认 500 响应也必须在 API 边界转换，不得形成第二套错误结构或 HTML 错误页。
- 智谱供应商状态码、原始响应、异常类名、堆栈、密钥和本机路径不得透传到业务接口。

### R65. 简单分页、有限筛选和固定排序

- 历史批次查询默认 `limit=20`、`offset=0`，案例队列默认 `limit=50`、`offset=0`；`limit` 最小 1、最大 100，`offset` 最小 0。
- 历史批次按 `imported_at` 倒序和稳定 `batch_id` 排序；案例队列只允许一个运行状态和一个介入等级筛选，继续由后端执行冻结的业务排序。
- M4 把案例筛选和分页位置保存在 URL，不能用前端重新排序改变后端返回的处理顺序。
- v1 不提供批次名称搜索、客户全文搜索、任意 `sort_by`、复杂组合筛选和游标分页。

### R66. 最小业务响应 DTO

- `BatchWorkspaceView` 只含批次标识、上传文件名、Mock 标记、业务状态、案例与证据计数、分析成功与异常计数、导入时间、开始分析能力及按需业务说明。
- `CaseQueueItemView` 只含案例定位、脱敏客户、高价值标记、按需风险摘要与当前介入等级、排序理由、业务状态和三类必要异常标记。
- `CaseDetailView` 只含页面定位、高价值结论、当前有效风险与处理结果、按需不确定性和证据缺口、当前结果实际引用的业务证据、Mock 与人工结果、可执行动作和按需 `review_token`。
- `ReviewResultView` 返回人工结果、当前有效人工判断、按需执行说明、审核原因和时间，不返回内部审核或案例运行编号；`HealthView` 只返回应用、数据库、分析能力及业务说明。
- 所有业务 DTO 省略不适用字段，不输出大批 `null`；证据编号只用于请求证据内容，页面不把编号作为文字展示。

### R67. 四种严格人工确认请求

- 人工确认以 `outcome` 为 Pydantic 区分字段，共同携带 UUID `submission_id` 和当前 `review_token`。
- `APPROVED` 不提交另一份最终结果或原因，后端直接采用页面当前系统结果。
- `MODIFIED_AND_APPROVED` 提交完整人工最终介入等级、当前原因枚举内的最终类别、最多三项目录内动作、按需执行说明和必填修改原因。
- `REJECTED_WITH_JUDGMENT` 提交完整人工最终介入等级、原因类别、最多三项目录内动作、按需执行说明和必填驳回原因；驳回不代表无需介入。
- `INSUFFICIENT_EVIDENCE` 提交必填证据缺口说明，不提交编造的确定原因和处理动作。
- 后端拒绝 JSON Patch、任意内部 JSON、修改无原因、驳回无人工判断、证据不足无说明和直接通过夹带另一份结果。

### R68. 六个业务原因和一个非原因兜底值

- 原因枚举的六个真实业务类别固定为 `LOGISTICS_FULFILLMENT`、`PRODUCT_ISSUE`、`RETURN_REFUND`、`SERVICE_COMMUNICATION`、`PRICE_OR_BENEFIT`、`OTHER`。
- `INSUFFICIENT_EVIDENCE` 是当前无法可靠判断原因时的非原因兜底值，不得表述为第七种客户流失原因，也不得合并进 `OTHER`。
- Agent 在证据不足时仍返回字段完整、引用缺失证据和不确定性的结果；页面只显示“证据不足，暂时无法确认主要原因”。
- 人工确认选择 `INSUFFICIENT_EVIDENCE` 结果时只填写证据缺口说明，不要求选择一个虚假的最终原因类别。

### R69. 五状态案例业务生命周期

- 案例状态固定为 `PENDING_ANALYSIS`、`ANALYZING`、`PENDING_REVIEW`、`COMPLETED`、`PROCESSING_ERROR`，所有转换由单一状态机控制。
- 待分析案例只能由所属批次开始进入分析；三阶段形成完整结果，包括诚实的证据不足结果，进入待人工确认；技术失败和程序中断进入处理异常。
- 待人工确认和处理异常允许人工触发重新分析并进入分析中；已完成是不可重跑、不可重新审核、不可无痕修改的终态。
- v1 不增加停止、取消、暂停、恢复、排队位置或 Agent 阶段业务状态，所有非法转换必须返回稳定业务错误且不写库。

### R70. 四种批次业务状态

- 批次业务状态固定为 `PENDING_ANALYSIS`、`ANALYZING`、`COMPLETED`、`COMPLETED_WITH_ERRORS`，不直接使用内部批次运行状态。
- 全部案例尚未启动时为待开始；任一当前案例分析中时为分析中；没有分析中和处理异常案例时为已完成；没有分析中但至少一个当前案例处理异常时为已完成但存在异常。
- 人工重跑单个案例期间批次业务状态为分析中，结束后根据全部案例当前状态重新计算。
- ZIP 校验失败不创建批次，因此不增加导入失败批次状态；页面不显示线程、Future、排队位置或内部运行阶段。

### R71. 当前批次投影与首次运行历史分离

- `batches.case_count` 保存固定案例总数，`batches.analysis_succeeded_count` 与 `batches.error_count` 保存案例当前投影，`batches.status` 保存当前批次业务状态。
- 案例开始重跑、重跑成功或重跑失败时，案例状态和 `batches` 当前投影必须在同一短事务中更新。
- `batch_runs.total_case_count`、`analysis_succeeded_count`、`error_count` 和内部终态保存首次批量分析历史，结束后只读，人工重跑不得改写。
- `BatchWorkspaceView` 只显示 `batches` 当前投影，不显示首次运行历史失败数，也不由前端重新统计形成第三套结果。

### R72. 启动前收敛中断运行

- 数据库迁移成功后、HTTP 接收请求前，M3 必须扫描遗留活动 `case_runs` 与 `batch_runs` 并在恢复流程中收敛。
- 活动案例运行标为 `INTERRUPTED`；没有形成完整决策包的对应案例进入 `PROCESSING_ERROR` 并记录稳定错误 `APP_INTERRUPTED`；活动批次运行标为 `INTERRUPTED`。
- 恢复必须保留已写阶段结果、模型调用、失败和历史，重新计算受影响批次当前状态和计数，并具有重复执行不再改变结果的幂等性。
- 恢复检查不得从中间阶段续跑、创建新运行或产生 GLM 调用；运营人员只能通过现有单案例重跑从头处理。

### R73. 单一应用级后台执行器和关闭边界

- M3 在 FastAPI 生命周期内只创建一个 `ThreadPoolExecutor`，一个案例运行对应一个 Future，`max_workers` 从严格配置读取，当前 PoC 基线为 2。
- 浏览器关闭或刷新不影响后台分析，一个案例失败不取消同批其他案例，重复请求和数据库约束共同阻止第二次付费运行。
- 正常关闭时先拒绝新的开始和重跑，取消尚未执行的 Future；正在进行的 GLM 请求等待返回或达到响应超时，M2 在下一 Agent 阶段开始前检查关闭标记。
- 当前请求和记录完成后不再进入下一阶段；已经形成完整三阶段结果的案例正常发布，其他案例内部运行标为 `INTERRUPTED`、业务状态进入处理异常。
- 强制结束来不及收敛时由下次启动恢复；本期不使用 Redis、Celery、多进程 Worker、断点续跑或用户停止按钮。

### R74. 项目根目录定位运行数据并允许显式本机覆盖

- `storage.runtime_dir` 默认值为项目根目录下的 `runtime_data/`，所有相对值都按项目根目录解析，不受调用脚本时的命令行当前目录影响。
- 成员可以通过不进入 Git 的 `config/local.toml` 显式改为另一处本机目录；允许本机绝对路径，不允许 UNC 网络路径、系统临时目录或不可写目录。
- 运行目录解析、创建或写入检查失败时必须以 `RUNTIME_DIR_INVALID` 停止，不得静默退回当前目录、默认目录或临时目录。
- 数据库只保存相对已解析运行根目录的文件路径；最终绝对目录只允许出现在启动诊断中，不进入业务 API 和页面。

### R75. 固定回环默认端口和显式端口覆盖

- 演示环境默认由 FastAPI 在 `127.0.0.1:8000` 提供页面和 API；开发环境默认使用 FastAPI 8000 和 Vite 5173。
- v1 只允许监听 `127.0.0.1`，不得通过配置改为 `0.0.0.0`、局域网地址或公网地址。
- 成员可以在 `config/local.toml` 中显式覆盖端口；端口必须位于 1024—65535，FastAPI 与 Vite 端口不得相同，启动脚本、应用和开发代理读取同一已校验配置。
- 端口被占用时以 `PORT_IN_USE` 停止并给出处理办法，不自动选择随机端口。

### R76. 每个运行目录使用 Windows 系统级单实例锁

- 应用在数据库备份、迁移、运行恢复、执行器创建和 HTTP 开放之前，必须取得运行根目录下 `psit.lock` 的操作系统独占锁并持有至进程退出。
- Windows 交付使用 Python 标准库 `msvcrt`，不得仅凭锁文件是否存在判断，也不为该能力新增第三方依赖。
- 同一运行目录无法取得锁时以 `INSTANCE_ALREADY_RUNNING` 停止，不接触数据库、不创建后台任务、不调用 GLM。
- 进程异常结束后系统锁自动释放；即使锁文件仍存在，下次启动也应能够取得锁并更新诊断信息。不同运行目录只有在同时使用不同端口时才允许作为两套独立开发实例运行。

### R77. MVP 数据保护边界

- Alembic 创建或升级失败时立即停止，不删除、不重建、不自动覆盖现有数据库。
- 重要演示前需要保护数据时，先停止应用，再人工复制完整 `runtime_data/`；不能只复制 WAL 模式下的单个 `psit.db` 并把它称为一致性备份。
- 本期不实现 SQLite backup API、自动完整性检查、自动保留轮换和 `backup.ps1`；这些能力在出现真实数据库升级和长期数据保留需求后单独立项。

### R78. 每次演示启动构建当前前端

- `scripts/start.ps1` 每次演示启动都执行当前锁文件对应的前端构建；构建失败以 `FRONTEND_BUILD_FAILED` 停止，不提供旧页面。
- 启动脚本不得安装新版本、升级依赖或修改锁文件，首次环境准备继续由 `bootstrap.ps1` 负责。
- 本期不实现内容指纹、构建缓存、临时 `dist` 和原子替换；这些优化不能阻塞真实业务闭环。

### R79. 就绪后显示实际访问地址

- 演示模式只有在应用和数据库均已就绪后，才显示实际已校验的本机访问地址；用户自行在浏览器打开。
- 分析服务暂不可用不阻止历史工作台读取；页面保持开始分析和重新分析不可用，并显示业务可理解的处理提示。
- 本期不自动打开浏览器，不提供 `-NoBrowser` 和浏览器打开告警逻辑。

### R80. 导入临时目录与原子发布

- 每次上传使用 `imports/tmp/` 下的唯一目录；正常校验失败在记录稳定错误和 `trace_id` 后清理，成功导入通过正式文件与数据库事务共同发布。
- 异常中断留下的未发布临时内容不得成为批次、证据或模型输入；启动恢复只依据数据库当前状态收敛运行，不从临时目录自动恢复业务。
- 本期不建设 quarantine、`recovery.json` 和隔离样本保留策略；程序不得因为清理临时残留而删除陌生文件或正式批次。

### R81. Ctrl+C 复用唯一正常关闭流程

- `start.ps1` 在前台窗口运行并显示实际地址、数据库状态、分析能力状态和 Ctrl+C 关闭说明；启动失败返回非零状态，正常安全关闭返回零。
- Ctrl+C 进入 R73 的唯一关闭流程；强制结束来不及收敛时由下次启动恢复。
- 本期不提供 `stop.ps1`、停止控制文件、业务页面关机按钮或无认证关机接口；浏览器关闭仍不停止后台分析。

## Acceptance Criteria

### A. 输入、身份、组案

- [ ] 导入包含来源标记的标准案例批次 → 合格案例进入待分析状态并显示批次摘要，不合格案例显示具体错误，导入过程不调用模型 → 通过批次校验、模型调用记录和案例状态验证。
- [ ] 通过浏览器上传固定演示 ZIP → 与任意同结构 ZIP 经过相同临时保存、校验和正式导入链路，页面不存在服务器路径输入或隐藏直入入口 → 通过 API 集成测试和浏览器 E2E 验证。
- [ ] 连续上传完全相同的 ZIP → 第二次返回已有批次，案例、证据和运行记录数量不增加 → 通过 API 集成测试和数据库读回验证。
- [ ] 上传已有 `batch_id` 但内容校验值不同的 ZIP → 无论其数据版本如何，导入均被拒绝并返回 `BATCH_ID_CONFLICT`，已有批次不改变 → 通过负向输入测试验证。
- [ ] 导入 observed 证据 → 保留原始内容、来源、原始 ID，以及源数据实际存在的时间 → 通过保存后的标准证据记录验证。
- [ ] 导入 derived 字段 → 能追溯到原始字段和固定计算规则 → 通过派生记录和复算结果验证。
- [ ] 导入 simulated 关系 → 页面和保存记录均显示模拟标记及双方真实来源 → 通过案例详情和持久记录验证。
- [ ] 导入关系未确认的数据 → 系统拒绝合并进同一标准案例包 → 通过校验错误和案例包内容验证。
- [ ] 导入含预写事件、情绪、原因、策略答案的运行输入 → 系统拒绝或隔离答案字段 → 通过校验结果和运行输入快照验证。

### B. 批量预分析与独立失败

- [ ] 导入至少 5 个案例但不点击开始分析 → 全部保持待分析且模型调用数为零 → 通过案例状态和模型调用记录验证。
- [ ] 点击一次“开始批量分析” → 每个案例独立流转到待人工确认或处理异常 → 通过运行状态记录验证。
- [ ] 开始批量分析请求返回 → 不等待整个批次，立即得到更新后的批次状态与统计摘要，响应不含内部运行标识 → 通过 API 集成测试验证。
- [ ] 同一批次分析中连续提交开始请求 → 每次返回同一运行记录且模型调用数量不增加 → 通过并发 API 集成测试和模型调用记录验证。
- [ ] 一个批次正在分析时启动第二个批次 → 返回当前活动批次编号，第二个批次保持待分析且没有新增模型调用；前一批结束后第二批也不会自动开始 → 通过 API 集成测试、状态记录和模型调用记录验证。
- [ ] 批次内一个案例先完成、其他案例仍在运行 → 已完成案例立即可打开和审核 → 通过浏览器 E2E 验证。
- [ ] 感知或归因阶段已经完成但策略阶段仍在运行 → 业务接口和页面只显示分析中，不返回事件、情绪、原因等半成品 → 通过 API 合同测试和浏览器 E2E 验证。
- [ ] 策略阶段最终失败 → 案例进入处理异常，内部已完成阶段仍可由开发核验，但业务页面没有残缺决策包 → 通过故障注入、数据库读回和浏览器 E2E 验证。
- [ ] 在一个案例中制造可复现技术失败 → 该案例进入处理异常，同批其他案例继续完成 → 通过批次状态和各案例运行记录验证。
- [ ] 对处理异常案例执行重新分析 → 系统创建可追溯的重试结果，不覆盖原失败原因 → 通过运行记录验证。
- [ ] 同一案例正在重新分析时重复提交重跑请求 → 返回当前重跑记录，不创建第二个运行版本 → 通过并发 API 集成测试验证。
- [ ] 启动含多个案例的批次 → 创建一个 `batch_run` 和每案例一个 `case_run`；重跑单个案例时只新增一个 `case_run` → 通过数据库读回验证。
- [ ] 重跑一个案例 → 新 `case_run.batch_run_id` 为空、`trigger_type` 为 `MANUAL_RERUN` 并指向上一个案例运行；原 `batch_run` 计数不改变 → 通过数据库读回验证。

### C. 多模态联合判断

- [ ] 输入文本、图片、行为齐全的验收案例 → 三类证据进入同一事件还原和决策包 → 通过结果结构和 evidence_id 引用验证。
- [ ] 输入图文相互支持案例 → 系统显示支持关系和对应证据 → 通过案例详情验证。
- [ ] 输入图文冲突案例 → 系统显示冲突、降低确定性或交给人工 → 通过冲突标记和结果内容验证。
- [ ] 输入图片模糊案例 → 系统显示图片证据不足，不根据文本编造图片内容 → 通过图片分析结果和事件还原验证。
- [ ] 输入无图片普通案例 → 系统降级运行并标记图片缺失 → 通过案例状态和详情提示验证。
- [ ] 输入缺少图片的多模态验收案例 → 系统判定该案例不能用于多模态验收 → 通过验收结果验证。

### D. 事件、情绪、决策包

- [ ] 完成一个正常案例分析 → 事件还原区分事实、派生、模拟、推断，并列出证据、按需出现的冲突、缺口和不确定性 → 通过结构化结果和页面验证。
- [ ] 完成一个正常案例分析 → 情感感知结果包含当前可能情绪、简短解释、客户文本证据和按需出现的不确定性 → 通过结构化结果和页面验证。
- [ ] 完成一个正常案例分析 → 决策包同时包含介入等级、风险原因假设、受约束处理方案 → 通过结果结构完整性验证。
- [ ] 生成介入等级 → 值只属于必须介入、建议介入、暂不介入，并含排序理由 → 通过字段枚举和页面验证。
- [ ] Agent 对未解决严重问题返回“暂不介入” → 程序采用最低允许等级并保存调整原因，不新增模型调用 → 通过规则单元测试和运行记录验证。
- [ ] 案例仅具有高价值客户身份、没有严重未解决事件 → 程序不自动提升为“必须介入” → 通过规则单元测试验证。
- [ ] 生成风险原因假设 → 含一个主要原因及支持证据；存在反证或另一类可信原因时才出现对应字段 → 通过结果结构验证。
- [ ] 生成处理方案 → 方案只由六类批准动作组成，每项含类型、描述、原因、支持证据和按需出现的前置条件 → 通过策略规则校验验证。
- [ ] 输入证据不足案例 → 三项决策字段仍存在，并明确无法判断内容和缺失证据 → 通过结果结构和人工审核结果验证。

### E. 队列、详情、人工确认

- [ ] 批次导入并启动分析 → 全部案例立即进入队列，规定字段、状态和异常提示随进度更新 → 通过浏览器 E2E 验证。
- [ ] 默认打开案例队列并切换历史批次 → 一次只展示所选批次，不混入其他批次或其他数据版本的案例 → 通过浏览器 E2E 验证。
- [ ] 查看运行中的批次和案例 → 页面只显示五种业务状态、总案例数、分析完成数和处理异常数，不显示 Agent 阶段、Prompt、Token 或技术调用日志 → 通过浏览器 E2E 和静态文本扫描验证。
- [ ] 查看队列和案例详情 → 除必要业务状态、可处理错误和按需展开的证据外，页面只回答高价值客户是谁、为什么有流失风险、应该怎么做；不出现 RFM 方法、数据库字段、运行编号、数据版本、Schema、原始 JSON 或技术阶段 → 通过浏览器 E2E 和敏感词扫描验证。
- [ ] 调用所有业务查询接口 → 响应均符合五类 Pydantic DTO 白名单，不出现数据库整数主键、`stage_results`、`model_calls` 或任意未声明字段 → 通过 OpenAPI Schema 和响应合同测试验证。
- [ ] 一个案例尚未人工确认 → 队列和详情使用系统建议；该案例完成结构化修改并确认后 → 队列和详情改用人工最终结果 → 通过 API 集成测试和浏览器 E2E 验证。
- [ ] 人工没有修改系统建议直接通过 → 已完成详情不重复展示两份相同结果；人工发生修改 → 默认展示人工结果，并能折叠查看系统原业务建议和修改原因 → 通过浏览器 E2E 验证。
- [ ] 打开案例详情证据区 → 默认只出现当前结果引用的客户对话、售后图片、订单与客户价值事实；不出现证据编号、哈希、路径、内部身份字段、来源 JSON 或模型参数 → 通过浏览器 E2E 和响应合同测试验证。
- [ ] 制造一个可复现导入错误和一个模型调用错误 → 页面均显示中文摘要、失败对象、失败环节、稳定错误编号和下一步，且不包含供应商原始响应、程序堆栈、API Key 或内部绝对路径 → 通过 API 集成测试和浏览器 E2E 验证。
- [ ] 队列存在不同系统建议介入等级 → 按介入等级排序，同等级按系统导入时间、稳定 `case_id` 排序 → 通过排序结果验证。
- [ ] 运营人员修改介入等级并填写原因 → 队列按最终人工介入等级重新排序，系统原等级保留 → 通过浏览器 E2E 和持久记录验证。
- [ ] 打开案例详情 → 先看到结论，可展开每项判断的原始证据和业务可读特征 → 通过浏览器 E2E 验证。
- [ ] 直接刷新批次地址或案例详情地址 → 页面从后端恢复对应记录，浏览器返回后仍回到原批次队列 → 通过浏览器 E2E 验证。
- [ ] 查看图片证据 → 页面通过批次、案例和证据标识加载实际 JPG、JPEG、PNG 或 GIF，不出现本机绝对路径；伪造其他案例的证据编号时访问被拒绝 → 通过 API 集成测试和浏览器 E2E 验证。
- [ ] 全部认可决策包并一键通过 → 人工审核结果为通过，系统原结果和操作时间保存 → 通过浏览器 E2E 和持久记录验证。
- [ ] 使用同一 `submission_id` 和原 `review_token` 重复提交人工确认 → 返回第一次保存的结果且数据库没有第二条人工结果；使用新编号修改已完成案例时返回 `CASE_ALREADY_COMPLETED` → 通过 API 集成测试和数据库读回验证。
- [ ] 修改介入等级、风险原因或处理动作后提交 → 只能选择已批准枚举，未填写原因时不能提交，填写后保存人工最终结果 → 通过浏览器 E2E 和持久记录验证。
- [ ] 驳回决策包 → 未填写原因时不能提交，提交后包含最终人工判断或证据不足结果 → 通过浏览器 E2E 和持久记录验证。
- [ ] 刷新页面并重新打开案例 → 系统原结果、人工最终结果、原因、时间仍可查看 → 通过浏览器 E2E 和存储读回验证。
- [ ] 打开已完成案例 → 页面保持只读且不存在重新分析入口 → 通过浏览器 E2E 验证。
- [ ] 一个待人工确认案例重新分析成功 → `current_case_run_id` 原子切换到新版本，旧案例运行、阶段结果、模型调用和原人工记录仍可读回 → 通过事务集成测试和数据库读回验证。
- [ ] 提交人工确认 → 页面只提交 `review_token`，M1 在事务内把 `reviews` 关联到正确案例和内部当前 `case_run`；旧 Token 不能审核已经过时的结果 → 通过外键、Token 和冲突测试验证。

### G. 数据库和启动保护

- [ ] 两个开始请求并发到达 → 数据库中最多只有一个活动 `batch_run` → 通过并发集成测试和唯一约束验证。
- [ ] 同一案例两个重跑请求并发到达 → 数据库中最多只有一个活动 `case_run` → 通过并发集成测试和唯一约束验证。
- [ ] 两个后台案例同时等待 GLM 返回 → 数据库不存在持续占用的长事务，其他状态查询仍可完成 → 通过集成测试验证。
- [ ] 启动空运行目录或已有数据库 → Alembic 正确创建或升级九表，升级失败时应用不启动且不删除、不重建、不覆盖原数据库 → 通过启动脚本、SQLite 和故障注入验证。
- [ ] 检查 Git 跟踪文件 → `runtime_data/`、`frontend/dist/` 不在版本控制中，固定夹具和密封答案保持物理分区 → 通过仓库静态检查验证。
- [ ] 导入两个不同数据版本但使用相同 `batch_id` 的不同 ZIP → 第二个批次被拒绝，API 路由始终只能定位一个批次 → 通过唯一约束和 API 集成测试验证。
- [ ] 检查九张表的外部标识 → 数据版本、批次和 UUID 标识满足对应唯一约束，数据库整数主键不出现在业务 API → 通过 Schema 测试和响应合同测试验证。
- [ ] 打开待确认案例后完成一次重新分析，再使用旧页面的 `review_token` 提交 → 返回 `STALE_CASE_RESULT`，`reviews` 不产生记录且业务 API 从未暴露 `case_run_id` → 通过 Token、外键和 API 集成测试验证。
- [ ] 检查批次统计 → 页面显示总案例数、分析完成数、处理异常数，人工重跑不修改原批次历史计数 → 通过数据库读回和浏览器 E2E 验证。
- [ ] 检查数据库和 API 中的程序时间 → 全部使用 UTC ISO 8601，页面显示北京时间；源数据缺失时间仍保持缺失 → 通过格式断言和浏览器 E2E 验证。

### H. Agent 调用、校验与 PoC

- [ ] 检查三个阶段的实际请求清单 → 感知不含高价值和 RFM，归因不含原始图片和高价值，策略只含两个上游结果、高价值结论、证据编号和动作约束 → 通过阶段输入单元测试和 `request_manifest_json` 验证。
- [ ] 静态扫描产品代码 → 只有 `GlmClient` 可以导入或调用智谱 SDK，Agent 和 `RunService` 不直接发送网络请求 → 通过依赖边界测试验证。
- [ ] 修改 Pydantic 输出字段 → 运行时注入的 JSON Schema 同步变化，Prompt 文件不需要手工复制字段结构 → 通过 Schema 快照测试验证。
- [ ] 模型返回多余字段、错误类型、错误案例编号或不存在的证据编号 → 结果不进入 `stage_results`，系统执行至多一次定向修复 → 通过参数化合同测试验证。
- [ ] Strategy Agent 返回低于程序最低要求的介入等级 → 程序直接提升并记录原因，模型调用数量不增加 → 通过规则单元测试和调用记录验证。
- [ ] 模型返回结构合格但包含明确不确定性 → 案例进入待人工确认，不因主观质量判断自动重跑 → 通过固定证据不足案例验证。
- [ ] 连续制造连接中断、429 或 5xx → 首次加最多两次网络重试；再触发一次结构修复后，总供应商请求不超过四次 → 通过故障注入和 `model_calls` 计数验证。
- [ ] 返回 401、403、请求参数、图片格式或输入合同错误 → 不执行网络重试，案例进入可定位的处理异常 → 通过故障注入测试验证。
- [ ] 定向修复请求发生网络错误 → 不继续递归重试，当前案例处理异常，同批其他案例继续 → 通过故障注入测试验证。
- [ ] 完成 15 个固定案例 PoC → 形成结构通过率、修复通过率、阶段失败率、耗时、Token、调用次数、图片贡献、`high/max` 对比和并发 1—3 对比记录 → 通过 PoC 报告完整性检查验证。

### I. 配置、工具链、日志与 PoC 硬门槛

- [ ] 用选定配置完整运行 15 个固定案例 → 45 个阶段全部产生真实调用记录，所有已发布结果通过严格合同，失败案例完整保留，且没有证据越界、目录外动作或密封答案泄漏 → 通过机器统计、请求清单和结果合同验证。
- [ ] 两名验收者独立检查 5 个密封案例 → 5 个案例均满足事实、证据、原因、不确定性、介入等级和动作范围要求，并一致认为可作为人工决策初稿 → 通过密封评分表验证。
- [ ] 执行两个图片贡献案例和一个行为贡献案例 → 三个案例均达到规定差异 → 通过消融结果验证。
- [ ] 以并发 1、2、3 运行 PoC → 记录首个完整案例时间、整批时间、429、超时、Token 和 SQLite 写入情况，再据实冻结正式并发和内部预警线 → 通过 PoC 时间线和错误统计验证。
- [ ] 删除 `ZAI_API_KEY` 后启动应用 → 工作台和历史数据仍可访问，开始与重跑操作不可用且只显示业务提示；`/health` 将分析服务标为未就绪且不泄露密钥 → 通过启动测试、API 测试和浏览器 E2E 验证。
- [ ] 在 `config/default.toml` 中写入类型错误或越界配置 → 启动立即失败并指出具体配置项，不带错运行 → 通过参数化启动测试验证。
- [ ] 扫描 Git 跟踪文件、配置、Prompt、日志和业务响应 → 不存在 API Key 值、`.env`、完整客户对话、图片 base64、完整 Prompt、密封答案或本机绝对路径 → 通过密钥与敏感信息扫描验证。
- [ ] 在干净环境执行四个 PowerShell 脚本 → 能按锁文件初始化、开发启动、运行完整测试和演示启动，且没有成员电脑或 Codex 缓存绝对路径 → 通过脚本集成测试和静态路径扫描验证。
- [ ] 制造一次跨 API、执行器、Agent、模型客户端和数据库的失败 → JSONL 日志字段稳定、同一链路 `trace_id` 一致、敏感内容和原始供应商响应不重复写入日志 → 通过日志解析、链路追踪和敏感信息测试验证。
- [ ] 完成或失败一次正式 PoC → 报告、汇总和逐案例结果三个产物完整且相互引用；未达到任一 R34 门槛时不会把配置标记为正式可用 → 通过产物 Schema、门槛判定和负向流程测试验证。

### J. 前端视觉与交互质量

- [ ] 打开三个产品页面 → 视觉使用单一动作蓝、白与浅灰表面、统一字体层级、统一圆角和无投影业务组件，不出现 AI 紫色渐变、营销 Hero、Bento 工作流或默认组件库皮肤 → 通过视觉令牌检查、页面截图和人工审阅验证。
- [ ] 在 `1366×768` 与 `1024×768` 打开批次导入、案例队列和案例详情 → 页面主体没有水平滚动，当前状态和唯一主操作可见；1024px 下只有案例表格可以局部横向滚动 → 通过浏览器尺寸测试验证。
- [ ] 打开案例详情首屏 → 先看到高价值结论、主要风险原因和处理建议，证据按需展开，不出现技术字段或装饰性业务数据 → 通过浏览器 E2E 和可见文字扫描验证。
- [ ] 使用键盘完成主操作、队列导航、证据展开和人工确认 → 焦点顺序正确、焦点环可见、主要点击区域不小于 44×44px → 通过键盘 E2E 和尺寸断言验证。
- [ ] 检查正文、按钮、输入框、帮助文字、错误文字和语义状态 → 正文对比度不低于 4.5:1，大字不低于 3:1；所有状态同时使用文字、图标和颜色 → 通过自动对比度检查和人工审阅验证。
- [ ] 分别触发加载、空数据、分析中、处理异常、只读和缺少模型密钥状态 → 每种状态都有稳定布局和可执行信息，不依赖整页旋转图标或短暂 Toast 承担关键错误 → 通过组件测试和浏览器 E2E 验证。
- [ ] 断开外部网络后启动并使用产品 → 字体、图标和必要页面资源正常加载，页面不请求外部字体、图片、脚本或外部 Skill → 通过网络请求拦截和离线 E2E 验证。
- [ ] 扫描前端依赖与源码 → 只使用一套功能组件库和一套图标族，不存在 GSAP、随机版式脚本、假截图或外部 Skill 安装产物 → 通过依赖清单和源码静态检查验证。

### K. 前端工程合同

- [ ] 检查前端依赖、主题入口和三个产品页面 → 使用 React、TypeScript、Vite、Ant Design、React Router、TanStack Query 和唯一图标族；页面均使用集中主题令牌而非默认皮肤或散落常量 → 通过依赖清单、源码静态检查和主题快照验证。
- [ ] 修改一个 FastAPI 业务响应字段但不重新生成前端类型 → 合同检查失败；重新生成后 `openapi-fetch` 客户端与 OpenAPI 一致 → 通过生成结果差异检查、TypeScript 类型检查和 API 合同测试验证。
- [ ] 在活动批次、终态批次和浏览器后台之间切换 → 活动批次每 2 秒刷新，终态停止，后台暂停，回到前台立即刷新；刷新和返回保持 URL 中的批次、案例与筛选条件 → 通过假计时器组件测试和浏览器 E2E 验证。
- [ ] 查看浏览器存储并修改其中任意值 → 批次、案例、系统结果和人工结果仍从后端恢复，`localStorage` 不能成为业务真源 → 通过浏览器 E2E 和服务端读回验证。
- [ ] 上传一个合格 ZIP → 页面先完成导入并显示文件名、案例数、证据数、校验结论和 Mock 标记，只有再次点击开始分析才产生模型调用；前端快速检查不能绕过 FastAPI 最终校验 → 通过 API 集成测试、模型调用记录和浏览器 E2E 验证。
- [ ] 分别运行确定性产品流程测试和真实 GLM 验证 → 两份结果独立标记，前者不声称模型可用，后者不替代产品回归；生产构建不存在测试模型入口 → 通过构建扫描、Playwright 报告和 PoC 产物验证。

### L. 四模块并行开发与集成

- [ ] 检查仓库结构和负责人清单 → 只存在 M1 数据与存储、M2 AI 分析引擎、M3 业务服务与 API、M4 前端工作台四个业务模块，每个模块有唯一主目录和主负责人 → 通过目录静态检查和任务清单验证。
- [ ] 执行依赖边界检查 → M4 不导入后端，M1 与 M2 互不导入，M3 不导入 M1、M2 内部实现，共享合同不反向导入模块，跨模块没有 ORM 或 SDK 响应 → 通过导入图和负向依赖夹具验证。
- [ ] 在 M2、M3、M4 尚未完成时运行 M1 → 固定 ZIP 能在临时 SQLite 完成导入、拒绝坏包和读回 → 通过 M1 独立测试验证。
- [ ] 在 M1、M3、M4 尚未完成时运行 M2 → 固定分析输入能通过测试模型产生完整结果和规定失败 → 通过 M2 独立测试验证。
- [ ] M3 只接入 `FakeDataGateway` 与 `FakeAnalysisEngine` → 导入编排、状态机、幂等、业务 DTO 和错误响应可以独立通过 → 通过 M3 API 与合同测试验证。
- [ ] M4 只接入 MSW → 三个页面、完整状态、URL 恢复和人工确认交互可以独立通过 → 通过组件测试和前端 Playwright 验证。
- [ ] 分别对替身和真实适配器运行同一合同测试 → 输入、输出、错误和状态行为一致；替换适配器后调用方业务代码没有变化 → 通过合同测试和变更范围检查验证。
- [ ] 按 M1—M3、M2—M3、M4—M3 顺序接线 → 每一步都有独立通过记录，确定性 E2E 通过后才执行真实 GLM 冒烟和 PoC → 通过分阶段集成记录验证。
- [ ] 检查普通任务与 PR → 一任务、一模块、一分支、一 PR，修改范围、验收命令、失败场景和相邻模块审查完整；合同变更独立提交 → 通过任务与 PR 模板检查验证。
- [ ] 运行 `scripts/test.ps1` 和 PR 检查 → 后端、前端、合同、依赖边界和确定性 E2E 使用同一门槛通过，普通检查没有调用真实 GLM → 通过本机与 CI 日志验证。

### M. 模块接口、事务与装配

- [ ] 静态检查 `backend/app/contracts/` → 只有三份规定合同，只依赖标准库、`typing` 和 Pydantic，严格模型拒绝额外字段与宽松类型，目录没有业务实现 → 通过导入边界和参数化合同测试验证。
- [ ] 分别对 M1 五个端口的 Fake 与 SQLite 实现运行同一测试 → 正常、拒绝、幂等、归属和失败行为一致，返回值中没有 ORM、Session 或绝对路径 → 通过参数化合同测试和对象类型检查验证。
- [ ] 让 M3 使用 M2 的测试引擎分析一个案例 → 按规定顺序收到阶段和模型事件，最终结果包含三份通过校验的合同，M2 没有数据库依赖 → 通过事件序列测试和导入检查验证。
- [ ] 输入一个证据不足案例 → M2 返回字段完整且明确未知的成功结果，M3 将案例推进到待人工确认而不是处理异常 → 通过模块集成测试验证。
- [ ] 分别制造导入拒绝、幂等命中、过期运行、磁盘异常和未处理 SDK 异常 → 前三类返回类型化结果，后两类保留技术链并映射为稳定业务错误，页面没有原始异常 → 通过故障注入、日志和 API 合同测试验证。
- [ ] 在 GLM 调用阻塞期间检查 SQLite → 没有开放事务；阶段事件或最终发布写入失败时，案例进入可定位异常且不留下半成品 → 通过并发与事务故障注入测试验证。
- [ ] 运行 `scripts/dev.ps1` → FastAPI 与 Vite 均可访问并支持热更新；运行 `scripts/start.ps1` → 单一地址同时提供页面与 API，且不依赖 Vite 或 Docker → 通过脚本集成和浏览器冒烟验证。
- [ ] 尝试在演示模式注入 Fake 或缺少密钥时触发假模型 → 启动或构建检查拒绝 Fake；缺少密钥只禁用分析并保留历史读取 → 通过装配测试和浏览器 E2E 验证。

### N. HTTP API、DTO 和人工确认合同

- [ ] 检查 OpenAPI → 只存在十个规定业务与健康接口，每个成功状态、请求类型和错误状态完整；不存在运行、阶段、模型、配置、日志、删除和通用 CRUD 接口 → 通过 OpenAPI 快照验证。
- [ ] 新上传、重复上传、开始分析、重跑、新审核和重复审核 → 分别得到规定的 201、200、202、202、201、200，正文始终是对应业务 DTO 且没有运行编号 → 通过 API 集成测试验证。
- [ ] 直接刷新和轮询运行中的批次与案例 → 只使用 `batch_id` 与 `case_id` 恢复；开始和重跑响应、OpenAPI 与前端类型中均没有 `batch_run_id` 或 `case_run_id` → 通过合同扫描和浏览器 E2E 验证。
- [ ] 案例进入待人工确认后读取两次详情 → 当前结果未变时 `review_token` 可用于提交；重新分析成功后 Token 改变，旧 Token 返回 `409 STALE_CASE_RESULT` 且不落库 → 通过并发 API 与数据库读回验证。
- [ ] 分别触发 400、404、409、413、415、422、503 和 500 → 全部返回相同 `BusinessError` 结构、稳定编号和可执行下一步，没有 FastAPI `detail`、HTML、SDK 状态或堆栈 → 通过参数化错误合同测试验证。
- [ ] 对批次和案例列表测试默认、最小、最大、越界 `limit` 与 `offset`，并组合两个允许筛选 → 总数、分页、固定排序和 URL 恢复正确，任意排序和搜索参数不被接受 → 通过 API 参数化测试与 E2E 验证。
- [ ] 扫描五类业务 DTO 与 `HealthView` → 字段只属于确认清单，可选内容不适用时省略，ORM 新增字段和技术信息不会自动进入响应 → 通过 Pydantic 白名单与 OpenAPI 回归测试验证。
- [ ] 分别提交四种人工确认请求及其缺字段、禁用字段、目录外动作、超过三项动作和重复提交变体 → 合法请求保存，非法请求统一拒绝，幂等与当前结果保护同时生效 → 通过区分联合类型和数据库集成测试验证。

### O. 状态机、当前投影和中断恢复

- [ ] 检查原因枚举 → 只有六个真实业务原因和一个 `INSUFFICIENT_EVIDENCE` 非原因兜底值，页面不把兜底值称为第七种原因 → 通过枚举、文案和合同测试验证。
- [ ] 参数化执行案例所有允许转换 → 只产生 R69 规定的新状态；执行任何其他转换 → 返回稳定错误且数据库不改变 → 通过状态机单元测试和数据库读回验证。
- [ ] 运行证据不足案例与模型超时案例 → 前者进入待人工确认并具有完整结果，后者进入处理异常且没有业务半成品 → 通过模块集成和浏览器 E2E 验证。
- [ ] 分别构造待开始、案例分析中、全部当前成功和存在当前异常的批次 → 业务 API 只返回四种规定批次状态，与当前案例一致且不暴露内部运行状态 → 通过投影测试和 API 合同测试验证。
- [ ] 对处理异常或待人工确认案例执行重跑 → 重跑期间批次为分析中，成功后当前成功数增加或保持、当前异常数下降；首次 `batch_run` 计数始终不变 → 通过事务集成和数据库读回验证。
- [ ] 在感知、归因和策略阶段分别强制结束进程后重启 → HTTP 开放前活动运行均变为中断，未完成案例得到 `APP_INTERRUPTED`，已有技术记录保留且模型调用数不增加 → 通过进程级故障注入验证。
- [ ] 连续执行两次启动恢复检查 → 第二次不重复改变结束时间、状态、计数或记录数量 → 通过幂等恢复测试验证。
- [ ] 关闭浏览器但保持 FastAPI 运行 → 后台案例继续；正常关闭 FastAPI → 不接受新任务、未开始任务取消、当前请求后不再进入下一阶段 → 通过线程池、HTTP 和事件序列测试验证。
- [ ] 并发开始、重复重跑和单案例失败 → 始终只有一个应用级线程池、一个活动批次和每案例一个活动运行，其他案例不被取消 → 通过执行器探针、唯一约束和故障注入验证。

### P. 本机运行、启动与停止保护

- [ ] 从项目根目录之外调用 `scripts/start.ps1` → 仍只使用项目根目录下的默认 `runtime_data/`，不会在命令行当前目录生成第二个数据库 → 通过脚本集成测试和路径读回验证。
- [ ] 在 `config/local.toml` 中设置合法本机绝对运行目录 → 数据库、批次、日志和锁都位于该根目录；设置 UNC、临时或不可写目录 → 以 `RUNTIME_DIR_INVALID` 停止且不回退 → 通过参数化启动测试验证。
- [ ] 使用默认配置启动演示和开发模式 → 分别使用 `127.0.0.1:8000` 以及 FastAPI 8000、Vite 5173；配置 `0.0.0.0` 或相同端口 → 启动校验拒绝 → 通过端口探针与配置测试验证。
- [ ] 占用默认 8000 后启动 → 以 `PORT_IN_USE` 停止且不随机换号；显式配置可用端口后 → 页面、API 和开发代理统一使用新端口 → 通过进程级启动测试验证。
- [ ] 对同一运行目录并发启动两个实例 → 第二个实例在迁移和 GLM 调用前以 `INSTANCE_ALREADY_RUNNING` 停止 → 通过双进程测试、数据库 revision 和模型调用记录验证。
- [ ] 强制结束持锁进程并保留 `psit.lock` 文件后重新启动 → 系统锁能够重新取得，遗留文件不会造成永久阻塞 → 通过进程级故障注入验证。
- [ ] 每次运行 `scripts/start.ps1` → 都按当前锁文件构建前端；构建失败时 HTTP 不开放，旧 `dist/` 不作为当前页面继续提供，依赖文件和锁文件不改变 → 通过脚本故障注入验证。
- [ ] 应用和数据库就绪后 → 启动窗口显示实际访问地址但不自动打开浏览器；不提供 `-NoBrowser` → 通过启动输出和进程探针验证。
- [ ] 不提供 `ZAI_API_KEY` 但数据库正常 → 手工打开工作台后可读历史，开始和重跑保持不可用，不出现 Fake 结果 → 通过启动脚本和浏览器 E2E 验证。
- [ ] 在上传写入中强制终止进程后重新启动 → 未发布临时内容不成为批次、不提供证据、不调用 GLM，陌生或越界对象不被自动删除 → 通过进程故障、数据库读回和文件边界测试验证。
- [ ] 在 GLM 请求期间按 Ctrl+C → 应用拒绝新任务，等待当前请求并阻止下一阶段，最终释放锁；强制结束后重启 → 启动恢复收敛未完成运行 → 通过事件序列与双进程测试验证。
- [ ] 扫描启动窗口和技术日志 → 只含允许的诊断字段，不含 API Key、客户正文、Prompt、模型原始响应或密封答案 → 通过敏感信息扫描验证。

### F. 模态贡献与声明边界

- [ ] 对两个图片对比案例运行有图与无图条件 → 图片至少改变规定判断项中的一项，并保留差异证据 → 通过对比结果验证。
- [ ] 对一个行为对比案例运行有行为与无行为条件 → 行为至少改变规定判断项中的一项，并保留差异证据 → 通过对比结果验证。
- [ ] 查看产品说明、演示文案、输出报告 → 不出现真实留存提升、真实 ROI、生产级准确率、已验证因果效果、企业可部署的无证据声明 → 通过静态文本扫描和人工审阅验证。

## Future Considerations

### 1. 真实业务验证

- 对真实售后运营人员开展访谈。
- 在真实售后场景开展小范围试点。
- 记录真实人工决策时间、决策一致性、证据核验成本。
- 在业务规则明确后评估产品是否值得继续投入。

### 2. 真实效果验证

- 接入真实客户、真实处理动作、真实动作成本、真实后续结果。
- 固定策略版本和观察窗口。
- 设计随机分组或可靠对照。
- 分别评估增量留存、成本、收入、风险。

### 3. 产品扩展

- 对接正式售后系统或 CRM。
- 建设企业级身份、权限、审计。
- 增加实时数据接入。
- 增加更完整的搜索、筛选、协作。
- 在业务授权后接入动作执行，但继续保留人工门禁。

### 4. 模态与模型扩展

- 真实数据和交付周期允许时评估音频。
- 真实数据和交付周期允许时评估视频。
- 用真实样本比较不同图片识别路线。
- 用真实样本比较不同模型和推理方式。
- 只有基线结果证明必要时，才评估训练型融合。

## Implementation Decisions

### Behavior Layer

以下行为已经冻结，技术实现不得改变其含义：

- 明确售后事件触发案例，不做全量主动异常扫描。
- 使用标准案例批次进入产品，不接管完整原始数据治理。
- 文本、图片、行为三类证据进入同一个联合判断。
- 先批量预分析，再进入案例队列，再进入案例详情。
- 系统给建议，运营人员承担最终业务判断。
- 系统原结果和人工最终结果都要持久保存。
- 单个案例失败不能阻断同批其他案例。
- 证据不足是有效业务结果，处理异常是系统运行失败。
- 普通无图片案例允许降级，多模态验收案例不允许缺少核心模态。

### Module + Interface Layer

当前没有代码仓库。以下是需求层模块边界，不代表已选择技术框架、进程数量、Agent 数量：

| 需求模块 | 单一职责 | 输入 | 输出 | 不承担 |
|---|---|---|---|---|
| 案例接入 | 校验并接收标准案例批次 | 上游处理结果、来源说明 | 合格标准案例包、明确校验错误 | 原始数据完整治理 |
| 多模态分析 | 提取三类业务特征并对齐证据 | 标准案例包 | 支持、冲突、缺失状态及证据引用 | 研究级融合训练 |
| 事件与情绪还原 | 组织事实、推断、缺口和按需出现的不确定性 | 对齐后的多模态证据 | 事件还原、情感感知结果 | 无证据补写 |
| 决策包生成 | 生成三项受约束建议 | 事件与情绪还原、客户价值依据、策略范围 | 介入等级、风险原因假设、处理方案 | 自动业务执行 |
| 批次调度 | 管理案例独立运行和状态 | 合格案例批次 | 各案例运行结果、失败记录 | 让单例失败中断批次 |
| 案例队列 | 支持“先处理谁” | 预分析结果、最终人工介入等级 | 排序队列、筛选、状态 | 完整 CRM |
| 案例详情 | 支持“怎么处理” | 案例结果和证据 | 结论优先页面、可展开依据 | 静态报告替代交互 |
| 人工确认 | 保存最终业务判断 | 系统原决策包、人工输入 | 审核结果、最终人工结果、原因、时间 | 无痕覆盖系统结果 |
| 记录存储 | 支持刷新和重新打开 | 案例快照、系统结果、人工结果 | 可读回记录 | 企业级审计系统 |

具体字段、数据类型、枚举、接口、模型、存储、调用方式写入技术文档。触发条件未满足前，不允许用经验值或 AI 猜测补齐。

### Locator Layer

> Reference only — execution agent must explore independently.

当前没有代码路径和测试路径。需求依据位于：

- 项目建设/00-当前项目唯一基线.md
- 项目建设/02-需求澄清-待决定事项.md
- 项目建设/CONTEXT.md
- 项目建设/docs/adr/0001-retain-image-as-required-mvp-modality.md
- 项目建设/docs/adr/0002-use-decision-level-multimodal-evidence-integration.md
- 项目建设/docs/adr/0003-separate-raw-data-processing-from-case-runtime.md
- 项目建设/docs/adr/0004-use-evidence-grounded-event-and-emotion-reconstruction.md
- 项目建设/docs/adr/0005-require-human-final-confirmation.md
- 项目建设/docs/adr/0006-use-case-queue-before-case-resolution.md
- 项目建设/docs/adr/0007-position-current-delivery-as-core-flow-validation-mvp.md

## Decisions

### D1. 保留图片作为 MVP 必做模态

- **Context**：老师最新要求保留文本、图片、行为，删除音频、视频；普通真实案例并非每例都有图片。
- **Decision**：图片是 MVP 必做能力；普通案例允许无图片降级；多模态验收案例必须包含并实际使用图片。
- **Alternatives rejected**：把图片降为可选附件；删除图片做纯文本产品；为补齐图片伪造数据关系。前两项无法验证多模态能力，后一项破坏证据真实性。

### D2. 采用决策级多模态证据联合判断

- **Context**：三类模态分别展示不能支持一个统一业务决定；10 天不足以建设研究级融合模型。
- **Decision**：分别提取特征，按客户、案例、事件、时间对齐，识别支持、冲突、缺失，让联合证据进入事件还原和决策包。
- **Alternatives rejected**：三类结果互不关联地并排展示；固定要求 Feature Fusion；固定要求 CLIP；固定要求 RAG；固定要求训练型融合。第一项不形成联合决策，后四项提前绑定技术并扩大范围。

### D3. 分离上游数据工作与产品运行

- **Context**：队友负责数据清洗和数据挖掘；产品周期只有 10 天。
- **Decision**：上游负责原始数据处理，产品只做接入校验、来源保留、少量适配、案例组装。
- **Alternatives rejected**：产品重复完整数据治理；接受任意原始文件自动治理。两项都会扩大交付范围并模糊团队职责。

### D4. 采用有证据的事件与情绪还原

- **Context**：单一情绪标签和分数不能支持运营判断，还会掩盖冲突和缺口。
- **Decision**：输出有证据的事件、当前可能情绪、简短解释和不确定性；v1 不强制情绪变化、目标情绪和备选解释数组。
- **Alternatives rejected**：只给情感倾向；只给情绪强度；为了结果完整补写故事。前两项信息不足，后一项违反证据边界。

### D5. 保留产品侧人工最终确认

- **Context**：当前系统只做决策辅助，证据和模型判断存在不确定性。
- **Decision**：运营人员通过、结构化修改或驳回；修改和驳回必须记录原因；系统原结果与人工结果同时保存，不开放任意 JSON 编辑。
- **Alternatives rejected**：系统自动执行业务动作；人工结果无痕覆盖系统结果；开放任意结果编辑；把开发侧代码验收当成业务确认。分别存在越权、破坏追溯、生成非法结果和混淆流程的问题。

### D6. 先通过案例队列决定处理谁

- **Context**：介入等级只有转化为实际处理顺序才对运营有用。
- **Decision**：采用批次导入与摘要、显式开始批量分析、案例队列、案例详情流程；导入本身不调用模型。
- **Alternatives rejected**：只做单案例分析页；逐个打开案例后才分析；扩展成完整 CRM。前两项无法支持优先队列，后一项超过 MVP。

### D7. 将本期定位为核心链路验证 MVP

- **Context**：没有真实企业访谈、业务试点、真实干预结果、生产系统。
- **Decision**：只验证标准案例进入、三类模态联合判断、队列、详情、人工确认、记录读回。
- **Alternatives rejected**：声称真实需求已验证；声称生产级准确率；声称留存提升；声称 ROI；声称企业可部署。全部缺少证据。

### D8. 明确事件触发，不做全量主动扫描

- **Context**：全量异常检测是另一类更大的产品。
- **Decision**：投诉、差评、退款、物流异常、商品问题触发案例。
- **Alternatives rejected**：持续扫描全部客户；把任意行为波动自动建成售后案例。两项扩大范围并增加误报。

### D9. 三项决策组成不可拆分的决策包

- **Context**：运营需要同时知道要不要处理、为什么、怎么处理。
- **Decision**：介入等级、风险原因假设、受约束处理方案同等重要；证据不足时字段也不能消失。
- **Alternatives rejected**：只给一个风险分数；输出三段互不关联文本；证据不足时省略字段。三项都不能形成完整处置依据。

### D10. 介入等级采用三档

- **Context**：当前没有真实企业 SLA，抽象风险分数不直接对应动作。
- **Decision**：必须介入、建议介入、暂不介入，并说明理由。
- **Alternatives rejected**：二元介入判断；高中低风险分数；自动生成处理时限。第一项粒度不足，第二项动作含义不清，第三项缺少业务规则。

### D11. 风险原因采用可反驳假设

- **Context**：当前证据只能支持归因假设，不能证明因果。
- **Decision**：一个主要原因必须有解释和支持证据；反证、不确定性和最多一个备选原因只在实际存在时输出。
- **Alternatives rejected**：输出一个确定原因；罗列全部原因不排序。前者过度自信，后者不能支持决策。

### D12. 处理方案受策略范围约束

- **Context**：模型自由生成动作会越过授权、预算、权益规则。
- **Decision**：只能从版本化动作目录中的六类批准动作选择最多三项，每项说明具体动作、原因、支持证据和按需出现的前置条件；人工批准由产品统一门禁，不由模型重复声明。
- **Alternatives rejected**：AI 自由生成任何方案；自动执行建议；只给原因不提供方案。前两项有业务风险，后一项无法完成决策支持。

### D13. 使用基于当前数据分布的相对 RFM 规则

- **Context**：Olist 的 F、M 长尾，旧绝对阈值没有数据依据。
- **Decision**：使用 `high_value_rule_v1`：`R <= P75(R) AND (M >= P80(M) OR (F >= 2 AND M >= P50(M)))`，实际分界随数据版本保存；不把该规则表述为真实企业规则。
- **Alternatives rejected**：按复购 10 次、2 次直接划档；使用未验证金额阈值；混用 recency_days 和 r_score。三项会产生不可解释或方向错误的标签。

### D14. 三套数据不做真实客户级强行拼接

- **Context**：Olist、E Commerce Dataset、JDDC 来源不同，没有共同客户 ID。
- **Decision**：三套数据分别支撑业务主线、流失辅助分析、客服文本辅助分析；模拟关联必须使用新 case_id 并明确标记。
- **Alternatives rejected**：按 Customer ID 强行连接；复制、扩增、重新编号后冒充真实客户。两项都会制造虚假关系。

### D15. 业务职责不等于固定 Agent 数量

- **Context**：感知、归因、策略职责已经明确，Agent Teams 与固定 Workflow 仍属于技术路线选择。
- **Decision**：PRD 只冻结业务行为，不冻结 Agent 数量、运行载体、进程边界。
- **Alternatives rejected**：把三个职责直接写成三个独立 Agent；把 Claude Code Agent Teams 写成唯一运行时；把固定 Workflow 写成唯一运行时。三项都越过技术验证。

### D16. 不做复杂训练

- **Context**：10 天周期和团队能力不支持模型微调、复杂训练、自研多模态融合。
- **Decision**：需求只规定业务结果和验收，不要求训练。
- **Alternatives rejected**：RAG、知识图谱、CLIP、SFT、RL、Cross-Attention、Transformer 融合网络、深度聚类全部作为本期必做。每项都缺少必要性验证，并扩大交付风险。

### D17. 不用静态页面或预设答案冒充完成

- **Context**：项目要求可运行、可演示、可人工检查。
- **Decision**：新案例必须经过真实导入、分析、排序、详情、人工确认、持久读回。
- **Alternatives rejected**：架构图；纯静态页面；固定页面结果；不能运行的 Agent 壳。四项只能展示概念，不能验证流程。

### D18. 导入后显式开始分析

- **Context**：模型调用产生费用，错误批次、重复导入和程序重启不能自动触发调用。
- **Decision**：导入后进入待分析状态并展示批次摘要；只有一次明确的“开始批量分析”操作才创建运行。
- **Alternatives rejected**：导入即自动分析；逐例勾选分析。前者缺少费用门禁，后者扩大批次状态和页面复杂度。

### D19. 程序执行介入等级最低要求

- **Context**：模型可能把未解决严重问题错误判断为暂不介入。
- **Decision**：Strategy Agent 提出等级，程序执行最低介入等级；发生提高时保留原始响应并记录调整原因，不再次调用模型。
- **Alternatives rejected**：完全由模型决定；完全由规则决定；违反底线后再次调用模型。分别会放行明显错误、削弱语义判断和增加费用与漂移。

### D20. 已完成案例保持只读

- **Context**：已完成人工确认的案例代表最终业务记录，重新分析会产生当前结果归属歧义。
- **Decision**：处理异常和待人工确认案例可以重新分析；已完成案例在业务页面只读。人工修改使用批准枚举和必填原因。
- **Alternatives rejected**：所有案例随时重跑；完全禁止重跑。前者破坏业务结果稳定性，后者无法恢复技术失败。

### D21. 使用进程内后台执行和定时查询

- **Context**：批次分析时间超过普通页面请求，但当前单机、最多 20 个案例，不需要分布式任务系统。
- **Decision**：FastAPI 接受启动请求后返回当前批次状态与统计摘要，不向业务前端暴露运行标识；`RunService` 在同一进程后台执行，页面活动期间每 2 秒查询状态，案例完成后立即开放结果。
- **Alternatives rejected**：请求同步等待整批；Redis/Celery；WebSocket/SSE。分别存在页面超时、基础设施过重和连接恢复复杂度。

### D22. 交付本机单进程网页应用

- **Context**：本期使用 Mock 数据和单一运营角色，没有公网部署、账号体系和多用户要求。
- **Decision**：React 构建结果由 FastAPI 提供，一个进程、一个 SQLite、一个启动命令，默认监听 `127.0.0.1`。
- **Alternatives rejected**：局域网多人部署；公网云部署。两项会新增登录、权限、HTTPS、密钥、运维和备份要求。

### D23. 浏览器 ZIP 统一进入同一导入链路

- **Context**：MVP 需要演示固定 Mock 数据，但固定演示包不能绕过真实导入校验，否则无法证明新案例能进入系统。
- **Decision**：用户通过浏览器上传标准 ZIP；上传内容先进入批次专用临时目录，校验通过后再原子移入正式目录。固定演示包和验收包使用完全相同的入口。
- **Alternatives rejected**：页面填写服务器文件路径；固定案例直接写入数据库；为演示包保留隐藏快捷入口。三项分别依赖开发机路径、绕过输入合同或制造两套不可比较的运行链路。

### D24. 单活动批次并按批次隔离队列

- **Context**：当前是单进程、SQLite、共享模型 API 额度，跨批次并发会扩大状态、费用和恢复复杂度。
- **Decision**：系统同一时间只运行一个活动批次；第二个批次保持待分析且不自动排队。队列一次显示一个批次，默认最近导入批次，可切换历史批次，不混合不同数据版本。
- **Alternatives rejected**：多个批次同时运行；按导入顺序自动排队；把所有批次案例混在一个队列。三项分别增加调度与成本风险、绕过显式付费门禁或破坏批次和数据版本边界。

### D25. 分离业务状态与技术诊断信息

- **Context**：运营人员需要知道哪里失败、怎么处理，但不需要理解 Agent 阶段、模型参数和程序异常；原始异常还可能泄露密钥与本机路径。
- **Decision**：业务页面只显示五种业务状态和可处理错误；错误包含中文摘要、失败对象、失败环节、稳定错误编号和下一步。内部阶段与原始异常只保存在技术记录中。
- **Alternatives rejected**：只显示“运行失败”；直接显示供应商响应或程序堆栈；在业务页面展示完整 Agent 运行过程。第一项无法处理问题，后两项增加误解和信息泄露风险。

### D26. 使用三层页面路由和最小 REST API v1

- **Context**：工作台需要支持刷新、返回和直接打开案例，同时避免把 MVP 扩展为后台管理系统。
- **Decision**：使用工作台入口、批次队列、案例详情三层页面地址；FastAPI 提供以 `/api/v1` 开头的十个明确接口，React 不保存权威业务状态。
- **Alternatives rejected**：全部功能塞入一个无地址状态的页面；为导入、模型、日志各建管理模块；使用一个通用接口处理全部动作。三项分别破坏刷新恢复、扩大范围或模糊前后端合同。

### D27. 成功响应直接返回资源，失败响应使用稳定错误合同

- **Context**：前端和自动化测试需要稳定判断真实结果，运营人员又需要可理解、可处理的错误。
- **Decision**：单项返回对象、列表返回 `items + total`、写操作返回保存后的记录；错误统一包含稳定编号、中文说明、失败对象、业务环节、下一步和 `trace_id`。
- **Alternatives rejected**：所有响应嵌套多层通用外壳；只返回 `success`；每个接口自定义错误结构。三项分别增加无效样板、丢失结果或造成前端分支失控。

### D28. 所有可能重复付费或重复落库的操作必须幂等

- **Context**：浏览器重复点击、网络超时重试和用户刷新都可能重复提交请求。
- **Decision**：上传按 ZIP 校验值与批次身份去重，开始和重跑按当前运行状态去重，人工确认按唯一 `submission_id` 去重；相同请求返回原结果，冲突请求明确拒绝。
- **Alternatives rejected**：只依赖按钮禁用；每次请求都新建记录；使用一个全局幂等编号覆盖所有操作。三项分别无法覆盖网络重试、产生重复费用或让编号职责不清。

### D29. 证据文件通过归属受控接口提供

- **Context**：案例详情必须直接查看图片，但公开磁盘路径会产生路径越界、错误文件访问和本机信息泄露。
- **Decision**：页面以批次、案例、证据标识请求图片；后端核验证据归属、正式目录边界和真实媒体类型。JPG、JPEG、PNG、GIF 按现有数据支持，GIF 的模型输入继续使用静态首帧。
- **Alternatives rejected**：直接返回本机路径；开放整个上传目录；只凭文件扩展名响应。三项无法保证证据归属或文件安全。

### D30. 将批次运行和案例运行拆成两张表

- **Context**：一次批次启动包含多个案例运行，单案例又可以独立重跑，两者具有不同状态、计数和生命周期。
- **Decision**：把原 `runs` 拆为 `batch_runs` 和 `case_runs`，数据库调整为九张核心表；人工确认明确关联被审核的 `case_run`。
- **Alternatives rejected**：用一张表加 `scope_type` 和大量可空字段；把案例运行埋进批次 JSON。两项都会让外键、进度统计、重跑和并发约束变得模糊。

### D31. 查询字段成列，版本化完整结果存 JSON

- **Context**：队列和运行状态需要稳定查询，而 Agent 结构化结果具有嵌套结构和版本。
- **Decision**：标识、状态、等级、计数、时间、校验值、相对路径和当前指针使用独立列；输入快照、Agent 结果、人工修改、模型响应和错误详情整体存 JSON。
- **Alternatives rejected**：所有内容放一个 JSON；把全部嵌套结果关系化成大量小表。前者破坏查询和约束，后者扩大迁移与开发成本。

### D32. 允许更新当前状态，但历史结果不得覆盖

- **Context**：运行中的状态和进度必须变化，“追加历史”不能被误解为所有行都永远不可更新。
- **Decision**：运行状态、计数、时间和当前指针允许更新；已形成的阶段结果、模型调用、结束后的案例运行、人工确认和失败详情只追加。切换当前运行使用单一事务。
- **Alternatives rejected**：所有字段都不可更新；重跑时覆盖原记录。前者无法表达进度，后者破坏追溯。

### D33. 使用同步 SQLAlchemy 和受约束的 SQLite 短事务

- **Context**：当前单进程、案例并发为 2，GLM SDK 和数据库规模不要求异步数据库层。
- **Decision**：每个请求和后台任务使用独立同步 Session；GLM 调用期间不持有事务；启用外键、WAL、5 秒忙等待和活动运行唯一约束。
- **Alternatives rejected**：共享全局 Session；调用模型时保持事务；引入 `aiosqlite` 或独立数据库服务。前两项会造成并发错误和锁等待，后两项当前没有收益证据。

### D34. 分离源码、测试夹具和本机运行数据

- **Context**：团队需要共享代码与固定案例，但不能提交运行数据库，也不能让运行 Agent 接触密封验收答案。
- **Decision**：源码按后端、前端、Schema、策略、迁移、脚本和测试组织；本机数据库与批次文件默认放在项目根目录下不入 Git 的 `runtime_data/`。迁移失败即停止；重要演示前由人工在停机后复制完整运行目录。
- **Alternatives rejected**：把数据库和上传文件提交 Git；把验收答案放入运行包；迁移失败后自动重建数据库。三项分别制造冲突、答案泄漏或数据丢失。

### D35. 使用全局唯一的公开批次标识

- **Context**：批次 API 地址只包含 `batch_id`，如果同一标识可以在多个数据版本重复，地址将无法唯一定位批次。
- **Decision**：`batch_id` 全局唯一；数据变化需要生成新的批次标识。内部关系使用整数主键，API 使用稳定文本标识。
- **Alternatives rejected**：继续使用 `batch_id + data_version` 联合公开身份；在 API 地址追加数据库整数 ID。前者让所有地址和前端状态都必须携带两个值，后者泄露内部实现并降低稳定性。

### D36. 人工单案例重跑不属于原批次运行

- **Context**：重跑可能发生在批次结束后，把它挂入原批次会改变已结束批次的历史计数。
- **Decision**：首次案例运行关联 `batch_run`；人工重跑的 `batch_run_id` 为空，以 `previous_case_run_id` 连接上一个版本，并使用 `MANUAL_RERUN` 标记触发来源。
- **Alternatives rejected**：把重跑计入原批次；为单案例重跑创建假的批次运行。两项都会污染批次含义或制造无业务对应的记录。

### D37. 冻结九表逻辑字段、枚举和时间含义

- **Context**：多个队友和 Coding Agent 将并行建设数据库、API、运行服务和测试，如果字段含义不固定，各任务会自行猜测。
- **Decision**：冻结 R26 所列逻辑字段、唯一关系、状态枚举、批次计数和 UTC 时间规则；字符串长度、索引名称和 Python 类名留给实现阶段。
- **Alternatives rejected**：只给九个表名；提前锁死全部物理数据库细节。前者不足以并行开发，后者会妨碍合理实现调整。

### D38. 业务页面只使用结果投影

- **Context**：运营人员关心客户价值、风险原因和处理动作，不需要理解系统采用的技术方法。
- **Decision**：内部数据库和开发测试保留完整运行信息；业务 API 与页面只呈现必要操作状态、可处理错误，以及高价值客户结论、流失风险原因、处理建议和按需展开的证据。
- **Alternatives rejected**：直接把数据库对象返回页面；展示 RFM、Agent 阶段和模型日志；完全隐藏支撑结论的业务证据。前两项把技术实现当成产品内容，后一项让结果无法人工核验。

### D39. 使用业务 DTO 白名单隔离数据库

- **Context**：数据库必须保存完整技术记录，但页面只需要有限业务结果；直接序列化 ORM 对象会让新增数据库字段意外进入业务响应。
- **Decision**：为批次、队列项、详情、人工确认和业务错误建立五类独立 Pydantic 响应 DTO，所有业务接口显式声明 `response_model`；不提供阶段结果和模型调用查询接口。
- **Alternatives rejected**：直接返回 ORM；先全部返回再由前端隐藏；增加公开开发模式。三项都无法从服务端保证技术信息不泄露。

### D40. 页面使用人工优先的当前有效业务结果

- **Context**：人工确认可能修改系统建议，继续在队列展示旧建议会让业务结果前后矛盾。
- **Decision**：未确认时使用系统建议，确认后使用人工最终结果；发生修改时只在折叠区保留系统原业务建议和修改原因。证据区只投影当前结果引用的业务事实。
- **Alternatives rejected**：始终展示系统建议；系统和人工结果默认并排；默认展示全部证据与来源结构。三项分别忽略最终判断、造成重复或把核验界面变成技术数据页。

### D41. 完整决策包通过后才发布业务结果

- **Context**：三个模型阶段顺序执行，前两阶段完成不代表运营人员已经获得可执行建议。
- **Decision**：三个阶段输出全部通过 Schema 和程序规则后，才原子发布完整业务结果并进入待人工确认；分析中或后续阶段失败时不通过业务 DTO 返回半成品。
- **Alternatives rejected**：逐阶段流式展示；失败时保留可见的部分原因；前端自行拼接阶段结果。三项都会让用户看到无法采取行动或前后不一致的结果。

### D42. 三个 Agent 使用不同的最小输入视图

- **Context**：三个阶段全部读取完整案例会重复传图、增加费用，并让客户价值或动作约束反向影响事实和原因判断。
- **Decision**：感知读取原始对话、图片和核验事实；归因读取感知结果及被引用事实，不重复传原图；策略读取两个上游结果、高价值结论和动作约束。高价值只在策略阶段进入模型。
- **Alternatives rejected**：三个阶段都读取完整 `CaseInput`；所有阶段都读取 RFM；归因和策略重复发送图片。三项会扩大数据暴露、偏置和调用成本。

### D43. 使用一个薄 GlmClient 统一接入智谱 SDK

- **Context**：三个 Agent 分别实现鉴权、请求、超时和重试会产生不一致行为和重复代码。
- **Decision**：只有 `GlmClient` 接触官方 SDK，Agent 管 Prompt，`RunService` 管流程和校验；客户端不承载业务判断或数据库状态更新。
- **Alternatives rejected**：每个 Agent 自带客户端；把全部业务流程放进 SDK 封装；引入第三方 Agent 框架。三项分别造成漂移、职责混乱或扩大依赖。

### D44. Prompt 文件版本与 Pydantic Schema 同源

- **Context**：手工同时维护 Prompt 字段清单和 Pydantic 模型容易产生结构漂移。
- **Decision**：三个 Prompt 独立版本化保存，Pydantic 是字段结构唯一来源，运行时生成 JSON Schema；每次调用记录 Prompt 版本和内容哈希。
- **Alternatives rejected**：Prompt 硬编码在 Agent 类；手写第二份 JSON Schema；允许页面在线改 Prompt。三项都会削弱版本追溯和合同稳定性。

### D45. 严格校验并限制一次定向修复

- **Context**：JSON 模式只能保证输出趋向 JSON，不能保证字段、证据和业务规则正确。
- **Decision**：按照解析、严格 Schema、身份、证据、跨阶段引用和程序规则顺序校验；收到不合格结果后最多定向修复一次，合格但不确定的业务判断交给人工。
- **Alternatives rejected**：只校验 JSON；静默忽略多余字段；为了更满意的内容反复采样。三项分别放行坏结果、隐藏合同漂移或造成成本与结果不可控。

### D46. 每阶段最多四次请求，正式参数由 PoC 冻结

- **Context**：网络重试和结构修复如果各自无限执行，会让调用次数和演示时间失控；未经当前数据验证的参数也不能宣称最佳。
- **Decision**：每阶段最多一次首调、两次可重试网络故障和一次结构修复；初始参数只做 PoC 基线，用 15 个固定案例比较推理强度和并发后冻结正式配置。
- **Alternatives rejected**：SDK 与业务层双重重试；修复失败继续递归；直接把初始参数作为生产结论。三项会造成重复调用、不可控费用或无证据配置。

### D47. 用固定案例硬门槛决定模型路线是否可用

- **Context**：只证明接口能返回内容，无法证明三阶段结果结构稳定、证据受控或人工能实际使用。
- **Decision**：以 45 个阶段全部真实运行、所有发布结果严格有效、失败完整保留、5 个密封案例双人质量判断和三项模态贡献作为 PoC 通过条件；耗时与用量先记录，再据实冻结参数和内部预警线。
- **Alternatives rejected**：只看一两个成功案例；只看 JSON 通过率；用平均分掩盖关键失败。三项都会把“偶尔能跑”误判为“可用级 MVP”。

### D48. 使用受校验的 TOML 配置和环境变量密钥

- **Context**：模型参数散落在代码中会让复现实验和调整失败；密钥写进文件又会形成泄漏风险。
- **Decision**：非敏感参数集中在 `config/default.toml` 并由 `pydantic-settings` 严格校验，密钥只从 `ZAI_API_KEY` 读取；缺少密钥时保留只读工作台，禁用分析操作。
- **Alternatives rejected**：参数硬编码在 Agent；把 API Key 写入 TOML 或 Prompt；缺少密钥时让整个工作台崩溃。三项分别破坏可复现性、安全性或历史结果可用性。

### D49. 锁定 Python、Node 和项目级执行入口

- **Context**：当前机器的全局 Python 是不可用的 WindowsApps 别名，而工具缓存路径也不稳定；团队并行开发不能依赖某个成员机器的偶然环境。
- **Decision**：统一 Python 3.12、uv 锁文件、Node.js 24、pnpm 11 和前后端锁文件，并提供四个 PowerShell 脚本作为初始化、开发、测试和演示入口。
- **Alternatives rejected**：只写口头安装步骤；依赖全局 Python 别名；硬编码 Codex 缓存路径。三项都会造成成员间无法复现或环境升级后失效。

### D50. 使用脱敏 JSONL 日志贯通一次运行

- **Context**：多阶段异步执行发生错误时，需要跨 API、执行器、Agent、模型调用和数据库定位，但业务页面不能暴露技术细节和客户原文。
- **Decision**：用统一 `trace_id` 写结构化 JSONL 日志，固定必要字段，并禁止密钥、图片、完整对话、完整 Prompt 和原始供应商响应进入日志；轮换方式属于 M3 内部默认值。
- **Alternatives rejected**：只打印自由文本；把所有输入输出写进日志；提供业务日志页。三项分别降低排障能力、扩大敏感数据复制或违反用户展示边界。

### D51. 保存可复算 PoC 产物并在失败时关闭通过出口

- **Context**：如果 PoC 只留下人工口头结论，后续无法知道参数为什么冻结；如果可以删案例或改答案，门槛形同虚设。
- **Decision**：每次 PoC 保存报告、汇总和逐案例机器结果；失败后按固定顺序定位并完整重跑，`high/max` 都失败时明确判定当前模型路线不可用。
- **Alternatives rejected**：只保存成功截图；只重跑失败子集后拼接结果；降低规则直到通过。三项都会让结果不可复算并制造假通过。

### D52. 把 Apple 与 taste-skill 适配为业务工作台规范

- **Context**：用户指定 Apple 设计分析并要求吸收 GitHub `taste-skill`，但两份来源主要面向营销页面；GitHub 主 Skill 还明确排除 dashboard、数据表格和多步骤产品界面。
- **Decision**：保留 Apple 的颜色、排版、间距、圆角和克制交互，采用 taste-skill 的一致性、完整状态、可访问性和发布前检查；固定 `3/2/6` 设计参数，并以独立项目规范约束三个业务页面。
- **Alternatives rejected**：照搬 Apple 官网；运行 `gpt-taste` 的 AIDA、随机版式和 GSAP；直接交付组件库默认皮肤。三项分别牺牲信息效率、操作稳定性或项目辨识度。

### D53. 使用单一 React 前端栈和集中主题

- **Context**：三个页面需要成熟表格、表单、上传与可访问交互，团队并行开发又不能让每个任务自行选择组件和样式体系。
- **Decision**：固定 React、TypeScript、Vite、Ant Design、React Router、TanStack Query、Ant Design Form 和 `@ant-design/icons`；通过 `ConfigProvider`、集中 CSS 变量与 CSS Modules 实现项目视觉合同。
- **Alternatives rejected**：同时引入多套组件或图标；使用 Tailwind、Redux、Zustand、GSAP 或 Motion；直接交付 Ant Design 默认皮肤。当前页面不需要这些复杂度，混用还会扩大状态与视觉漂移。

### D54. 由 FastAPI OpenAPI 生成前端合同

- **Context**：前后端并行开发时，手写两份字段和请求类型会产生无法及时发现的接口漂移。
- **Decision**：以 FastAPI OpenAPI 为唯一接口来源，用 `openapi-typescript` 生成并提交类型，用 `openapi-fetch` 请求；持续集成检查生成结果是否过期，业务错误先由后端稳定映射再集中呈现。
- **Alternatives rejected**：前端手写重复类型；直接返回 ORM；每个页面自行解释原始错误。三项分别造成合同漂移、信息泄露或错误体验不一致。

### D55. URL 保存位置，服务端保存事实

- **Context**：用户需要刷新和返回到相同批次与案例，运行状态也要自动更新，但浏览器副本不能覆盖 SQLite 中的最终结果。
- **Decision**：URL 保存当前批次、案例和筛选；TanStack Query 管可失效的服务端缓存。活动批次每 2 秒轮询，终态停止，后台暂停，回到前台立即刷新；业务事实不写入 `localStorage`。
- **Alternatives rejected**：用本地存储作为事实真源；Redux 或 Zustand 镜像后端结果；组件各自永久轮询。三项会造成旧数据覆盖、双份状态或重复请求。

### D56. 单 ZIP 导入和分析保持两个动作

- **Context**：导入后的数据需要先确认，自动开始分析会产生意外费用；未经实际演示包测量的上传上限可能直接使演示失败。
- **Decision**：每次上传一个 ZIP，导入后显示摘要，再由用户明确开始分析。前端只快检，FastAPI 最终校验；最大合格 ZIP 形成后，以其约两倍并向上取整冻结 `upload.max_zip_bytes`。
- **Alternatives rejected**：上传成功自动分析；支持多个零散文件入口；拍脑袋设置 50 MB 或 100 MB；只做浏览器校验。四项分别失去费用控制、扩大输入面、缺少数据依据或留下绕过路径。

### D57. 产品流程测试和真实 GLM 验证分开

- **Context**：所有自动化都调用真实模型会昂贵且不稳定，只用固定结果又不能证明模型在现有数据上可用。
- **Decision**：Vitest、React Testing Library、MSW 和 Playwright 使用确定性测试模型替身验证真实产品链路；GLM-5.3-Flash 只在独立冒烟和 PoC 中验证，两类报告分别下结论。
- **Alternatives rejected**：每次 UI 回归调用真实模型；用组件 Mock 直接跳到成功页；自动化全绿后宣称模型可用。三项分别导致不稳定、没有流程证据或结论越界。

### D58. 两种桌面尺寸、可访问性和离线资源是硬门槛

- **Context**：本机 MVP 不做手机端，但如果只在开发者大屏和联网环境可用，仍然只是演示实现。
- **Decision**：三个页面在 `1366×768` 和 `1024×768` 覆盖主流程与完整状态；限制主体溢出，满足键盘、44×44px 和 WCAG AA；字体、图标与必要资源本地打包。
- **Alternatives rejected**：只验一张正常桌面截图；把 1024px 溢出留给用户缩放；依赖运行时 CDN；用颜色单独表示状态。四项都会让真实操作存在可预见阻断。

### D59. 使用模块优先的单仓库和单向依赖

- **Context**：四名成员需要独立文件边界；纯横向分层会让每个功能跨越多人目录，四个仓库又会增加合同同步和本机启动成本。
- **Decision**：在一个仓库中建立 M1 数据、M2 分析、M3 应用和 M4 前端四个模块；纯合同独立保存。M4 调用 M3，M3 通过公开接口调用 M1、M2，M1 与 M2 互不调用。
- **Alternatives rejected**：四个独立仓库；按数据库、API、组件和测试横向分工；允许跨模块导入内部代码。三项分别扩大集成成本、文件冲突或隐式耦合。

### D60. 共享合同使用独立变更和共同审查

- **Context**：Pydantic、OpenAPI、迁移、状态和 ZIP 合同同时影响多人，在普通功能 PR 中变化会让其他成员继续依据旧合同开发。
- **Decision**：每份合同有一名主负责人，变更使用独立任务与 PR，并同步差异、生成物和测试，受影响模块共同审查。
- **Alternatives rejected**：建立无人负责的公共业务目录；每个模块复制合同；提供方单方面修改后让调用方联调适配。三项都会制造漂移或责任空白。

### D61. 四名成员分别主责四个产品模块

- **Context**：需要四人真正并行，又不能让每人重复实现一套完整产品或持续修改相同文件。
- **Decision**：M1 主责数据与存储，M2 主责 AI 分析引擎，M3 主责业务服务与 API，M4 主责前端工作台；每块有固定目录、公开接口、禁止职责和相邻模块审查者。
- **Alternatives rejected**：按零散任务随机分人；增加第五个公共业务模块；一人同时主责多个模块。三项分别增加冲突、形成责任黑洞或失去并行能力。

### D62. 先冻结合同并用替身独立开发

- **Context**：等待上游完成会把并行开发变成串行，各自猜接口再在最后补齐则会产生无法组合的实现。
- **Decision**：功能开发前冻结三组接口；M1 使用固定 ZIP 与临时库，M2 使用固定输入与测试模型，M3 使用两个 Fake，M4 使用 MSW；替身和真实适配器运行同一合同测试。
- **Alternatives rejected**：最后联调时才定义接口；复制其他模块的未完成代码；替身直接跳到成功结果。三项分别造成返工、代码分叉或虚假通过。

### D63. 分阶段连接四模块并以 37 张任务卡为基准

- **Context**：一次大合并会把数据、模型、状态和页面错误混在一起；任务太大会迫使执行者重新做架构判断。
- **Decision**：依次连接 M1—M3、M2—M3、M4—M3，再执行确定性 E2E、真实模型冒烟和 PoC；先按 9、10、9、9 共 37 张任务卡规划，最终允许 32—40 张。
- **Alternatives rejected**：四块同时首次接线；机械地每人分配完全相同工作量；用“完成前端或后端”作为一张任务。三项分别难以定位、忽略不确定性或无法单次验收。

### D64. 一任务一 PR，并执行交叉审查和确定性门槛

- **Context**：长分支、跨模块修改和不同验收命令会增加合并冲突；单人垄断模块知识又会形成接手风险。
- **Decision**：一张任务卡对应一个分支和 PR，普通任务限于一个模块；相邻模块交叉审查；本机与 GitHub PR 统一执行 Ruff、Pyright、Pytest、Alembic、依赖检查、ESLint、TypeScript、Vitest、OpenAPI 同步、Playwright 和视觉检查。
- **Alternatives rejected**：直接提交主分支；只由模块作者自审；把真实 GLM 作为普通 PR 必过检查。三项分别破坏可追溯性、形成知识孤岛或导致费用和随机失败。

### D65. 三份共享合同保持纯净和严格

- **Context**：共享目录若导入框架、数据库或业务模块，会变成第五个耦合模块；宽松模型则会静默接受跨模块错误类型。
- **Decision**：只建立 `data.py`、`analysis.py`、`states.py` 三份纯合同，数据用严格 Pydantic，行为用 Protocol；继续复用现有数据支持的案例和 Agent 字段。
- **Alternatives rejected**：建立通用企业基础框架；合同依赖 SQLAlchemy 或 FastAPI；为模块方便复制字段。三项分别过度建设、制造反向依赖或产生漂移。

### D66. M1 提供五个小型公开端口

- **Context**：ZIP、查询、证据、运行和审核具有不同权限与失败方式，一个无限增长的 DataGateway 会扩大任何改动的影响面。
- **Decision**：M1 分别提供 `BatchImportGateway`、`CaseQueryGateway`、`EvidenceGateway`、`RunStore` 和 `ReviewStore`，共用同一 SQLite 实现并分别执行合同测试。
- **Alternatives rejected**：一个含全部方法的巨大接口；M3 直接操作 SQLAlchemy；把五个端口做成五个进程。三项分别难以维护、穿透边界或超过本机 MVP 需要。

### D67. M2 使用一个阻塞入口和类型化事件出口

- **Context**：M2 需要独立控制三阶段，M3 又必须及时保存阶段、调用和失败；只在最后返回会丢失中间记录，直接写库则破坏模块边界。
- **Decision**：M2 公开同步 `analyze_case`，执行中通过 `AnalysisEventSink` 报告事件，M3 在线程池调用并通过 M1 保存；成功结果包含三份严格合同。
- **Alternatives rejected**：M2 直接访问数据库；M2 启动独立服务；用异步生成器贯穿整个系统；只返回最终文本。四项分别耦合存储、扩大部署、增加复杂度或失去可追溯性。

### D68. 预期情况类型化，意外异常统一映射

- **Context**：业务可预期结果与程序故障混用异常，会让模块无法稳定处理，也可能把堆栈和供应商信息带到页面。
- **Decision**：预期情况返回可区分合同；真正异常在模块边界保留因果链，由 M3 映射为稳定 `BusinessError`；证据不足继续作为正常业务结果。
- **Alternatives rejected**：所有失败都抛异常；吞掉原始异常；把异常文本直接作为 `message`。三项分别缺少合同、无法排障或泄露技术信息。

### D69. M3 管状态意义，M1 管短事务

- **Context**：业务状态机属于应用编排，数据库原子性属于存储；任一方同时拥有两种职责都会穿透模块或埋藏流程规则。
- **Decision**：M3 决定状态流转，M1 通过短事务执行原子写入；M3 不接触 Session，M2 不接触事务，模型等待期间不打开事务。
- **Alternatives rejected**：M3 直接提交 Session；M1 根据数据自行推进业务；事务包住 GLM 调用。三项分别破坏边界、分散状态机或阻塞 SQLite。

### D70. 开发双进程，演示单入口并显式注入

- **Context**：开发需要热更新与模块替换，演示需要用户一次启动；隐藏 Fake 回退会让实际使用的分析能力不可确认。
- **Decision**：应用工厂显式注入 M1、M2；开发脚本启动 FastAPI 与 Vite，演示脚本由 FastAPI 提供 API 和构建前端；Fake 只存在于测试装配。
- **Alternatives rejected**：演示依赖两个终端或 Vite；强制 Docker；全局单例切换实现；缺密钥自动换 Fake。四项分别增加操作失败点、超过部署范围、难以测试或制造假结果。

### D71. 冻结十个面向产品动作的 HTTP 接口

- **Context**：三个页面的动作已经明确，增加通用 CRUD、运行查询或多种响应外壳只会扩大 M3、M4 的并行合同面。
- **Decision**：`/api/v1` 固定十个接口；单项返回资源、列表返回 `items` 与 `total`，开始和重跑以 202 立即返回更新业务视图。
- **Alternatives rejected**：通用后台 API；公开阶段和运行接口；所有写操作只返回成功布尔值。三项分别扩大范围、泄露实现或缺少可用结果。

### D72. 业务 API 隐藏运行编号并使用审核 Token

- **Context**：页面不应看到运行编号，但人工确认必须防止审核一次重跑已经替换的旧结果。
- **Decision**：开始与重跑只返回批次或案例视图；允许审核时返回不展示的 `review_token`，结果变化后 Token 改变，旧值返回 `STALE_CASE_RESULT`，数据库仍关联内部真实案例运行。
- **Alternatives rejected**：向页面返回 `case_run_id`；不做并发保护；依赖更新时间由前端比较。三项分别违反展示边界、可能审核旧结果或把最终判断交给客户端。

### D73. 使用有限 HTTP 状态和一个 BusinessError

- **Context**：FastAPI 默认错误、ZIP 错误、业务冲突和供应商错误若各自返回不同结构，前端会重复解析并可能暴露技术内容。
- **Decision**：只采用 11 个规定状态码，所有 JSON 错误统一为 `BusinessError`；FastAPI 默认 422 和 500 同样转换，供应商状态不透传。
- **Alternatives rejected**：全部返回 200；每个端点自定错误；直接返回 SDK 错误。三项分别隐藏失败语义、造成漂移或泄露内部信息。

### D74. 使用 limit-offset 和固定服务端排序

- **Context**：反复导入会积累历史数据，完全不分页会逐渐变慢；游标和任意查询又超过当前需要。
- **Decision**：批次默认 20、队列默认 50、最大 100，使用 `limit` 与 `offset`；只允许状态和介入等级筛选，排序由后端固定，前端保存 URL。
- **Alternatives rejected**：加载全部；游标分页；自由 `sort_by` 和全文搜索。三项分别缺少上限、增加复杂度或扩大 CRM 范围。

### D75. 只暴露最小业务 DTO 和 HealthView

- **Context**：把完整数据库或 Agent 结果返回后再由 React 隐藏，会让技术字段进入 OpenAPI、网络和缓存。
- **Decision**：批次、队列、详情、审核与错误只保留页面真实消费者；健康检查单独使用运行状态 DTO；不适用可选字段省略。
- **Alternatives rejected**：返回 ORM；通用 `data: any`；所有可能字段都设为 `null`。三项分别泄露、失去类型或增加前端误判。

### D76. 人工确认使用四种严格区分请求

- **Context**：四种审核结果具有不同必填关系，一个通用可选字段对象无法阻止修改无原因和驳回无判断。
- **Decision**：以 `outcome` 区分四个 Pydantic 请求；通过不重复结果，修改与驳回提交完整人工判断和原因，证据不足提交缺口说明，共同携带幂等编号与审核 Token。
- **Alternatives rejected**：一个塞满可选字段的请求；JSON Patch；允许页面编辑内部 JSON。三项分别难以校验、增加状态歧义或破坏业务边界。

### D77. 六个业务原因之外单设证据不足兜底

- **Context**：现有文字把六个真实原因和证据不足合计七个值误写成六类，且证据不足本身不是原因。
- **Decision**：保留六个业务原因，另设 `INSUFFICIENT_EVIDENCE` 非原因值；页面表达无法确认，人工证据不足不填写虚假原因。
- **Alternatives rejected**：把证据不足计为第七种原因；并入其他；证据不足时删除整个原因字段。三项分别语义错误、掩盖未知或破坏合同完整性。

### D78. 冻结五状态案例生命周期

- **Context**：业务必须区分未开始、处理中、待审核、完成和技术失败，又不能让 Agent 内部阶段进入页面。
- **Decision**：只使用五种案例业务状态；完整的证据不足结果进入待人工确认，技术故障进入处理异常，待确认与异常可重跑，已完成是终态。
- **Alternatives rejected**：按 Agent 阶段增加页面状态；把证据不足当错误；允许已完成案例修改。三项分别暴露实现、混淆边界或破坏审计。

### D79. 从当前案例推导四种批次业务状态

- **Context**：内部批次运行状态用于技术执行，页面只需要知道是否待开始、分析中、完成或仍有当前异常。
- **Decision**：批次业务状态固定四种并根据当前案例推导；单案例重跑期间显示分析中，ZIP 导入失败不创建批次。
- **Alternatives rejected**：直接暴露内部 batch_run；增加导入失败半成品状态；显示线程排队。三项分别泄露实现、污染存储或增加无依据信息。

### D80. 分开批次当前投影和首次运行历史

- **Context**：人工重跑不能覆盖首次批量运行历史，但页面继续显示旧错误数会与当前案例冲突。
- **Decision**：`batches` 保存当前业务状态与计数并随案例原子更新，`batch_runs` 保存首次运行历史且重跑不改，业务 DTO 只显示当前投影。
- **Alternatives rejected**：重跑改写历史；页面永远显示首次统计；前端自行汇总。三项分别破坏追溯、误导用户或形成第三套真相。

### D81. HTTP 开放前幂等收敛中断运行

- **Context**：程序强制结束可能留下永久分析中状态，自动恢复又可能在用户不知情时继续付费。
- **Decision**：迁移后把遗留活动运行标为中断，未完成案例记录 `APP_INTERRUPTED` 并进入处理异常，保留历史、重算当前投影且不调用模型。
- **Alternatives rejected**：永久保留分析中；启动时自动续跑；删除半程记录。三项分别无法恢复、费用不可控或失去证据。

### D82. 一个应用级执行器在阶段边界响应关闭

- **Context**：每请求自建线程池会突破并发上限，强杀正在运行的 Python 网络线程不可靠，关闭后继续进入新 Agent 阶段又产生额外调用。
- **Decision**：M3 只创建一个 ThreadPoolExecutor；关闭时拒绝新任务、取消未开始任务，当前模型请求返回或超时后由 M2 在阶段边界停止，强制退出留给启动恢复。
- **Alternatives rejected**：每任务新建线程池；强行杀线程；关闭时完成全部剩余阶段；引入 Celery。四项分别失控、不可靠、延长付费或超过 MVP。

### D83. 默认从项目根目录定位运行数据并保留本机覆盖

- **Context**：按当前命令目录解析会意外生成多套数据库，始终写在仓库旁又不利于以后迁移到 Windows 用户数据目录。
- **Decision**：默认从项目根目录解析 `runtime_data/`；允许 `config/local.toml` 显式覆盖到另一处本机目录，非法位置停止且不回退。
- **Alternatives rejected**：按当前目录解析；立即固定到 `%LOCALAPPDATA%`；失败时改用系统临时目录。三项分别制造多套状态、增加当前交接成本或隐瞒数据位置变化。

### D84. 固定回环默认端口但允许显式本机改号

- **Context**：完全写死端口会被成员电脑已有服务占用，自动随机换号又会破坏启动脚本、代理和测试的一致性。
- **Decision**：演示默认 `127.0.0.1:8000`，开发另用 Vite 5173；冲突时失败，允许通过本机配置显式改号，始终禁止非回环监听。
- **Alternatives rejected**：端口永不可改；自动寻找随机空闲端口；监听 `0.0.0.0`。三项分别阻塞部分电脑、造成地址漂移或扩大安全与部署范围。

### D85. 用 Windows 标准库系统锁保护每个运行目录

- **Context**：重复启动可能让两个进程同时迁移 SQLite、调度案例和调用 GLM；只看锁文件存在又会在异常退出后永久误判。
- **Decision**：进程全程持有 `psit.lock` 的系统独占锁，以能否取得锁判断实例状态，Windows 使用 `msvcrt` 而不新增依赖；不同数据根仅作为显式隔离的开发实例。
- **Alternatives rejected**：不加锁只依赖 SQLite；只检查端口；只检查文件存在；引入 Redis 锁。四项分别无法保护调度、无法覆盖不同端口、不能处理遗留文件或明显超过本机 MVP。

### D86. 本期不建设自动备份体系

- **Context**：自动 backup API、完整性检查和保留轮换会增加启动器与故障路径，10 天 MVP 没有真实长期数据恢复需求。
- **Decision**：Alembic 失败即停止并保留原库；重要演示前只在停机后人工复制完整 `runtime_data/`，自动备份另行立项。
- **Alternatives rejected**：每次启动备份；迁移前自动备份；只复制 WAL 模式下的单个数据库文件。三项分别增加范围、增加启动失败点或不能证明一致。

### D87. 每次演示启动构建当前前端

- **Context**：旧页面不能冒充当前实现，但内容指纹、缓存清单和原子替换超过本期核心需要。
- **Decision**：`start.ps1` 每次执行当前前端构建，失败即停止且不提供旧页面。
- **Alternatives rejected**：盲用已有 `dist`；本期建设内容指纹缓存和临时产物切换。前者可能展示旧页面，后者扩大范围。

### D88. 就绪后只显示实际访问地址

- **Context**：自动打开浏览器不是业务闭环，也会增加平台差异和启动分支。
- **Decision**：应用和数据库就绪后显示实际地址，由用户手工打开；不提供 `-NoBrowser`。
- **Alternatives rejected**：启动即打开；健康后自动打开；反复轮询打开。三项都增加无必要的演示失败点。

### D89. 未发布导入残留不进入业务

- **Context**：中断上传不能成为正式批次，但 quarantine、恢复元数据和保留策略会形成额外文件生命周期。
- **Decision**：只有正式文件与数据库事务共同发布的导入可见；未发布临时内容永不进入业务或模型输入，启动时不自动恢复也不删除陌生文件。
- **Alternatives rejected**：自动恢复残留；清空临时根；本期建设隔离样本保留。三项分别制造半批次、可能误删或扩大范围。

### D90. Ctrl+C 是唯一正常关闭入口

- **Context**：正常关闭必须收敛任务，但停止脚本和实例控制协议不改变用户核心结果。
- **Decision**：保留前台 `start.ps1` 和 Ctrl+C，复用一个关闭流程；强制结束由下次启动恢复。
- **Alternatives rejected**：业务页面关机按钮；无认证 HTTP 关机接口；`stop.ps1` 控制文件协议；强杀 PID。四项分别混入业务、扩大接口或破坏一致性。

## Risk Scenarios

| 编号 | 风险场景 | 预期处理 | 来源 |
|---|---|---|---|
| RS1 | 图片只上传但不参与判断 | 图片模态验收失败 | From docs/adr/0001 and CONTEXT.md |
| RS2 | 图片模糊，文本声称图片存在问题 | 标记图片证据不足，不按文本补造图片观察 | From CONTEXT.md |
| RS3 | 文本、图片、行为给出冲突信号 | 显示冲突，降低确定性或交给人工 | From docs/adr/0002 and CONTEXT.md |
| RS4 | 三套异源数据被当成同一真实客户 | 阻止客户级拼接；模拟关系必须标记 | From 00-当前项目唯一基线.md |
| RS5 | 运行输入含情绪、事件、原因、策略答案 | 拒绝或隔离答案字段 | From 00-当前项目唯一基线.md and CONTEXT.md |
| RS6 | 一个案例运行失败 | 该案例进入处理异常，同批其他案例继续 | From docs/adr/0006 |
| RS7 | 普通案例没有图片 | 降级运行，标记缺失，不用于多模态验收 | From docs/adr/0001 |
| RS8 | 多模态验收案例缺少核心模态 | 判定输入不合格，不得通过验收 | From CONTEXT.md |
| RS9 | 证据不足但系统强制给结论 | 判定输出不合格；保留未知和缺失证据 | From docs/adr/0004 and CONTEXT.md |
| RS10 | 运营人员修改介入等级但没有原因 | 阻止提交 | From docs/adr/0005 |
| RS11 | 运营人员驳回后没有最终人工判断 | 阻止完成，要求补充判断或标记证据不足 | From CONTEXT.md |
| RS12 | 刷新页面后人工结果丢失 | 判定人工闭环未完成 | From docs/adr/0005 |
| RS13 | 未经确认生成退款、发券、补偿金额 | 策略规则拒绝该输出 | From CONTEXT.md |
| RS14 | 生成消费下降、频次下降等行为变化 | 判定为无来源推断并拒绝结果；v1 只使用确定性客户价值和经人工核验的订单事实 | From CONTEXT.md |
| RS15 | recency_days 与 r_score 混用 | 判定 RFM 规则不合格 | From 00-当前项目唯一基线.md |
| RS16 | 用离线 Mock 结果声称真实留存或 ROI | 删除声明，标记证据越界 | From docs/adr/0007 |
| RS17 | 技术文档在真实样本形成前猜字段和阈值 | 停止依赖实现，等待对应触发条件 | From 02-需求澄清-待决定事项.md |
| RS18 | 固定演示案例绕过上传和导入校验 | 判定演示链路不合格；固定演示 ZIP 必须重新经过统一入口 | From docs/adr/0025 |
| RS19 | 一个批次运行时第二个批次也开始调用模型 | 拒绝第二次启动，返回当前活动批次编号并保持第二批待分析 | From docs/adr/0025 |
| RS20 | 案例队列混入其他批次或数据版本 | 判定队列隔离失败，只保留所选批次案例 | From docs/adr/0025 |
| RS21 | 业务页面显示原始 SDK 错误、堆栈、密钥或内部路径 | 判定错误分层失败，只显示稳定错误编号和可执行下一步 | From docs/adr/0026 |
| RS22 | 浏览器重复提交开始或重跑请求，产生第二次模型调用 | 返回当前运行记录，模型调用数量不得增加 | From docs/adr/0028 |
| RS23 | 人工确认响应丢失后重试，生成两条最终结果 | 相同 `submission_id` 返回原结果，只保存一次 | From docs/adr/0028 |
| RS24 | 使用另一个案例的 evidence_id 或本机路径读取图片 | 拒绝访问，不返回文件内容或绝对路径 | From docs/adr/0029 |
| RS25 | 刷新案例详情后页面丢失当前记录 | 按 URL 标识从后端重新读回案例 | From docs/adr/0027 |
| RS26 | 批次运行和案例运行共用一行或一类含混记录 | 拆分 `batch_runs`、`case_runs` 并用明确外键关联 | From docs/adr/0030 |
| RS27 | 案例重跑覆盖旧 Agent 结果或人工确认 | 新增案例运行并原子切换当前指针，旧记录保持可读 | From docs/adr/0031 |
| RS28 | 后台任务共享 Session 或在模型调用时持有事务 | 判定实现不合格；每任务独立 Session，模型等待期间无事务 | From docs/adr/0032 |
| RS29 | 两次并发开始产生两个活动批次运行 | 数据库唯一约束拒绝其中一个 | From docs/adr/0032 |
| RS30 | 数据库迁移失败后程序清空、重建或覆盖数据 | 立即停止并保留原库，不自动执行破坏性恢复 | 本需求与技术执行文档 |
| RS31 | 不同数据版本复用同一 `batch_id`，API 地址产生歧义 | 全局唯一约束拒绝第二个批次 | From docs/adr/0034 |
| RS32 | 人工重跑改变已结束批次的分析完成数或异常数 | 重跑不关联原 `batch_run`，原统计保持不变 | From docs/adr/0035 |
| RS33 | 人工审核提交时页面结果已经被一次重跑替换 | 旧 `review_token` 返回 `STALE_CASE_RESULT`，不写入审核结果 | From docs/adr/0036 and docs/adr/0071 |
| RS34 | 业务接口直接序列化数据库对象 | 合同测试失败；只允许返回业务结果 DTO | From docs/adr/0037 |
| RS35 | 页面展示 RFM、Agent、Prompt、Token、运行编号或原始 JSON | 判定业务结果视图不合格并移除 | From docs/adr/0037 |
| RS36 | 新增数据库字段后被 ORM 自动序列化进业务接口 | 响应 DTO 白名单拒绝未声明字段 | From docs/adr/0038 |
| RS37 | 人工修改后队列仍展示系统原建议 | 使用人工优先的当前有效业务结果重新投影 | From docs/adr/0039 |
| RS38 | 页面默认加载全部证据和内部来源结构 | 只返回当前结果引用的业务证据投影 | From docs/adr/0039 |
| RS39 | 归因已经完成、策略尚未完成时页面提前显示风险原因 | 保持分析中，完整决策包通过后一次性发布 | From docs/adr/0040 |
| RS40 | 感知或归因阶段看到高价值结论后夸大事件和原因 | 阶段输入构造器移除高价值与 RFM，仅策略阶段接收高价值结论 | From docs/adr/0041 |
| RS41 | 三个 Agent 分别调用 SDK，超时和重试行为不一致 | 静态检查只允许 `GlmClient` 导入和调用官方 SDK | From docs/adr/0042 |
| RS42 | Prompt 字段说明与 Pydantic 合同不一致 | 运行时从 Pydantic 生成 Schema，合同测试阻止发布 | From docs/adr/0043 |
| RS43 | 模型返回不存在或属于其他案例的证据编号 | 严格校验失败并最多定向修复一次 | From docs/adr/0044 |
| RS44 | 网络重试与结构修复叠加成无限调用 | 每阶段供应商请求硬上限为四次 | From docs/adr/0045 |
| RS45 | 通过改密封答案、删失败案例或放宽证据规则制造 PoC 通过 | 固定案例清单、答案哈希和门槛；任何变更都使本次结果无效 | From docs/adr/0046 |
| RS46 | 缺少 API Key 导致整个工作台无法读取历史结果 | 应用和数据库正常启动，只禁用开始与重跑并报告分析服务未就绪 | From docs/adr/0047 |
| RS47 | 密钥进入源码、配置、Prompt、日志或业务响应 | 只从 `ZAI_API_KEY` 读取，并执行 Git 与运行产物敏感信息扫描 | From docs/adr/0047 |
| RS48 | 团队成员依赖不同解释器、未锁依赖或个人绝对路径 | 固定主版本、提交双锁文件并通过四个项目脚本执行 | From docs/adr/0048 |
| RS49 | 技术日志记录客户全文、图片或模型完整响应 | 记录固定事件字段和哈希，禁止敏感正文并用自动扫描阻止提交 | From docs/adr/0049 |
| RS50 | PoC 未通过却仍把参数标记为正式可用 | 门槛判定失败关闭冻结出口，保留失败产物并明确模型路线不可用 | From docs/adr/0050 |
| RS51 | 照搬 Apple 营销页后案例表格和业务动作被巨大留白挤出首屏 | 使用 `3/2/6` 工作台参数和专用页面结构，不采用产品 Tile 与 Hero | From docs/adr/0051 |
| RS52 | 把 GitHub 社区 Skill 当成适用于全部页面的官方规范 | 固定来源提交，只读采用通用规则并服从项目冲突顺序 | From docs/adr/0051 |
| RS53 | `gpt-taste` 的随机版式、AIDA 和 GSAP 进入业务工作台 | 依赖与源码扫描拒绝相关实现 | From docs/adr/0051 |
| RS54 | 组件库默认皮肤与 Apple 视觉令牌混杂 | 集中主题映射，页面禁止散落颜色、圆角和间距常量 | From docs/adr/0051 |
| RS55 | 追求单一蓝色导致成功、提醒和错误无法区分 | 语义状态色作为受限例外，并同时使用文字与图标 | From docs/adr/0051 |
| RS56 | 只验正常桌面截图，1024px、键盘、错误和离线状态不可用 | 两种尺寸、完整状态、键盘、对比度和离线资源全部设为验收门槛 | From docs/adr/0051 |
| RS57 | 团队成员各自增加组件库、图标、状态库或样式体系 | 依赖静态检查失败，只保留 D53 固定前端栈 | From docs/adr/0052 |
| RS58 | FastAPI 合同已变化，前端仍使用旧生成类型或手写兼容字段 | 生成结果差异和类型检查失败，必须从当前 OpenAPI 重新生成 | From docs/adr/0053 |
| RS59 | 批次进入终态或页面进入后台后仍持续轮询，或多个组件产生重复计时器 | 停止或暂停轮询，由唯一查询定义管理刷新 | From docs/adr/0054 |
| RS60 | 浏览器缓存或 `localStorage` 中的旧结果覆盖后端人工最终结果 | 丢弃本地业务副本并从服务端重新读回 | From docs/adr/0054 |
| RS61 | 上传大小上限凭经验猜测，或浏览器校验通过后绕过后端校验 | 根据最大合格演示包冻结配置，FastAPI 始终执行最终校验 | From docs/adr/0055 |
| RS62 | 测试模型替身进入生产构建、由页面开启或绕过真实合同 | 构建和依赖检查失败，测试替身只能替换模型边界 | From docs/adr/0056 |
| RS63 | 产品流程测试全部调用真实 GLM，结果因费用、速度或随机性无法稳定回归 | 普通回归使用确定性替身，真实模型仅执行独立冒烟与 PoC | From docs/adr/0056 |
| RS64 | 字体、图标或必要页面资源依赖外部 CDN，断网后工作台缺失内容 | 资源本地打包并通过网络拦截 E2E | From docs/adr/0057 |
| RS65 | 四个模块到最终联调时才决定输入输出字段 | 停止依赖实现，先完成三组合同、示例和合同测试 | From docs/adr/0061 |
| RS66 | 纯横向目录导致四名成员持续修改相同 API、Service 和 Schema 文件 | 按四个业务模块分配目录和主负责人 | From docs/adr/0058 |
| RS67 | M1 调模型、M2 读数据库或 M4 直接调用 GLM | 依赖边界检查失败，只允许固定单向调用 | From docs/adr/0058 and docs/adr/0060 |
| RS68 | `common`、`utils` 或 `shared` 逐渐承载无人负责的业务实现 | 阻止新增公共业务代码，把实现归回明确模块 | From docs/adr/0059 |
| RS69 | 一个模块复制共享合同并独立修改 | 删除复制合同，从唯一合同重新生成并补兼容测试 | From docs/adr/0059 |
| RS70 | Fake 与真实适配器输出、错误或状态行为不同 | 对两者执行同一组合同测试，差异阻止接线 | From docs/adr/0061 |
| RS71 | 模块替身直接绕过导入、校验、状态机或持久化跳到成功页 | 判定独立测试无效，替身只允许替换模块边界 | From docs/adr/0061 |
| RS72 | 四个模块在一个大 PR 中首次接线，失败无法归属 | 回退为三次相邻模块接线并分别验收 | From docs/adr/0062 |
| RS73 | 一张任务卡跨多个模块或要求执行者重新设计整体架构 | 重新拆卡，只保留一个结果、模块和文件范围 | From docs/adr/0062 |
| RS74 | 普通功能 PR 偷带合同变化或未经过相邻模块审查 | PR 门槛失败，合同拆成独立变更并补审查 | From docs/adr/0059 and docs/adr/0063 |
| RS75 | 一个模块只有主负责人理解，成员缺席后无人能接手 | 相邻模块交叉审查，状态与验收夹具四人审查 | From docs/adr/0063 |
| RS76 | 共享合同导入 FastAPI、SQLAlchemy、SDK 或业务模块 | 依赖边界检查失败，合同只保留标准库、typing 和 Pydantic | From docs/adr/0064 |
| RS77 | 跨模块 Pydantic 宽松转换错误类型或接受未声明字段 | 严格校验拒绝输入并阻止合同测试通过 | From docs/adr/0064 |
| RS78 | 一个 DataGateway 累积导入、查询、证据、运行和审核全部方法 | 按五个小端口拆分并独立执行合同测试 | From docs/adr/0065 |
| RS79 | M3 为了方便直接使用 ORM 或数据库 Session | 依赖检查失败，只能调用 M1 公开端口 | From docs/adr/0065 and docs/adr/0068 |
| RS80 | M2 直接保存阶段结果或模型调用到 SQLite | 阻止数据库依赖，通过事件出口交给 M3 和 M1 | From docs/adr/0066 |
| RS81 | M2 只在全部完成后返回，进程中断导致阶段和模型尝试无记录 | 类型化事件逐阶段交付并及时短事务保存 | From docs/adr/0066 |
| RS82 | 证据不足被 M2 或 M3 当成技术失败 | 返回字段完整的成功结果并进入待人工确认 | From docs/adr/0066 and docs/adr/0067 |
| RS83 | Python 异常文本、堆栈或 SDK 响应穿过模块进入业务页面 | 模块边界保留因果链，M3 只返回稳定 BusinessError | From docs/adr/0067 |
| RS84 | M1 自行决定业务状态或 M3 自行提交数据库事务 | 边界测试失败，恢复 M3 决策与 M1 原子写入分工 | From docs/adr/0068 |
| RS85 | 数据库事务跨越 GLM 等待，导致查询和其他案例写入受阻 | 模型调用前结束事务，事件和结果分别短事务写入 | From docs/adr/0068 |
| RS86 | 演示模式依赖 Vite、Docker、第二个手工终端或可变全局装配 | `start.ps1` 从应用工厂启动单入口真实模块 | From docs/adr/0069 |
| RS87 | 缺少密钥或真实模块失败时系统自动切换 Fake 并展示假结果 | 保留只读工作台并禁用分析，绝不自动注入 Fake | From docs/adr/0069 |
| RS88 | 开始或重跑接口继续向页面返回批次运行或案例运行编号 | API 合同和敏感字段扫描失败，只返回业务视图 | From docs/adr/0071 |
| RS89 | 案例重跑后旧页面仍能提交人工确认 | `review_token` 变化，旧值返回 `STALE_CASE_RESULT` | From docs/adr/0071 |
| RS90 | 把 `review_token` 当作登录、权限或可编辑业务字段 | 只用于并发一致性且页面不展示，安全声明不得扩大 | From docs/adr/0071 |
| RS91 | FastAPI 默认 `detail`、HTML 500 或 SDK 状态形成第二套错误 | API 边界统一转换为 BusinessError | From docs/adr/0072 |
| RS92 | 全部失败返回 200，前端只能解析文案猜测结果 | 使用固定 HTTP 状态和稳定错误编号 | From docs/adr/0072 |
| RS93 | 历史批次和案例队列无上限一次加载 | 应用默认分页和最大 100 条边界 | From docs/adr/0073 |
| RS94 | 前端传任意排序后不同成员看到不同处理顺序 | 拒绝 `sort_by`，后端执行唯一业务排序 | From docs/adr/0073 |
| RS95 | ORM 或 Agent 新增字段自动进入业务响应 | DTO 白名单和 OpenAPI 快照失败 | From docs/adr/0074 |
| RS96 | HealthView 返回密钥、模型参数、数据库路径或异常详情 | 只返回三项就绪状态和业务说明 | From docs/adr/0074 |
| RS97 | 直接通过请求夹带另一份人工结果 | 区分联合类型拒绝该请求 | From docs/adr/0075 |
| RS98 | 修改后通过没有原因，或动作来自目录外且超过三项 | 请求合同和动作规则拒绝并不落库 | From docs/adr/0075 |
| RS99 | 驳回只有理由但没有人工最终判断 | 请求合同拒绝，要求完整人工结果或证据不足 | From docs/adr/0075 |
| RS100 | 证据不足请求同时提交确定原因和处理动作 | 区分联合类型拒绝矛盾字段 | From docs/adr/0075 |
| RS101 | 把六个业务原因加证据不足继续写成“六类原因” | 文档与枚举检查失败，明确兜底值不属于原因 | From docs/adr/0076 |
| RS102 | 证据不足被并入 OTHER 或强迫人工选择一个原因 | 使用独立兜底值和证据缺口说明 | From docs/adr/0076 |
| RS103 | 非法案例状态跳转、已完成案例重跑或证据不足进入处理异常 | 单一状态机拒绝并回滚 | From docs/adr/0077 |
| RS104 | 页面新增感知中、归因中、策略中、暂停或恢复状态 | 业务状态合同和可见文字扫描失败 | From docs/adr/0077 |
| RS105 | 批次业务状态直接等于内部 batch_run 状态或与当前案例冲突 | 根据当前案例重新推导四种业务状态 | From docs/adr/0078 |
| RS106 | 人工重跑成功后页面仍显示旧异常数 | 原子更新 batches 当前投影，页面不读取历史统计 | From docs/adr/0079 |
| RS107 | 人工重跑覆盖首次 batch_run 成功数和错误数 | 历史记录只读，重跑只影响当前投影 | From docs/adr/0079 |
| RS108 | 强制结束后案例永久停在分析中 | HTTP 开放前收敛为 APP_INTERRUPTED | From docs/adr/0080 |
| RS109 | 程序启动时自动从中间阶段继续调用 GLM | 恢复流程禁止创建任务和模型请求 | From docs/adr/0080 |
| RS110 | 每个请求或 Agent 创建自己的线程池，实际并发突破配置 | 生命周期只允许一个执行器并用探针验证 | From docs/adr/0081 |
| RS111 | 正常关闭后任务继续开始新的 Agent 阶段 | M2 在阶段边界检查关闭标记并停止 | From docs/adr/0081 |
| RS112 | 强制杀死正在进行的网络线程造成不一致写入 | 不强杀线程，当前请求返回或超时；来不及收敛由启动恢复 | From docs/adr/0081 |
| RS113 | 从不同当前目录启动后生成多套 `psit.db` | 所有相对路径固定按项目根目录解析 | From docs/adr/0082 |
| RS114 | 自定义运行目录无效后程序静默改用另一目录 | 以 `RUNTIME_DIR_INVALID` 停止并显示已解析位置 | From docs/adr/0082 |
| RS115 | 默认端口被占用后程序随机换号，页面和代理仍指向旧地址 | 以 `PORT_IN_USE` 停止，只接受显式本机改号 | From docs/adr/0083 |
| RS116 | 为解决端口问题把监听地址改为 `0.0.0.0` | 配置校验只允许 `127.0.0.1` | From docs/adr/0083 |
| RS117 | 两个进程同时迁移同一 SQLite 或重复调用 GLM | 数据库操作前必须取得运行目录独占锁 | From docs/adr/0084 |
| RS118 | 程序异常退出留下锁文件，后续永远无法启动 | 只判断系统锁，不判断文件是否存在 | From docs/adr/0084 |
| RS119 | 为单实例保护新增未锁版本的第三方库并产生环境差异 | Windows 交付使用 Python 标准库 `msvcrt` | From docs/adr/0084 |
| RS120 | 迁移失败后自动删除、重建或覆盖原数据库 | 立即停止并保留原库；演示前只在停机后人工复制完整运行目录 | 本需求与技术执行文档 |
| RS121 | 页面源码已变化但启动脚本继续提供旧 `dist` | 每次演示启动构建当前前端，失败则不开放 HTTP | 本需求与技术执行文档 |
| RS122 | 自动打开浏览器产生错误地址或重复标签 | 启动就绪后只显示实际地址，由用户手工打开 | 本需求与技术执行文档 |
| RS123 | 中断上传残留被当成正式批次或送入 GLM | 只有原子发布的导入可见，未发布临时内容永不进入业务 | 本需求与技术执行文档 |
| RS124 | 停止方式绕过正常关闭并破坏写入 | Ctrl+C 使用唯一关闭流程；强制结束由下次启动恢复 | 本需求与技术执行文档 |

## Testing Decisions

### 1. 测试原则

- 测试从需求行为和可观察结果出发，不从模型内部实现出发。
- 确定性测试模型必须经过真实上传、校验、状态机、持久化和页面读回链路，不能用组件 Mock 直接跳到成功页。
- 确定性测试只证明产品流程，真实模型能力由独立冒烟和 PoC 证明，两类结论不得互相替代。
- 运行输入与隐藏人工参考答案必须分开。
- 结构、状态、排序、持久化使用自动化测试。
- 事件还原和情绪解释的合理性使用至少两人独立判断形成的人工参考答案。
- E2E 只记录事实、复现步骤、实际结果，不在测试记录中写修复方案。

### 2. 行为与边界映射

| 需求 | 可观察行为 | 测试方法 |
|---|---|---|
| 批次导入 | 合格与不合格案例被分别识别 | 输入校验测试 |
| 统一上传链路 | 固定演示包与普通 ZIP 使用相同临时保存、校验和正式导入流程 | API 集成测试和 E2E |
| 数据身份 | observed、derived、simulated 全部可追溯 | 结构校验和复算 |
| 关系边界 | 未确认关系不进入同一案例 | 负向输入测试 |
| 批次独立性 | 单例失败不阻断其他案例 | 故障注入测试 |
| 后台执行 | 启动请求立即返回，结果逐案例可见 | API 集成测试和 E2E |
| 进度更新 | 活动批次按 2 秒轮询，结束后停止 | 前端计时器测试和 E2E |
| 单活动批次 | 第二个批次不启动、不自动排队且不产生模型调用 | API 集成测试和模型调用记录 |
| 批次队列隔离 | 切换批次时不混合案例或数据版本 | 查询集成测试和 E2E |
| 错误信息分层 | 业务错误可处理，技术原始信息不出现在页面和业务响应 | API 集成测试、E2E 和敏感信息扫描 |
| 页面路由恢复 | 刷新和返回后恢复同一批次与案例 | 路由测试和 E2E |
| 接口返回合同 | 单项、列表、写操作和错误结构保持一致 | OpenAPI 合同测试和 API 集成测试 |
| 操作幂等 | 重复上传、开始、重跑和人工确认不重复调用或落库 | 并发 API 集成测试和数据库读回 |
| 证据访问边界 | 只有归属于当前案例的合法图片可以读取 | 路径穿越、越权标识和媒体类型负向测试 |
| 运行层级 | 一次批次启动对应一个批次运行和多个案例运行 | 数据库关系集成测试 |
| 当前指针切换 | 重跑完成后原子指向新运行，旧历史不变 | 事务与回滚测试 |
| Session 隔离 | 请求和后台任务不共享 Session，模型等待不占事务 | 并发集成测试 |
| 单活动运行约束 | 并发请求不能创建第二个活动批次或同案例运行 | 唯一约束和并发集成测试 |
| 迁移保护 | 迁移失败不启动、不删除、不重建也不覆盖原数据库 | 启动脚本故障注入测试 |
| 目录边界 | 运行数据不入 Git，验收答案不进运行目录 | 仓库静态检查 |
| 公开标识唯一性 | 一个批次地址始终只定位一条批次记录 | 唯一约束和 API 合同测试 |
| 人工重跑链路 | 重跑指向上一个案例运行且不污染批次统计 | 数据库关系集成测试 |
| 逻辑 Schema | 九张表字段、外键、唯一约束和枚举符合冻结含义 | Alembic Schema 测试 |
| 业务结果投影 | 页面和业务 API 不泄露技术字段，只呈现业务结果 | DTO 单元测试、API 合同测试和 E2E |
| DTO 白名单 | ORM 新增字段不能自动出现在业务响应 | Pydantic 响应 Schema 和回归测试 |
| 当前有效结果 | 人工确认后队列和详情切换为人工最终结果 | 投影服务单元测试和 E2E |
| 业务证据投影 | 只返回当前结果引用的业务证据，不返回内部元数据 | DTO 单元测试和 E2E |
| 结果发布门槛 | 三阶段全部通过前没有业务半成品 | 故障注入、状态机测试和 E2E |
| 阶段输入最小化 | 每个 Agent 只收到当前职责允许的数据 | 输入构造器单元测试和请求清单快照 |
| 模型接入边界 | 只有 `GlmClient` 调用官方 SDK | 导入依赖静态检查和集成测试 |
| Prompt 与 Schema 同源 | Pydantic 变化能够同步生成运行 Schema | Schema 快照和 Prompt 装配测试 |
| 严格输出校验 | 多余字段、宽松类型、错误证据和跨案例引用均被拒绝 | 参数化合同测试 |
| 请求次数上限 | 网络重试和结构修复合计不超过四次 | 故障注入和模型调用计数 |
| 模型 PoC | 正式参数有 15 个固定案例的结构、质量、耗时和用量依据 | PoC 报告检查和密封评分 |
| PoC 硬门槛 | 45 个阶段全部真实运行、所有发布结果严格有效、失败完整保留、5 个密封案例和三项模态贡献通过 | 合同统计、双人人工评分和时间线记录 |
| 配置校验 | 非敏感参数从 TOML 读取，非法配置阻止启动 | 参数化配置与启动测试 |
| 密钥与降级 | 密钥只来自环境变量，缺失时历史只读能力保留且分析不可用 | 敏感信息扫描、API 测试和 E2E |
| 开发工具链 | 双锁文件和四个脚本能在干净环境复现安装、启动与测试 | 脚本集成测试和绝对路径扫描 |
| 结构化日志 | 固定必要字段、跨层 trace 和脱敏规则均生效 | JSONL 解析、链路追踪和敏感信息测试 |
| PoC 产物闭环 | 报告、汇总、逐案例结果可相互核对，失败时不能冻结参数 | 产物 Schema、引用一致性和负向流程测试 |
| 前端视觉合同 | 三个页面使用项目令牌，不直接交付组件库默认皮肤 | 主题快照、令牌静态检查和人工截图审阅 |
| 前端尺寸适配 | 1366×768 与 1024×768 均能完成核心操作且主体不横向滚动 | 多尺寸浏览器 E2E |
| 交互完整状态 | 加载、空数据、分析中、异常、只读和密钥缺失均有稳定界面 | 组件状态矩阵和浏览器 E2E |
| 前端可访问性 | WCAG AA、44px 点击区域、键盘焦点和非颜色单一表达均达标 | axe、样式断言和键盘 E2E |
| 本机资源边界 | 断网时字体、图标和页面资源仍可用 | 网络拦截和离线 E2E |
| 外部审美来源边界 | taste-skill 只读参考，gpt-taste、GSAP 和随机版式不进入依赖与源码 | 依赖清单和源码静态扫描 |
| 前端依赖边界 | 只有冻结的 React 技术栈、Ant Design 和单一图标族进入产品 | 依赖清单、导入边界和源码静态检查 |
| OpenAPI 类型同步 | 后端合同变化会使旧生成类型检查失败，重新生成后前后端一致 | 生成结果差异检查、TypeScript 类型检查和 API 合同测试 |
| URL 与服务端真源 | 刷新、返回和本地存储变化不改变后端保存的当前业务结果 | 路由 E2E、缓存失效测试和服务端读回 |
| ZIP 两步交互 | 导入先显示摘要且不调用模型，开始分析必须由第二个动作触发 | 组件测试、API 集成测试、调用计数和 E2E |
| 确定性产品流程 | 测试模型替身经过完整产品链路且不能进入生产构建 | Playwright、构建扫描和数据库读回 |
| 真实模型分层 | GLM 冒烟与 PoC 报告独立，不被自动化流程通过状态替代 | 标签隔离、报告检查和 PoC 门槛验证 |
| 四模块目录边界 | 每块业务代码只归属 M1—M4 之一，纯合同目录无业务实现 | 目录清单和源码静态检查 |
| 模块依赖方向 | M4→M3、M3→M1/M2，M1 与 M2 互不调用 | 导入图、架构测试和负向夹具 |
| 共享合同治理 | 合同变更独立提交并同步示例、生成物和测试 | PR 模板检查和合同差异测试 |
| 模块独立运行 | 其他模块未完成时，四块分别通过自己的替身与夹具验收 | 四组独立测试命令 |
| Fake 与真实一致 | 相同合同测试对替身和真实适配器均通过 | 参数化合同测试 |
| 分阶段接线 | 三次相邻模块连接分别有通过记录，问题能够归属 | 集成记录、变更范围和确定性 E2E |
| 任务原子性 | 每张卡只有一个结果、一个模块和声明文件范围 | 任务 Schema 与 PR 变更检查 |
| 交叉审查 | 相邻模块和共享合同均有规定审查者 | PR 审查状态检查 |
| 纯合同依赖 | 三份合同不引用框架、数据库、SDK 和业务模块 | 导入边界和循环依赖检查 |
| M1 五端口 | Fake 与 SQLite 实现的正常、失败、幂等和安全行为一致 | 参数化合同测试 |
| M2 事件顺序 | 每次阶段与模型尝试形成合法事件序列，最终成功含三份结果 | 事件序列单元测试和模块集成测试 |
| 证据不足归类 | 字段完整、明确未知并进入人工确认，不进入技术异常 | 固定证据不足案例和状态测试 |
| 错误与异常分层 | 预期结果可区分，意外异常可追踪但业务页面无原始信息 | 故障注入、日志和 API 合同测试 |
| 事务归属 | M3 不接触 Session，GLM 等待无事务，发布与审核原子完成 | 依赖检查、并发测试和回滚测试 |
| 应用装配 | 开发双进程、演示单入口，Fake 只在测试显式注入 | 脚本集成、构建扫描和浏览器冒烟 |
| 十个 API | 路径、方法、成功状态和响应 DTO 与合同一致 | OpenAPI 快照和 API 集成测试 |
| 运行编号隐藏 | 开始、重跑、轮询和审核流程均不向业务 API 返回 run id | OpenAPI 与响应敏感字段扫描 |
| 审核 Token | 当前结果稳定时可提交，重跑后旧 Token 不能落库 | 并发 API、Token 和数据库读回测试 |
| HTTP 错误统一 | 规定状态均使用 BusinessError，没有默认 detail、HTML 或 SDK 原文 | 参数化错误合同测试 |
| 简单分页筛选 | 默认、上下界、总数、后端排序和 URL 恢复一致 | API 参数化与浏览器 E2E |
| DTO 最小化 | 页面需要字段完整，技术字段和不适用 null 不出现 | Pydantic 白名单、OpenAPI 快照和响应扫描 |
| 四种审核请求 | 每种必填和禁用字段、动作范围、幂等与并发校验正确 | 区分联合类型和数据库集成测试 |
| 原因枚举语义 | 六个真实原因与一个证据不足兜底在合同和文案中一致 | 枚举、Schema、文案和快照测试 |
| 案例状态机 | 允许转换成功，所有其他转换拒绝且不写库 | 参数化状态机和事务测试 |
| 批次业务状态 | 四种状态始终与案例当前状态一致，不泄露内部运行 | 投影单元、数据库集成和 API 合同测试 |
| 当前与历史统计 | 人工重跑只改变 batches 当前投影，不改变 batch_runs 历史 | 事务、重跑和数据库读回测试 |
| 启动中断恢复 | 遗留活动运行在 HTTP 开放前幂等收敛且零模型调用 | 进程故障、启动集成和调用计数测试 |
| 执行器生命周期 | 单一线程池、并发受限、浏览器无关、关闭不启动新阶段 | 执行器探针、并发和事件序列测试 |
| 多模态联合 | 重要结论引用三类证据 | 结构校验和 E2E |
| 模态冲突 | 冲突可见并影响确定性 | 固定冲突案例 |
| 图片失败 | 不反向编造图片内容 | 模糊图和不可读图测试 |
| 证据不足 | 字段保留，明确未知 | 缺证据案例 |
| 队列排序 | 人工等级优先，同等级按系统导入时间、稳定 `case_id` | 排序单元测试和 E2E |
| 人工门禁 | 修改、驳回无原因不能提交 | 表单 E2E |
| 持久化 | 刷新和重开后结果存在 | 存储集成测试和 E2E |
| 图片贡献 | 有图与无图结果存在规定差异 | 消融对比 |
| 行为贡献 | 有行为与无行为结果存在规定差异 | 消融对比 |
| 声明边界 | 无越界商业结论 | 静态文本扫描和人工审阅 |

### 3. 现有测试参考

当前项目建设目录没有代码仓库、测试文件、可复用测试样例。执行 Agent 不得伪造参考路径。技术方案确定并创建仓库后，应先建立测试目录和 5 个固定案例，再写实现。

## E2E Verification Plan

### Environment

编写本 PRD 时的环境核查结果：

- 项目建设目录不是 Git 仓库。
- 当前没有可运行产品、服务地址、数据库、API。
- 当前没有登录账号、角色权限、测试账号要求。
- 当前 Codex 环境具备文件读取和命令执行能力。
- 当前 Codex 环境具备浏览器 UI 自动化能力。
- 当前 Codex 环境不提供原生桌面应用控制，不能假设 computer-use 可操作任意本机应用。
- 当前没有 gh 命令，不能发布 GitHub Issue。
- curl/API 验证是否可用取决于技术文档是否定义 HTTP 接口。
- DB 查询是否可用取决于技术文档是否选择数据库并提供只读验证方式。

E2E 开始前必须重新核查工具、账号、权限、产品地址，不能沿用本文档日期的环境状态作为实时事实。

### Step 0 范围

E2E 覆盖以下产品表面：

- 案例批次导入。
- 批量预分析启动和进度。
- 案例队列。
- 案例详情。
- 产品侧人工最终确认。
- 刷新和重新打开后的记录读回。

### Step 1 前置

E2E 被以下事项阻塞：

- 至少一份真实上游处理样本和来源说明已形成。
- 产品输入合同已冻结。
- 文本、图片、行为特征最小字段已冻结。
- 5 个固定案例和隐藏人工参考答案已建立。
- RFM 最小可用规则已冻结。
- 技术路线完成最小测试并确定。
- 产品已能在明确环境启动。
- 持久化方式和读回方式已确定。

### Step 2 冒烟

- 启动产品。
- 通过浏览器上传并导入一个合格案例批次。
- 通过浏览器上传一个不合格案例批次。
- 启动一次预分析。
- 打开案例队列。
- 打开一个案例详情。
- 完成一次人工通过。
- 刷新后重新打开记录。

任何一步失败，先记录实际结果和复现路径，再进入修复流程。

### Step 3 核心

| 用户旅程 | 验证标记 | 核心结果 |
|---|---|---|
| 通过统一入口导入含 5 个固定案例的 ZIP | [BROWSER]；有 API 后补 [CURL] | 固定包不绕过校验；合格案例进入批次，不合格输入给出具体原因 |
| 运行批量预分析 | [BROWSER]；有存储后补 [DB] | 单例失败不阻断批次 |
| 在队列决定先处理谁 | [BROWSER] | 字段齐全，排序和筛选符合规则 |
| 在详情核验证据 | [BROWSER] | 结论优先，可展开三类依据、冲突、缺口 |
| 完成人工通过 | [BROWSER]；有存储后补 [DB] | 原结果和人工结果同时保存 |
| 完成人工修改 | [BROWSER]；有存储后补 [DB] | 无原因不能提交，修改后重排 |
| 完成人工驳回 | [BROWSER]；有存储后补 [DB] | 原因和最终人工判断保存 |
| 验证图片贡献 | [BROWSER]；可导出结果时补 [CURL] | 两个案例的有图与无图结果满足差异规则 |
| 验证行为贡献 | [BROWSER]；可导出结果时补 [CURL] | 一个案例的有行为与无行为结果满足差异规则 |
| 验证刷新读回 | [BROWSER]；有存储后补 [DB] | 记录完整存在 |

### Step 4 回归

- 普通无图片案例仍可降级完成。
- 证据不足案例不进入处理异常。
- 模态技术失败案例可以重试。
- 批次运行时，已完成案例不等待整批即可打开。
- 待人工确认案例可以重新分析并生成新运行版本。
- 已完成案例在业务页面没有重新分析入口。
- 人工修改后系统原建议仍存在。
- 人工修改后的队列排序不影响未修改案例的数据。
- 重跑一个案例不覆盖历史人工确认记录。
- 切换到历史批次后只看到该批次案例，返回最近批次后列表恢复。
- 业务页面不出现 Agent 阶段、Prompt、Token、供应商原始响应或程序堆栈。
- 直接刷新批次和案例详情地址后，页面从后端恢复相同内容。
- 同一上传、开始、重跑和人工确认请求发生网络重试时，不产生重复数据或模型调用。
- 案例重跑完成后刷新详情，页面显示新运行；历史记录仍能查到旧运行和旧人工确认。
- 两个案例同时运行时，队列查询和人工查看已完成案例不被长事务阻塞。
- 页面主信息始终围绕高价值客户结论、流失风险原因和处理建议，不出现技术实现说明。
- 人工修改后返回队列再打开案例，两个页面均先显示人工最终结果；系统原建议只在折叠区可查看。
- 任一内部阶段完成但完整决策包尚未形成时，业务页面始终只显示分析中或可处理错误。
- 缺少 `ZAI_API_KEY` 时仍能打开工作台和历史详情，开始分析与重新分析入口不可执行，补回密钥并重启后分析能力恢复。
- 同一次故障可以用一个 `trace_id` 从 API 入口定位到执行器、Agent、模型调用和数据库事件，但页面与业务响应看不到该技术链路。
- PoC 未满足任一硬门槛时，报告明确为未通过，正式配置不改变；完整重跑通过后才允许冻结对应参数。
- 三个页面在 1366×768 和 1024×768 保持相同视觉令牌、业务层级和主操作，不退回组件库默认样式。
- 键盘、断网、加载、空数据、异常、只读和密钥缺失状态保持可操作、可读且布局稳定。
- 活动批次轮询、终态停止、后台暂停和回到前台立即刷新在重复进入页面后仍只存在一套请求节奏。
- 修改浏览器本地存储、刷新页面或使用浏览器返回，不会覆盖 SQLite 中的系统结果和人工最终结果。
- 确定性测试模型完成完整上传到人工确认旅程时，报告只标记产品流程通过；真实 GLM 冒烟与 PoC 单独执行和记录。
- 生产构建不包含测试模型开关、外部字体请求、第二套组件库或手写的重复 API 类型。
- M1、M2、M3、M4 在只接入各自替身时分别能够独立启动和通过模块验收。
- 替身替换为真实适配器后，调用方业务代码和页面组件不需要修改，合同测试继续通过。
- 三次真实接线各自形成通过记录，不以一次大合并的最终结果掩盖中间接口问题。
- 普通任务没有跨模块内部修改，共享合同变化全部出现在独立 PR 中并经过规定审查。
- M2 每个阶段和模型尝试都能通过事件追溯，M3 只按合法状态机保存，M2 中止时已完成记录仍存在。
- 证据不足案例保持完整结果并进入人工确认，模型超时或结构耗尽才进入处理异常。
- 开发脚本与演示脚本使用相同真实合同；演示模式缺密钥或模块失败时不出现 Fake 结果。
- 开始、重跑和轮询只使用批次与案例业务地址，响应 JSON 和页面均不出现运行编号。
- 四种人工确认分别完成合法提交、必填缺失、禁用字段、旧 Token 和重复提交回归。
- 列表在 0、1、默认、100 和超过 100 的边界下保持稳定总数、排序与错误合同。
- 证据不足、技术失败、人工重跑和已完成案例分别只能进入规定状态，页面不出现内部 Agent 阶段。
- 人工重跑成功或失败后，批次当前状态和计数立即与案例一致，首次批量运行统计不变。
- 浏览器关闭不影响分析；正常停止和强制结束后重新启动都不会留下永久分析中或自动续费调用。

### Step 5 边缘

- 空批次。
- 重复 case_id。
- 重复 evidence_id。
- 来源标记缺失。
- simulated 关系无说明。
- 图片格式不支持。
- 图片模糊。
- 模型服务超时。
- 文本、图片冲突。
- 输入意外出现行为变化或比较窗口字段。
- 风险原因存在反证但未引用，或没有反证却输出空洞占位内容。
- 策略超出批准范围。
- 动作目录存在重复代码或非法动作。
- 修改无原因。
- 驳回无最终人工判断。
- 已完成案例尝试从业务 API 重新分析。
- 批次运行中关闭应用并重新启动。
- 一个批次运行时尝试启动第二个批次。
- 前一个批次结束后等待一段时间，确认待分析的第二批没有自动启动。
- 上传 ZIP 时尝试提交服务器文件路径或包含路径穿越的文件名。
- 模型供应商返回含内部细节的错误，确认业务页面只显示稳定错误编号和下一步。
- 使用另一个案例的 `evidence_id` 请求图片。
- 将本机绝对路径作为图片参数提交给证据接口。
- 上传内容完全相同的 ZIP，以及批次身份相同但内容不同的 ZIP。
- 使用相同和不同 `submission_id` 重复提交已完成案例。
- 同时发送两个批次启动请求和两个同案例重跑请求。
- 模型调用长期未返回时执行队列查询和另一案例状态写入。
- 在 Alembic 升级中注入失败并检查原数据库未被删除、重建或覆盖，进程以失败状态退出。
- 使用相同 `batch_id`、不同 `data_version` 和不同内容上传两个 ZIP。
- 在打开人工确认页面后完成一次后台重跑，再提交旧页面中的 `review_token`。
- 让业务 API 序列化器意外加入内部整数 ID、运行编号、Prompt 版本或 Token，确认合同测试失败。
- 在归因完成后暂停策略阶段，检查业务接口没有原因半成品。
- 让策略阶段失败，同时确认数据库保留内部诊断、页面不显示残缺结果。
- 人工修改系统建议后检查队列、详情、折叠原建议和证据引用的一致性。
- 在感知输入中故意加入高价值结论、在归因输入中加入原图，确认阶段输入构造器拒绝。
- 让模型返回多余字段、字符串形式数组、错误 `case_id`、不存在或跨案例的 `evidence_id`。
- 分别模拟连接中断、429、500、401、403、结构错误和修复请求网络失败，并核对请求次数。
- 修改 Prompt 文件但不更新版本或哈希预期，确认合同检查失败。
- 在 PoC 中分别运行 `high/max` 和并发 1、2、3，确认结果记录可复算且密封答案未进入请求。
- 让任一已发布阶段结果违反严格合同、任一密封案例不可用或任一模态贡献未达标，分别确认 PoC 判定为失败；未发布技术失败继续保留而不是删除。
- 修改固定案例清单、密封答案哈希或只保留成功案例结果，确认本次 PoC 产物被判为无效。
- 删除 `ZAI_API_KEY`、向 TOML 写入错误类型和越界并发值，分别确认只读降级与启动失败边界。
- 向日志事件注入 API Key、完整对话、图片 base64、完整 Prompt、本机绝对路径和供应商原始响应，确认脱敏或写入阻止规则生效。
- 解析技术日志并注入敏感内容，确认固定必要字段可关联且敏感内容不会写入。
- 在另一台干净 Windows 环境或隔离环境运行四个 PowerShell 脚本，确认不依赖 WindowsApps 别名、成员目录或 Codex 缓存路径。
- 将窗口设为 1024×768 并填入最长合法客户名、风险原因和按钮文字，确认主按钮不换行、页面主体不横向滚动、主要结论不被截断。
- 暂停字体和外部网络请求，确认本地字体回退、图标和三个页面仍能正常显示与操作。
- 去掉状态图标或状态文字，仅保留颜色，确认可访问性检查失败。
- 把主按钮、输入框、帮助文字或错误文字改成不满足 WCAG AA 的颜色，确认自动检查失败。
- 向页面加入第二套图标、卡片投影、AI 紫色渐变、GSAP、假截图或装饰性 Hero，确认视觉静态检查失败。
- 在活动批次打开多个页面、反复前后台切换并进入终态，确认没有重复轮询、后台请求或终态请求。
- 修改 `localStorage` 中任意键值后刷新案例，确认页面只采用服务端保存结果。
- 让浏览器快速检查通过但 ZIP 内部目录、身份或关系不合格，确认 FastAPI 仍拒绝导入。
- 使用刚好低于、等于和高于 `upload.max_zip_bytes` 的 ZIP，确认前后端采用同一边界且错误信息可处理。
- 修改 FastAPI OpenAPI 但不更新生成类型，确认合同检查失败；重新生成后再通过。
- 尝试在生产构建中开启测试模型，或让测试模型绕过 ZIP、Schema、状态机和数据库，确认构建或测试失败。
- 阻断全部外部网络请求，确认没有字体、图标、脚本或页面资源请求离开本机。
- 在 M1 中导入 M2 内部实现、让 M2 访问 SQLAlchemy、让 M4 引用后端模块，确认依赖边界检查分别失败。
- 让共享合同反向导入 M1 或 M3，确认循环和反向依赖检查失败。
- 分别修改 Fake 和真实适配器的错误编号、终态或字段结构，确认同一合同测试能发现差异。
- 让 M3 的 Fake 直接返回已完成结果而不经过状态机，确认模块独立验收失败。
- 在普通功能 PR 中同时修改两个模块或共享合同，确认文件范围门槛失败。
- 模拟任一模块主负责人暂时离开，确认相邻审查者能够依据接口、测试和文档运行该模块。
- 向纯合同目录加入 FastAPI、SQLAlchemy、智谱 SDK 或模块导入，确认依赖检查失败。
- 向严格合同增加多余字段、数字字符串、错误数组或错误布尔值，确认边界直接拒绝。
- 让 M1 Fake 和 SQLite 实现对同一重复导入、跨案例证据或过期审核返回不同结果，确认合同测试失败。
- 在 M2 第二阶段完成后强制中断，确认已经发生的事件可读、案例没有发布半成品业务结果。
- 让 AnalysisEventSink 保存失败，确认当前案例终止并进入可定位技术异常，不继续调用后续阶段。
- 在 GLM 请求阻塞期间尝试队列查询、人工读取和另一案例写入，确认没有长事务阻塞。
- 在演示启动参数、环境变量或页面中尝试启用 Fake，确认装配或构建检查拒绝。
- 分别让 FastAPI 请求校验、未捕获异常和模型服务不可用，确认响应均为 BusinessError 而不是默认 detail、HTML 或 SDK 原文。
- 在开始、重跑、批次和案例查询响应中注入 `run_id`、`case_run_id` 或 `batch_run_id`，确认合同扫描失败。
- 对历史批次和案例队列传负数、零、100、101、未知筛选和任意 `sort_by`，确认边界行为符合合同。
- 分别向四种审核请求加入不属于该分支的字段，确认严格联合类型拒绝且数据库不产生记录。
- 枚举六个真实原因并额外检查 `INSUFFICIENT_EVIDENCE`，确认页面不把它称为一种原因。
- 对五种案例状态尝试全组合转换，确认只有白名单路径成功。
- 在处理异常案例重跑成功、待人工确认案例重跑失败两个方向分别核对当前批次计数和首次运行历史。
- 在三个 Agent 阶段和线程池排队期间分别强制结束应用，再连续启动两次并核对状态、记录和模型调用数。
- 在浏览器关闭、FastAPI 正常关闭和进程强制终止三种情况下核对 Future、阶段事件和恢复结果。
- 页面中意外出现停止、取消或恢复按钮。
- 刷新发生在保存过程中。

## 手测 Checklist

### 必须由人判断

- [ ] [HUMAN] 售后运营人员能否在队列首屏看懂为什么这个案例排在前面。
- [ ] [HUMAN] 案例详情的结论是否先于证据出现，又没有隐藏关键不确定性。
- [ ] [HUMAN] 事件还原是否忠于证据，没有把推断写成事实。
- [ ] [HUMAN] 情绪解释是否符合人工参考答案允许范围，没有“读心”措辞。
- [ ] [HUMAN] 风险原因、反证、不确定性是否足以支持人工反驳系统。

### 可以由 AI 或自动化先检查

- [ ] [AI] 所有重要判断均引用可展开的 evidence_id。
- [ ] [AI] 模态冲突、证据不足、技术失败使用不同状态。
- [ ] [AI] 六类批准动作之外的动作被拒绝。
- [ ] [AI] 修改和驳回原因持久保存。
- [ ] [AI] 页面和报告没有越界商业声明。

## Knowledge Base References

### 当前需求真源

- E:\xtzj\Desktop\【临时】纪要处理\项目建设\CONTEXT.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\PSIT前端视觉与交互规范.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\PSIT四模块并行开发架构.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\PSIT模块接口合同.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\PSIT-HTTP-API合同.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\PSIT状态机与中断恢复.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\PSIT本机运行与启动保护.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0001-retain-image-as-required-mvp-modality.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0002-use-decision-level-multimodal-evidence-integration.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0003-separate-raw-data-processing-from-case-runtime.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0004-use-evidence-grounded-event-and-emotion-reconstruction.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0005-require-human-final-confirmation.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0006-use-case-queue-before-case-resolution.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0007-position-current-delivery-as-core-flow-validation-mvp.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0018-use-source-bounded-caseinput-v1.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0019-use-typed-evidence-and-grounded-perception-result.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0020-admit-v1-fields-by-source-and-consumer-need.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0021-require-explicit-analysis-start-and-limit-business-reruns.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0022-enforce-policy-in-code-and-use-structured-human-review.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0023-use-in-process-background-execution-and-rest-polling.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0024-ship-a-single-process-local-web-application.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0025-use-browser-zip-upload-and-one-active-batch.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0026-separate-business-status-and-errors-from-technical-diagnostics.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0027-use-three-page-routes-and-minimal-rest-api-v1.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0028-make-costly-and-persistent-mutations-idempotent.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0029-serve-evidence-through-scoped-identifiers.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0030-split-batch-runs-from-case-runs.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0031-store-query-fields-as-columns-and-versioned-results-as-json.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0032-use-synchronous-sqlalchemy-and-short-sqlite-transactions.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0033-separate-source-fixtures-and-runtime-data.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0034-use-globally-unique-public-batch-ids.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0035-keep-manual-reruns-out-of-batch-progress.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0036-freeze-nine-table-logical-schema-and-enums.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0037-expose-only-business-result-projections.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0038-use-explicit-business-response-dtos.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0039-project-effective-results-and-referenced-evidence.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0040-publish-only-complete-decision-packages.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0041-use-stage-specific-least-privilege-contexts.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0042-centralize-provider-access-in-glm-client.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0043-version-prompts-and-generate-schema-from-pydantic.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0044-use-strict-stage-validation-and-one-directed-repair.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0045-cap-stage-requests-and-freeze-parameters-after-poc.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0046-require-fixed-case-poc-gates.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0047-use-validated-toml-config-and-environment-secrets.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0048-pin-python-node-and-project-toolchains.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0049-use-redacted-structured-jsonl-logs.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0050-preserve-poc-artifacts-and-fail-closed.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0051-adapt-apple-and-taste-guidance-for-the-psit-workbench.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0052-use-react-vite-antd-and-centralized-theme.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0053-generate-the-frontend-client-from-fastapi-openapi.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0054-use-url-state-and-bounded-tanstack-query-polling.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0055-use-one-zip-two-step-import-and-data-derived-size-limit.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0056-separate-deterministic-flow-tests-from-live-glm-validation.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0057-verify-two-desktop-sizes-accessibility-and-offline-assets.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0058-organize-a-module-first-monorepo-with-one-way-dependencies.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0059-treat-shared-contracts-as-separately-reviewed-changes.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0060-divide-delivery-into-four-owner-modules.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0061-build-modules-against-contract-fakes-before-real-integration.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0062-integrate-four-modules-in-stages-and-plan-36-tasks.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0063-use-one-task-one-pr-cross-review-and-deterministic-gates.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0064-keep-cross-module-contracts-pure-and-strict.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0065-expose-five-narrow-data-ports.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0066-use-one-blocking-analysis-entry-and-a-typed-event-sink.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0067-return-typed-expected-outcomes-and-map-unexpected-errors.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0068-let-application-decide-transitions-and-data-own-atomic-writes.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0069-use-explicit-app-wiring-for-development-and-single-entry-demo.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0070-freeze-ten-resource-oriented-http-endpoints.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0071-hide-run-ids-and-use-an-opaque-review-token.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0072-use-a-small-http-status-set-and-one-business-error.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0073-use-limit-offset-and-fixed-server-side-sorting.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0074-expose-minimal-business-dtos-and-an-operational-health-view.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0075-use-four-discriminated-human-review-commands.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0076-separate-six-business-causes-from-insufficient-evidence.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0077-freeze-the-five-state-case-lifecycle.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0078-derive-four-batch-business-states-from-current-cases.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0079-separate-current-batch-projection-from-run-history.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0080-reconcile-interrupted-runs-before-serving-requests.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0081-use-one-app-scoped-executor-and-stop-between-stages.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0082-anchor-runtime-data-to-the-project-with-a-local-override.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0083-use-fixed-loopback-defaults-with-explicit-port-overrides.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0084-lock-each-runtime-directory-with-the-windows-os.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0085-create-bounded-sqlite-backups-before-real-migrations.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\docs\adr\0086-use-a-verified-local-launcher-and-instance-scoped-stop-request.md

### 背景与未决边界

- E:\xtzj\Desktop\【临时】纪要处理\项目建设\00-当前项目唯一基线.md
- E:\xtzj\Desktop\【临时】纪要处理\项目建设\02-需求澄清-待决定事项.md
- E:\xtzj\Desktop\【临时】纪要处理\【记忆交接handoff】\8-PSIT多模态情感感知MVP需求澄清-26.9.2.md

## Out of Scope

### 1. 数据与业务范围

- 持续扫描全部客户并主动发现异常。
- 接受任意原始文件并完成完整数据治理。
- 把 Olist、E Commerce Dataset、JDDC 强行拼接为同一真实客户。
- 复制、扩增、重新编号异源数据后冒充真实关系。
- 自动联系客户、自动退款、自动发券、自动执行补偿。
- 完整 CRM、客户资料库、营销活动管理。

### 2. 模态与算法

- 音频输入、音频特征、音频界面、音频验收。
- 视频输入、视频特征、视频界面、视频验收。
- 自研多模态模型。
- 模型微调。
- 复杂模型训练。
- 训练型 Feature Fusion。
- Cross-Attention 融合网络。
- Transformer 融合网络。
- 把 RAG、知识图谱、CLIP、SFT、RL、深度聚类设为必做。

### 3. 技术路线

- 在 PRD 中确定 Claude Code Agent Teams 为产品运行时。
- 使用 Claude、其他供应商模型或本地推理作为运行时备用路线。
- 多模型自动路由、供应商故障自动切换和动态模型选择。
- Redis、Celery 或其他独立任务队列。
- WebSocket 或 SSE 实时通道。
- 批次停止、取消和恢复流程。
- 多个活动批次和自动批次队列。
- 局域网多人访问、公网云部署、HTTPS 和公网身份认证。
- GraphQL、无版本接口和通用后台管理 API。
- 登录页、模型配置页、技术日志页和独立数据管理系统。
- `aiosqlite`、独立数据库服务和多进程数据库写入。
- 在业务页面或业务 API 中暴露数据库结构、运行编号、数据版本、Schema、RFM 方法、Agent 阶段、Prompt、Token、模型日志或原始 JSON。
- 让三个 Agent 分别实现智谱 SDK 调用、重试和鉴权。
- 向感知或归因 Agent 提供高价值结论、RFM 过程或动作目录。
- 向归因和策略阶段重复发送原始图片。
- 在页面在线编辑 Prompt，或在 Prompt 中保存 API Key 和验收答案。
- SDK 自动重试与 `RunService` 重试同时开启。
- 超过一次定向结构修复，或超过每阶段四次供应商请求。
- 在业务页面提供模型参数、API Key、Prompt 或技术日志配置入口。
- 把 API Key、`.env`、成员电脑绝对路径或 Codex 缓存路径提交进仓库。
- 不提交依赖锁文件，或允许成员自行选择不兼容的 Python、Node 和包管理器版本。
- 使用无固定字段、无 `trace_id`、不轮换或记录完整客户内容的技术日志。
- PoC 未通过时修改密封答案、删除失败案例、降低证据规则、只保留成功结果或静默降低硬门槛。
- Apple 官网式产品摄影、全屏产品 Tile、营销 Hero 和巨大标题。
- AIDA 营销页面结构、随机版式、GSAP、视差、滚动锁定、无限轮播和磁性按钮。
- 用 Bento、三个等宽统计卡或装饰性大屏承载案例队列和人工确认。
- AI 紫色渐变、满页玻璃拟态、卡片投影、假截图、假客户 Logo 和装饰性业务数据。
- 安装或执行 GitHub `taste-skill`，或让外部 Skill 覆盖本项目业务、安全和技术约束。
- 手机端、平板专用布局和双主题切换；本期只验收 1366×768 与 1024×768 桌面页面。
- 第二套组件库或图标族，以及 Tailwind CSS、Redux、Zustand、GSAP、Motion。
- 手写并独立维护与 FastAPI OpenAPI 重复的前端业务接口类型。
- 使用 `localStorage` 保存或覆盖批次、案例、分析结果和人工最终结果。
- 上传后自动开始模型分析，或只通过浏览器校验决定 ZIP 合格。
- 让每次组件测试和浏览器回归都调用真实 GLM，或用确定性测试结果声称真实模型可用。
- 在生产构建或业务页面提供测试模型替身开关。
- 运行时从 CDN 加载字体、图标、脚本或必要页面资源。
- 四个独立仓库，或把四个人长期按数据库、API、组件和测试横向分工。
- M1 与 M2 直接互调、M4 绕过 M3、跨模块传递 ORM 或 SDK 原始响应。
- 建立承载业务实现但没有主负责人的第五个 `common`、`utils` 或 `shared` 模块。
- 等到最终联调才定义模块接口，或由每个模块复制并维护自己的合同。
- 四个模块在一个大 PR 中首次接线，或普通任务同时修改多个模块内部实现。
- 只由模块主负责人理解和审查自己的代码。
- 共享合同目录依赖 FastAPI、SQLAlchemy、智谱 SDK 或任何模块实现。
- 一个不断扩张的 DataGateway、M3 直接操作 ORM 或把 M1 五个端口拆成五个服务进程。
- M2 直接读写数据库、启动独立服务或把证据不足当技术失败。
- 让数据库事务覆盖 GLM 网络等待，或由 M1 自行决定业务状态流转。
- 通过全局变量、隐藏参数或页面开关切换真实模块与 Fake。
- 演示模式依赖 Vite 开发服务器、Docker 或两个手工启动终端。
- 业务 API 返回 `batch_run_id`、`case_run_id` 或要求前端提交内部运行编号。
- 不使用 `review_token` 的旧结果并发保护，或把 Token 展示为业务内容和登录凭证。
- 通用 CRUD、运行与阶段详情、模型调用、配置、日志和永久删除 HTTP 接口。
- 全部错误返回 200、FastAPI 默认 `detail`、HTML 500 或供应商原始错误透传。
- 客户全文搜索、任意 `sort_by`、复杂组合查询和游标分页。
- 返回 ORM、通用 `data: any`、充满无意义 `null` 的页面 DTO。
- 使用 JSON Patch、任意内部 JSON 或一个充满可选字段的人工确认请求。
- 把 `INSUFFICIENT_EVIDENCE` 当成第七种真实风险原因或合并进 `OTHER`。
- 案例已完成后重跑、重新审核或无痕修改，以及为页面增加 Agent 阶段、暂停、取消和恢复状态。
- 向页面暴露内部 batch_run 状态、线程、Future、排队位置和估算等待时间。
- 人工单案例重跑改写首次批量运行统计，或页面继续显示已经恢复的历史错误数。
- 应用启动后自动从感知或归因阶段续跑并产生模型调用。
- 每个请求、案例或 Agent 创建独立线程池。
- Redis、Celery、多进程 Worker、远程任务队列和自动断点续跑。
- 自动 SQLite 备份、备份轮换和 `backup.ps1`。
- 前端构建内容指纹、构建缓存、临时 `dist` 和原子替换。
- 自动打开浏览器、`-NoBrowser`、quarantine、`recovery.json`、隔离样本保留和 `stop.ps1` 控制协议。

### 4. 商业与生产结果

- 真实企业需求已经验证。
- 生产级准确率。
- 真实留存提升。
- 真实 ROI。
- 真实因果干预效果。
- 企业级安全、权限、审计、扩缩容、灾备。
- 正式生产部署。

### 5. 研究与后续交付

- 论文完整研究方案。
- 真实随机 A/B Test。
- 真实客户触达实验。
- 真实策略成本收益评估。
- 第三份并行开发文档或每张 Task 单独生成本地文件；37 张 Task 统一保存在 `docs/PSIT-MVP技术实施规格.md`。
- 项目仓库和代码。

## Further Notes

### 1. 技术文档前置事项状态

以下状态只说明技术设计可以推进到什么程度，不改变产品目标。

| 编号 | 事项 | 当前状态 | 当前边界 |
|---|---|---|---|
| P1 | 产品输入合同 | 已检查清洗后主表、服务任务 JSONL 和图片引用，v1 最小输入已冻结 | 只接收本需求文档与《PSIT MVP 技术实施规格》列出的来源支持字段；新字段必须重新过准入规则 |
| P2 | 文本、图片、行为特征字段 | v1 最小结构已冻结，模型结构稳定性仍需 PoC 验证 | 图片只输出事实与图文关系，行为只使用确定性客户价值和经人工核验的订单事实 |
| P3 | 情绪分类与验收阈值 | 当前情绪枚举已按现有参考数据收敛，最终通过阈值未冻结 | v1 只使用平静、不耐烦、无法判断；不要求情绪变化 |
| P4 | RFM 与案例优先级规则 | RFM 规则和同级排序已冻结，介入等级细则仍需用固定案例校验 | 同级按系统导入时间和稳定 `case_id`；不补造等待时间 |
| P5 | 技术实现路线 | 运行框架、模型接入、存储、前端工程、四模块架构、接口、HTTP、状态机和中断恢复已冻结在《PSIT MVP 技术实施规格》中，模型性能、成本和前端组件实现仍需按 Task 验证 | 当前投影与首次历史分开；启动恢复零模型调用；演示不得回退 Fake；未通过 PoC 的性能数字不得写成已证实结果 |

共同约束：

- 触发条件未满足时，不使用经验值或 AI 猜测补齐。
- 对应事项未冻结前，不启动依赖它的正式实现。
- 新决定只写入本需求文档或《PSIT MVP 技术实施规格》，不再新增并行真源。
- 新决定不得回填成无来源结论。
- P1 至 P5 不改变本 PRD 的项目核心需求目标。

### 2. 使用状态

本文件是唯一需求真源。当前工作目录不是 Git 仓库，因此没有创建 Issue 或 PR。代码仓库建立后，Task、分支和 PR 只引用本文件与《PSIT MVP 技术实施规格》，不复制维护第三份需求正文。

### 3. 最强反对意见

**反对意见**：10 天内同时完成图片必做、多模态联合判断、批量预分析、案例队列、案例详情、人工确认、持久保存、模态贡献对比，范围仍然偏大。任何一个环节做成静态演示，都会让端到端验收失真。

**力度：强。**

本 PRD 不通过继续增加功能来回应该风险。控制方式已经写入范围：

- 只接收上游处理后的标准案例批次。
- 只使用至少 5 个固定案例验证关键分支。
- 不接管完整原始数据治理。
- 不做复杂训练。
- 不做真实业务动作。
- 不做生产级集成。
- 不把技术载体写成产品价值。

如果 10 天执行中必须进一步缩减，优先减少界面装饰、搜索筛选、非核心展示，不删除图片真实参与、证据追溯、案例队列、人工最终确认、持久读回。删除这五项中的任一项，会改变已经确认的产品核心需求目标。
